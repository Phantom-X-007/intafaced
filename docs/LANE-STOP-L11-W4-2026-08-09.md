# LANE STOP — L11 SHELL-DESK · wave 4 · 2026-08-09

```
LANE: L11 wave 4
shipped: #1175 duplicate lang keys + gate · #1225 confirm/fill decimal strings · #1224 book shape errors visible + invalid_response i18n · #1230 shell-i18n blocking gate
in flight: #1221 feedLive honesty + shell-golden CI · #1231 UC/home accept on money reads · #1242 DEX/protocol health custodial shape
parked: shell-brand baseline rename (bitrade package.json/README) · mobile · support form residual (OPS) · hotkeys/subaccounts (goldens green, no open break) · L16 file split (no same-file war on tip)
Nitro must decide: CDN/go-live Class X only — none this wave
SAFE TO CLOSE: yes — babysit in-flight to green merge; no blank §8
tip: 09829ea9 fix(ws): ready says when the trade bus is down and the kill-switch story is true (#1243)
```

## Wall

`vendor/upstream-exchange/05_Web_Front/**` · shell gates · i18n package coord (package stays notify-only; shell `en.js` is UI catalog)

## Shipped (merged)

| PR        | Plain words                                                                                               |
| --------- | --------------------------------------------------------------------------------------------------------- |
| **#1175** | Duplicate `intafaced.socket` key silently ate translations; `lang-duplicate-key` gate now blocks          |
| **#1225** | Order confirm + fill expand print typed/venue decimal strings (no float `toFloor` on fills)               |
| **#1224** | Book/trades shape failures print the real message; `intafaced.reason.invalid_response` exists for IxState |
| **#1230** | `shell-i18n` promoted to GATES — tip had 0 hardcodes; stale “200+” NOT_GATES reason removed               |

## In flight (rebased; CI queue)

| PR        | Done bar                                                                  | Class | Pickup                                                                                               |
| --------- | ------------------------------------------------------------------------- | ----- | ---------------------------------------------------------------------------------------------------- |
| **#1221** | Live only after depth snapshot (not bare TCP open); 11 desk goldens in CI | N     | Merge when Tests green (earlier red was **svc-protocol** `lending-oracle.onchain` flake — not shell) |
| **#1231** | UC balances/orders/fills + home markets run `accept()` before paint       | M-adj | Adversarial note already on PR; merge when green                                                     |
| **#1242** | Dex/Protocol health refuse `custodial:true` via schema on `load()`        | P     | Merge when green                                                                                     |

## Parked with pickup

| Unit                                | Why parked                                                                       | Pickup                                                                   |
| ----------------------------------- | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| Brand baseline drain                | 8 frozen hits in package.json / lock / README / config comments — rename surface | Rename package + author + README; shrink BASELINE rows only after delete |
| Mobile                              | No path residual this wave                                                       | Park unless wall path appears                                            |
| Support form                        | OPS lane residual                                                                | OPS stop                                                                 |
| Hotkeys / subaccounts               | Goldens already green; no false claim found                                      | Re-open only if product asks                                             |
| L16 file split                      | Open Dependabot PRs only on Java folders; L11 sole desk writer                   | Continue wall discipline                                                 |
| Header last/24h still REST snapshot | #1221 badge says “Depth live” — does not invent ticker WS                        | Optional: Snapshot label on last/24h or ticker stream (product)          |

## Engine B (promise pass)

| Claim                               | Verdict                                          |
| ----------------------------------- | ------------------------------------------------ |
| False Live on open socket           | Fixed in #1221 (pending merge)                   |
| Shape fail looks like outage        | Fixed #1224                                      |
| Float free paints as balance        | Fixed in #1231 (pending merge)                   |
| custodial:true paints on DEX health | Fixed in #1242 (pending merge)                   |
| Empty book vs unreachable           | Already three-state; shape message now preferred |

## Nitro-only

CDN / go-live Class X — **none this wave**.

## Tip ritual (re-derive)

```bash
git fetch origin && git log -1 --oneline origin/main
gh pr list --state open --search "shell"
```
