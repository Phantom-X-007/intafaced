# D26-P1-T10 — Seed/mm honesty backend

**Lane:** `denon-d26-p1-t10-mm-seed-honesty`  
**Branch:** `feat/trade-mm-seed-honesty`  
**Mountain:** `trade.mm-bot` (coordinate; backend honesty only — no FE)

## Done bar

| Criterion               | Proof                                                                                     |
| ----------------------- | ----------------------------------------------------------------------------------------- |
| Seeded flagged          | `MM_SEED_ORDER_SEEDED` on placements + `recordSeededOrder` → `trade.orders.seeded=true`   |
| Not in user volume      | `seedVolumeCountsTowardUserStats() === false`; public tape/candles already exclude seeded |
| Killable                | `mmSeedJobsArmed` + `TRADE_MM_SEED_ENABLED` (jobs + `seedPlaceEnabled`)                   |
| No manufactured crosses | `classifySeedSubmitResult` refuses fills; seed submits are limit `PO` only                |

## Paths

- `services/svc-trade/src/mm/seed-honesty.ts` (+test)
- `services/svc-trade/src/mm/seed-market.ts` / `seed-jobs.ts`
- `services/svc-trade/src/index.ts` (wire kill + recorder)

## Leverage

Phase A IN: existing `src/mm/**` seeder + spot `orders.seeded` / publicTape SD-3 — extend honesty, do not rebuild MM or invent mids/crosses.
