# REGROUP — 2026-08-03

**Status:** current · **Tip when written:** `824365b` · **Re-derive before acting:** `git fetch && git log -1 --oneline origin/main`

Written by the product-surfaces session (chat B) after landing seven branches and running the fleet end to end. This is the board both chats should read. It supersedes nothing — `docs/COORDINATION-TRUTH-LAYERS.md` is still the law on who-owns-what, `tooling/tracker/features.mjs` is still the product map.

---

## 1 · The one decision that changed everything today

**`apps/web` retires. The terminal ports into the Vue shell.** Owner decision, recorded in [`docs/adr/2026-08-03-retire-apps-web-port-to-vue-shell.md`](adr/2026-08-03-retire-apps-web-port-to-vue-shell.md), closing the open question left by the 2026-08-02 adoption ADR.

There is now **one** product surface: `vendor/*/05_Web_Front`, served on `:8090`. Everything genuinely built in the Next scaffold moves there first. Nothing is lost, including the reasoning for what is deliberately dropped.

**This is not a pure win and should not be sold as one.** The retired app was better at three things the shell has no equivalent of: a live websocket depth feed with gap detection and resnapshot, runtime shape validation of every response, and decimal-safe arithmetic on the desk. Those three _are_ the port.

---

## 2 · Verified state of the running platform

The fleet was brought up from the documented path and checked end to end. **It is genuinely solid** — 30 containers, all healthy, zero restarts, `migrate` exited 0. There were no failures to report.

What is **serving**: svc-edge, svc-trade (16 real markets from Postgres), svc-matching, svc-ledger, svc-identity, svc-ws, svc-token, svc-bank, svc-p2p, svc-dex, svc-protocol, svc-indexer.

What is **propped, and says so honestly**: svc-pay (`liveRails: []`, both rails sandbox, in-memory reference chain), svc-agents (`providerMode: mock`), svc-blueprint (engine `usable: false`, card renderer unconfigured), svc-academy (`stream: none`), svc-notify (in-app only; email/push/sms `channel.not_configured`).

What is **dead**: the shell's `/uc/*` announcement and advertisement calls (405 — retired Java prefix), the `/ws` proxy path, the `PRICE TREND` column.

### Facts that change the plan

- **`/exchange/btc_usdt` is clean and truthful.** Em-dashes throughout, "No feed · not live prices", "No candles yet", zero console errors. The desk is in good shape.
- **The landing page is not.** All sixteen markets render the literal string `null` where a price belongs, each with a **green ▲**. The arrow is the untrue part — `parseFloat(null) < 0` is `false`, so no-price defaults to up. Sixteen markets with no data are being shown as rising.
- **`PRICE TREND` throws on every row.** `Index.vue:408` reads `params.row.trend.length`; `ix-trade.js` never emits a `trend` key. The crash is masking a literal **25-zero array** staged as a price history for markets that have never traded.
- **No invented price literals remain in the served bundle.** 61 JS chunks, 2.2 MB scanned — the five historical ones are gone. The untruth on this surface is now behavioural, not literal.
- **`apps/web` is already non-functional in a browser.** svc-edge sets no CORS headers, so every call from `:3000` is blocked. It degrades honestly (`PLATFORM UNREACHABLE`, `LEDGER SNAPSHOT — NOT WIRED`) but it is dead. **Nothing proxies to `:3000`** — confirmed against both compose files, all docs, and the shell's nginx. Retiring it breaks no route.
- **`docs/RUNNING.md` is stale by nine containers and two services.** It says "twenty-one containers" and omits svc-notify, svc-academy, vendor-shell and both anvils.

### Housekeeping on the host

A stray `intafaced-shell-web` container (exited, no compose project) holds a **stale `8090` binding claim**. If anyone starts it, it collides with `vendor-shell`. There is also an unrelated `intafaced-coinex-otc` from another worktree, exited 1 on a Shiro classpath error.

---

## 3 · The blocker nobody had found

**The depth/tape port cannot proceed yet, and it is not a shell problem.**

1. **The `/ws` proxy has never routed.** nginx sends `/ws` → `svc-ws:4014` with no URI part, so the path passes through unchanged. svc-ws serves `/stream` and `/private/stream` — there is no `/ws` route, and it 404s. Worse, `location /` catches `/stream` before it could reach a proxy. **There is no URL on `:8090` that reaches the depth stream.**
2. **svc-ws and svc-edge market-ID namespaces have zero overlap.** Edge exposes 16 markets, svc-ws knows 10, intersection **0**. A direct `101 Switching Protocols` against svc-ws closes immediately with `unknown market "a7cc445e-…"`. svc-ws derives its list from **svc-matching**.

So dropping a websocket client into the shell would not have worked, and fixing nginx alone would not have worked either. **svc-matching and svc-ws are the platform-integrity lane's services.** This needs that lane, or an explicit handoff, before any depth work is worth starting.

---

## 4 · Landed on main today

| PR   | What it actually fixed                                                                                                                                                                            |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #434 | the vendored shell no longer carries the upstream vendor's identity — verified by independent grep and against the built bundle, because `brand-scan` skips `vendor/` entirely and proved nothing |
| #435 | i18n reported **100% coverage for 27 languages with not one word written**; Arabic layout-mirrored English; svc-notify stamped English margin calls `ar`                                          |
| #442 | the Academy could never have started — compose withheld the one secret its env demands; proven by counterfactual, not inferred                                                                    |
| #440 | svc-dex ran with **no tracker row at all** (2,117 lines, 87 tests, an edge route); `custody-scan`'s service list was correct by luck and is now checked                                           |
| #444 | three `token.*` rows claimed done where nothing tallies, closes or executes; 9 of 11 file:line citations were stale                                                                               |
| #447 | the admin console had two freeze paths — one real and unreachable, one reachable and fake; 30 tests, mutation-proven                                                                              |
| #449 | **the fabricated-money gate** — see below                                                                                                                                                         |

### Why #449 mattered most

`apps/web/src/testing/fabricated-money.ts` was the **only automated check in this repo that reads rendered output and fails on a money-shaped literal no service supplied**. Its two consumers were `apps/web` tests. Deleting that app before porting the check would have reopened the exact conditions that put five invented prices on the old landing page for months.

It is now `tooling/ci/fabricated-money-scan.mjs`, enforcing, with 12 real findings frozen as a ratchet — **by exact matched text, not by count**, because a count lets a fixed violation and a fresh one cancel out silently.

---

## 5 · In flight — branches pushed, PRs not yet open

All four are committed with honest messages stating what is done and what is not. None of them claims to be finished.

| Branch                        | Done                                                                                                                             | Not done                                                                                                     |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `fix/shell-money-on-the-wire` | `ix-money.js` on the vendored bignumber.js, **mutation-proven** against the float it replaces; `book-honesty.js` ported onto it  | `Exchange.vue` gains only the `require` — its `num`/`fmt`/`floor`/`groupPlate` still float                   |
| `fix/shell-wire-validation`   | `ix-wire.js`, 40 schemas; refuses JSON-number money, 19dp, and `custodial: true`; `INVALID_RESPONSE` threaded into the transport | no golden test; no `ix-trade.js` call site adopts a schema yet, so nothing is enforced on the live read path |
| `fix/shell-landing-honesty`   | the three strings (`notTraded`, `noneTraded`, provenance) in `en.js`                                                             | not wired into `Index.vue` — the green ▲ and the `null` are still live                                       |
| `docs/retire-apps-web`        | the ADR; `web.terminal` repointed to `Exchange.vue`                                                                              | `web.shell` deliberately untouched — see §7                                                                  |

---

## 6 · Task queue, in dependency order

**Product-surfaces lane (chat B)**

1. **Convert `Exchange.vue`'s call sites to `ix-money`.** This is the actual money-on-the-wire fix. `useBookPrice` (`~:2234`) and `applyPercent` (`~:2255`) still write float-derived values into `form.price` / `form.amount`, and `toCreateOrderBody` sends them verbatim to `POST /api/v1/orders`. Until this lands, the comment at `ix-trade.js:12-20` — _"the string the user typed, never a round-tripped float"_ — **is false**.
2. **Wire the landing copy into `Index.vue`.** Both duplicated column sets. Kills the green ▲ and the `null`. Delete or make real the `PRICE TREND` column; do not leave the 25 zeros.
3. **Give the announcement strip a stated reason** instead of an empty error toast, using the existing `sockets.js` / `IxNoSurface.vue` machinery rather than new copy.
4. **Add `ix-wire.golden.js`** and adopt schemas at the `ix-trade.js` read sites.
5. **Then** the remaining terminal ports: socket panel (extend `IxNoSurface.vue` to take an inline entry — today it can only render eight hardcoded rows, none of them trading), plane switch, sign-in TOTP + tier, the order-ticket refusal ladder.

**Platform-integrity lane (chat A) — requests, not instructions**

- **The websocket namespace blocker in §3.** svc-ws ↔ svc-edge market IDs, and the `/ws` route. Nothing depth-related can start without it.
- **Five PRs conflict because of what this lane merged today** — #425, #423, #432, #436, #438. Mostly regenerated tracker files, which rebase cleanly. **#436 is the awkward one**: my admin work rewrote `kill-switch-board.tsx` and `vitest.config.ts` underneath it.
- **Both i18n gates go vacuous when `apps/web` dies.** `i18n-bypass-scan.mjs:52` roots at `apps/` with `apps/admin` allowlisted, and all 15 baseline rows are `apps/web` paths; `i18n-scan.mjs` has the same root and only reads `.tsx`. Emptying the baseline leaves a gate that walks an allowlisted-only tree and passes forever. Recommended: a non-vacuity assertion — if the walk visits zero non-allowlisted files, fail. Three lines. `findShellRoots` in the new scan is reusable and rename-proof.
- **`brand-scan.mjs` has `vendor` in `SKIP_DIRS`.** Its "955 files, 0 forbidden names" includes none of the shell. Every vendor PR that ever passed it was unchecked.
- **svc-edge sets no CORS headers.** Fine for the same-origin shell; it is why `apps/web` is already dead in a browser.

---

## 7 · The deletion, when the queue above is clear

`apps/web` must go in **one commit**, because any split leaves main red:

- the directory
- `Dockerfile:61` — `COPY apps/web/package.json` fails the image build with `COPY failed: file not found`, and **no gate catches a stale COPY**. `pnpm verify` does not build the image, so only `docker compose build` finds this.
- `docker-compose.apps.yml:707-722` — the `web:` service, plus stale comments at `:347`, `:642`, `:693-705`, `:729`
- `tooling/ci/i18n-bypass-scan.mjs:64-80` — the whole baseline, **plus** a decision on the scan's future scope
- `tooling/tracker/features.mjs` — `web.shell` retitled and repointed to the shell, `dependsOn: ['infra.ui-tokens']` corrected (false of the shell, which defines its own `--ix-*` variables), notes at `:123` and `:343`
- `.env.example:318-332` and `.env:191-205` — `NEXT_PUBLIC_EDGE_URL` / `NEXT_PUBLIC_WS_URL` have no consumer outside `apps/web`

Then `pnpm tracker` in the same commit, `pnpm verify`, **and `docker compose build`** — that last one is the only thing that catches the Dockerfile.

**`web.shell` is `status: done`, so `tracker.mjs` checks its `requires` path.** It fails the moment the directory goes. `web.terminal` is `wip`, so its path is **never** checked — it is already repointed, because left alone it would have pointed at a deleted directory indefinitely with nothing red.

---

## 8 · Ways of working that were re-learned the hard way

- **Do not commit interrupted work as `wip: NOT reviewed, NOT verified`.** Seven branches carried that message under the wrong git identity and it took four rounds of agents to unpick. Commit at an honest boundary with a message stating what is and is not done, or leave it uncommitted in the worktree.
- **Commit identity is `D3-HAKI <intafaced@gmail.com>`.** The global `~/.gitconfig` was `theplugXE`; repo-local config is now correct, but check `git log --format='%an'` before pushing.
- **Never `git stash`.** It is shared across every worktree here and an agent already popped someone else's.
- **Path-intersect before merging**, not after: `gh pr diff <n> --name-only` against the other lane's PRs, then `git merge-tree --write-tree` for a real conflict answer. Two PRs touching the same file often do not conflict.
- **A green scan is not evidence until you know what it looks at.** `brand-scan` skips `vendor/`; the i18n gates will soon walk nothing; a gate that passes on an empty walk is worse than no gate.
