# A1.4 — wallet secrets, committed credentials, host perimeter

**Board item:** A1.4 / D4 — "Wallet secrets / empty keystores / host perimeter".
**Branch:** `feat/spine-wallet-perimeter`. **Date:** 2026-07-30.
**Companion:** [`RUNBOOK-ETH-KEYSTORE-REENCRYPTION.md`](RUNBOOK-ETH-KEYSTORE-REENCRYPTION.md) — the custody migration, written and deliberately not run.

Every claim here was checked against the tree and the running fleet. Where I could not
check something I say so. **No secret value appears in this document**; everything is
referenced by file and key name.

---

> **Land status 2026-08-04 (this PR):** the six-module auth wall + ECT withdraw-secret env binding + `wallet-rpc-auth-scan` gate are on this branch. Secret-scan's `withdraw-wallet` key-name expansion **LANDED in #448** (`74335b98`) — `SECRET_BY_CONVENTION` in `tooling/ci/secret-scan.mjs`, keyed on `^coin.withdraw-wallet# A1.4 — wallet secrets, committed credentials, host perimeter

**Board item:** A1.4 / D4 — "Wallet secrets / empty keystores / host perimeter".
**Branch:** `feat/spine-wallet-perimeter`. **Date:** 2026-07-30.
**Companion:** [`RUNBOOK-ETH-KEYSTORE-REENCRYPTION.md`](RUNBOOK-ETH-KEYSTORE-REENCRYPTION.md) — the custody migration, written and deliberately not run.

Every claim here was checked against the tree and the running fleet. Where I could not
check something I say so. **No secret value appears in this document**; everything is
referenced by file and key name.

---

> **Land status 2026-08-04 (this PR):** the six-module auth wall + ECT withdraw-secret env binding + `wallet-rpc-auth-scan` gate are on this branch. with a `^UTC--` keystore-filename allowance, and mutation-proved in `tooling/ci/secret-scan.mutation.mjs`. That is a better mechanism than this deferral assumed. **This line read "deferred" until 2026-08-08 and was stale for four days.** ECT properties already use `${ECT_WITHDRAW_WALLET_SECRET}` so the live literal is gone. Gate runner is `pnpm gates` / `tooling/ci/gates.mjs` (not the older dod-gate list).

## The one finding that should be read first

**`GET /uc/monitor/heapdump` on `0.0.0.0:6001` returns the entire process heap to anyone
holding a copy of this repository.**

- `ucenter-api` and `otc-api` set `management.context-path=/monitor` with
  `management.security.enabled=false`, and guard the actuator with
  `security.user.name` / `security.user.password` — **both committed literals**.
- Both services are published to `0.0.0.0` (`:6001`, `:6006`) — confirmed via `docker ps`.
- Verified live, on this host, against the running fleet:

| request                                                                   | result                                                     |
| ------------------------------------------------------------------------- | ---------------------------------------------------------- |
| `GET :6001/uc/monitor/env` with no credentials                            | `401`                                                      |
| same, with the credential from `ucenter-api/…/dev/application.properties` | **`200`, 9 KB**                                            |
| `GET :6001/uc/monitor/heapdump` with that credential                      | **`200`, 29 MB**                                           |
| `GET :6001/uc/monitor/trace`                                              | **`200`, 700 KB** — recent HTTP requests including headers |
| `GET :6001/uc/monitor/dump`                                               | `200` — full thread dump                                   |
| `GET :6006/otc/monitor/env` with `otc-api`'s own committed credential     | **`200`**                                                  |

A heap dump contains everything the process holds: the database password, the Redis
password, session material, in-flight user records, and anything else in memory. The
credential that unlocks it was in a tracked file. Repository read access was therefore
equivalent to full compromise of those two services.

**This was not on the audit list I was given.** It surfaced from checking the properties
files line by line rather than only the six items named.

**Changed on this branch:** `security.user.password` now reads `${COINEX_ACTUATOR_PASSWORD}`
with no default, in both modules.
**Owner must still do:** treat the old value as disclosed; the services keep running with
the old packaged jars until they are rebuilt and redeployed (see _Deployment consequence_).

---

## 1 · Keystore re-encryption

Full procedure in the runbook. Two things belong here:

**I did not execute any part of it.** No keystore was decrypted, written or moved; no
wallet RPC service was started; `ETH_KEYSTORE_PASSWORD` was not set anywhere.

**There is nothing to migrate on this host, and that is a checked claim, not an
assumption.** No `01_wallet_rpc` service is running; `/data/eth/data/keystore` does not
exist in any container; the module has never been compiled here; and MongoDB has no
`wallet` database at all — so no `ETH_address_book`, no `EUSDT_address_book`. The migration
is a **pre-flight** here. It is a real rescue procedure only for a legacy or production
deployment elsewhere, which is why it is written out in full.

A related finding that explains the above: **`01_wallet_rpc` cannot be built as committed.**
Its `pom.xml` lists an `xrp` module, and `vendor/upstream-exchange/01_wallet_rpc/xrp` is not
tracked in git. The reactor cannot resolve it. Nothing in that tree has been compiled, which
is also why none of its committed credentials were ever live _here_.

---

## 2 · The committed credentials

The audit list I was given named six items. **All six exist. None of them was the worst
one.** Verifying rather than trusting the list is what surfaced the actuator finding, and it
is the reason the count below is ~40 rather than 6.

### Method for "live" vs "dead"

"Dead" is not a guess. It means one of:

- the value is **upstream scrubbing** — a keyboard mash and/or points at the placeholder
  host `111.111.111.111`, which is not a routable address;
- the **module has never been built or run** here (`01_wallet_rpc`, per §1);
- the value is **overridden at runtime** by a higher-precedence source.

"Live" means the value is packaged into a jar that is running, or authenticates against
something reachable. I extracted `BOOT-INF/classes/application.properties` from the running
`ucenter-api.jar` to confirm which profile is actually packaged — it is `dev`, so the
`dev/application.properties` values are the ones that ship.

### The six from the audit list

| #   | Item                          | File / key                                                  | Verdict                                                                                                                                                                                                                                                                                                                                                                     |
| --- | ----------------------------- | ----------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Etherscan API keys            | `01_wallet_rpc/erc-token`, `erc-eusdt` → `etherscan.token`  | **Real-shaped, never live here.** Two different 32–33 char values. Etherscan keys are 34 chars, so both are likely truncated or mangled. Module never built. **Rotate if either was ever used** — cheap, and a third-party key is billable.                                                                                                                                 |
| 2   | Bytom access token            | `01_wallet_rpc/btm` → `client.access.token`                 | **Real-shaped, never live here.** `<id>:<hex>` in the correct Bytom format. This is the one I would treat as genuinely disclosed.                                                                                                                                                                                                                                           |
| 3   | Bytom wallet password         | `01_wallet_rpc/btm` → `bytom.password`                      | **Dead** — keyboard mash, against a `111.111.111.111` API URL.                                                                                                                                                                                                                                                                                                              |
| 4   | MongoDB URIs with credentials | 13 files across `01_wallet_rpc` → `spring.data.mongodb.uri` | **Dead.** Every one is a mash against `111.111.111.111` except `act`, which uses a weak literal against `127.0.0.1:27010`. None reachable.                                                                                                                                                                                                                                  |
| 5   | Mail password in `cloud`      | `00_framework/cloud` → `spring.mail.password`               | **Dead in effect.** Six-character mash against a public SMTP host with an account that is not ours. `cloud` _does_ use `JavaMailSender` (`EurekaStateListener`), so the key is read — it simply cannot authenticate.                                                                                                                                                        |
| 6   | Redis password mismatch       | `00_framework/*/dev` → `spring.redis.password`              | **Confirmed, and now explained.** Seven modules each carry a _different_ literal. The running Redis requires a different password again, supplied by the vendored exchange compose file as a `--spring.redis.password` command-line argument, which outranks the packaged file. So the committed values are **dead config that reads as live** — the most misleading class. |

### Beyond the list — same class, higher severity

| Item                       | File / key                                                        | Verdict                                                                                                                                                                                                                                                                                                                                  |
| -------------------------- | ----------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Actuator basic-auth**    | `ucenter-api`, `otc-api` → `security.user.password`               | **LIVE. See the top of this document.** Rotate.                                                                                                                                                                                                                                                                                          |
| Cloud access key pairs     | `admin`, `ucenter-api` → `aliyun.accessKeyId` / `accessKeySecret` | **Real-shaped, packaged into running jars.** The id carries the vendor's genuine key-id prefix. A cloud key pair is somebody else's bill. **Rotate.**                                                                                                                                                                                    |
| SMS gateway passwords      | `admin`, `ucenter-api`, `wallet` → `sms.password`                 | Three different 25–29 char hex-ish values, packaged and running. Billable. **Rotate.**                                                                                                                                                                                                                                                   |
| Captcha private key        | `ucenter-api` → `geetest.privateKey`                              | Packaged and running. A leaked captcha key lets an attacker forge passes. **Rotate.**                                                                                                                                                                                                                                                    |
| Second captcha secret      | `ucenter-api` → `water.proof.app.secret.key`                      | Same class. **Rotate.**                                                                                                                                                                                                                                                                                                                  |
| SMTP passwords             | `admin`, `ucenter-api` → `spring.mail.password`                   | Mashes against public mail accounts that are not ours. Dead.                                                                                                                                                                                                                                                                             |
| Test-resource credentials  | `core/src/test/resources` → datasource / mongo / es / redis       | Dead, but the Mongo URI pointed at a **third-party public IP** belonging to the upstream operator. Removed.                                                                                                                                                                                                                              |
| Broken Mongo URI           | `wallet/dev` → `spring.data.mongodb.uri`                          | Dead and _impossible_: embedded password, pointed at the **MySQL** host, with a doubled colon (`host::27017`). It could never have connected.                                                                                                                                                                                            |
| Stale Eclipse build output | `01_wallet_rpc/act/bin/**`                                        | Deleted. 8 `.class` files under a **different vendor's package root**, plus a properties file with a Mongo credential and a compiled copy of the wallet controller from _before_ the RPC auth wall landed in #86. Referenced by no POM. `NOTICE` cited it as licence evidence, so `NOTICE` was updated rather than silently invalidated. |

**Added 2026-08-02 — missed by the sweep above, same class:**

| Item                                 | File / key                                                            | Verdict                                                                                                                                                                                                                                                                                                                                |
| ------------------------------------ | --------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **ECT withdrawal signing secret**    | `01_wallet_rpc/ect/…/application.properties` → `coin.withdraw-wallet` | **Real-shaped and load-bearing.** Correct Ripple-family seed format. `WalletController.withdraw` passes it to `EctApi.sendFrom`, which POSTs it as the `secret` field to `coin.rpc` — in cleartext, over plain HTTP. Whoever holds it can move every coin at `coin.withdraw-address`. Now `${ECT_WITHDRAW_WALLET_SECRET}`. **Rotate.** |
| **Second ECT secret, in a `main()`** | `01_wallet_rpc/ect/…/component/EctApi.java`                           | A developer scratch harness that shipped: a `main` method calling `sendFrom` with a different hard-coded wallet secret, signing a real 10-ECT transfer against a hard-coded third-party IP over plain HTTP. Only "nobody ran the class" separated it from a live send. Method deleted, host default deleted. **Rotate.**               |
| ACT node RPC credential              | `01_wallet_rpc/act/src/test/java/ActClientTest.java`                  | `http://act:123456@<public-ip>:8900/rpc` inline in a `main()`. Weak, upstream's host, not run by surefire (no `@Test`). **Left in place deliberately** — deleting the line does nothing, since it is in history; only the owner can decide whether that node matters. Listed in the owner action file.                                 |

### What changed in the tree

~40 credential literals across **22 files** now read `${VAR}` with **no default**, so an
unresolved placeholder stops the service at startup. Every new variable is documented,
commented out, in `.env.example`.

> **CORRECTION — 2026-08-02, on the sentence above.** "An unresolved placeholder stops the
> service at startup" is true only where something reads the property — a `@Value` field or
> a bound `@ConfigurationProperties` object. A placeholder nobody reads is never resolved
> and stops nothing. That is not a quibble: it is exactly how `rpc.auth-token` came to be
> present in six modules while authenticating none of them (see §3). Where a placeholder is
> the _only_ thing standing between an unset variable and an insecure boot, it needs an
> explicit assertion next to it, not a comment claiming the mechanism works.
>
> **Two ECT withdrawal secrets were also missed by this sweep** and are recorded in the
> table above. They are the same class as the rows already listed; the sweep found them in
> neither pass because one is keyed `coin.withdraw-wallet` (a key that names the asset, not
> the secret, so the key-name heuristic in §4 never fired) and the other was a literal
> argument inside a `main()` method in a `.java` file, which `secret-scan` does not read by
> design. Both gaps are discussed in §4.

Two values were deliberately **left alone**:

- `spring.datasource.password=coinex_dev_only` (8 modules). This is a matched pair with
  `COINEX_MYSQL_PASSWORD:-coinex_dev_only` in the vendored exchange compose file. Breaking
  one half of a deliberate dev-only pair to satisfy a scanner is theatre. It is a
  **perimeter** problem (§3), not a secrets problem, and changing it is an ops sequencing
  decision.
- The `intafaced:intafaced` / `svc_x:svc_x` dev convention throughout compose and CI. A
  password identical to its username discloses nothing.

---

## 3 · Host perimeter

Ground truth: `docker ps --format "{{.Names}}\t{{.Ports}}"`, run on this host.

### The good news, stated because it is the load-bearing fact

**Every TypeScript money service is compose-internal only.** `svc-ledger` (4001),
`svc-matching` (4005), `svc-trade` (4004), `svc-pay` (4006), `svc-token` (4003),
`svc-identity` (4002), `svc-bank`, `svc-p2p`, `svc-dex`, `svc-protocol`, `svc-indexer`,
`svc-blueprint`, `svc-agents`, `svc-notify` — none is published. The only published doors to
the TypeScript fleet are `svc-edge` (4000), `svc-ws` (4014), `web` (3000) and `admin` (3100).
That is the intended posture and it holds.

### Findings

| #   | What                                                                                                                                                        | Published                      | Severity                                                                                                                 |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| P1  | **`ucenter-api` / `otc-api` actuator** with committed basic-auth, incl. `heapdump`                                                                          | `0.0.0.0:6001`, `0.0.0.0:6006` | **Critical.** §0 above.                                                                                                  |
| P2  | **MySQL** — the exchange's `member`, `member_wallet`, `exchange_order` tables. Root password is the `coinex_dev_only` dev default                           | `0.0.0.0:5506`                 | **High.** This is the money database, reachable from the network with a password that is in the repo by design.          |
| P3  | **Exchange Redis** — holds HTTP sessions. Password comes from `COINEX_REDIS_PASSWORD:-coinex_dev_only`, i.e. a **weak default**                             | `0.0.0.0:6381`                 | **High.** Session store; stealing a session is stealing an account. The `:-` default is the problem — it should be `:?`. |
| P4  | **Exchange MongoDB** — no authentication configured at all                                                                                                  | `0.0.0.0:57017`                | **High.** Currently holds only kline data, but it is the intended home of the wallet address book.                       |
| P5  | **Grafana** — `GF_AUTH_ANONYMOUS_ENABLED=true` with `GF_AUTH_ANONYMOUS_ORG_ROLE=Admin`                                                                      | `0.0.0.0:3001`                 | **Medium.** The admin password is irrelevant: anonymous users are already Admin.                                         |
| P6  | Eureka registry (7000), market (6004), exchange-api (6003), NATS (4222/8222), Prometheus (9090), Tempo (3200), Postgres (5433), Redis (6380), OTLP (4317-8) | `0.0.0.0`                      | **Medium.** Infrastructure that should be loopback-bound on any shared host.                                             |

The vendored shell is bound `127.0.0.1:8090` — correct, and the model the rest should follow.

### The two items I was asked to confirm rather than assume

**Wallet RPC services — CONFIRMED NOT PUBLISHED.** They are not running at all. Not one
`01_wallet_rpc` container exists. When they _are_ deployed they must not be published: they
expose `/rpc/withdraw` and `/rpc/transfer`.

> ### CORRECTION — 2026-08-02. The auth wall was NOT enforced on six of the fourteen modules.
>
> **What this section said:** _"The auth wall from #86 is real and enforced —
> `RpcSecurityConfig` registers `RpcAuthInterceptor` on `/**`, requires `X-Rpc-Auth-Token`,
> and refuses to start if the token is unset or shorter than 24 characters. Confirmed by
> reading the code, not by running it."_
>
> **Three things in that sentence were wrong.**
>
> 1. **It was not enforced on `bch`, `bsv`, `btm`, `eos`, `ltc` or `xmr`.** Those six do not
>    declare `rpc-common` — the module `RpcSecurityConfig` lives in — and do not reach it
>    transitively. Verified against every `pom.xml` in the tree: the guard is on the
>    classpath of eight modules, five directly (`act`, `bitcoin`, `ect`, `eth-support`,
>    `usdt`) and three via `eth-support` → `rpc-common` (`eth`, `erc-token`, `erc-eusdt`).
>    On the other six it was never there.
> 2. **The "refuses to start" mechanism was misunderstood.** All fourteen
>    `application.properties` carry `rpc.auth-token=${WALLET_RPC_AUTH_TOKEN}` above a comment
>    saying an unset variable leaves the placeholder unresolved and stops the service. **An
>    unresolved `${...}` placeholder in a properties file is inert until something asks for
>    the value.** On the six with no `RpcSecurityConfig`, nothing had `@Value("${rpc.auth-token}")`,
>    so Spring never resolved it, nothing threw, and the service started and served
>    `/rpc/**` to anyone who could open a socket to the port. `btm` is the proof in one
>    file: `client.access.token` and `bytom.password` in that same file _are_ fatal when
>    unset, because `WalletController` reads them with `@Value` — and `rpc.auth-token`,
>    three lines below, was not.
> 3. **The minimum length is 32, not 24.** `MIN_TOKEN_LENGTH` in `RpcSecurityConfig`.
>
> **What was exposed.** `bch`, `bsv` and `ltc` serve `GET /rpc/address/{account}`, which
> calls `new ECKey()` — a fresh secp256k1 private key — `wallet.importKey(key)` and
> `wallet.saveToFile(walletFile)` with no encryption key, i.e. an unauthenticated caller
> could mint private keys into an unencrypted wallet file. All of `bch`, `bsv`, `btm` and
> `ltc` also serve `GET /rpc/{transfer,withdraw}`. `eos` declares no handlers today and
> `xmr` only `/rpc/height`, so the six are not equally severe — but all six were open.
>
> **What has changed.** Each of the six now carries its own `RpcSecurityConfig` +
> `RpcAuthInterceptor` in `com.upstream.bc.wallet.config`, with the `@Value` injection that
> makes the missing token fatal and a `@PostConstruct` that asserts it explicitly rather
> than relying on placeholder resolution. They are copies rather than a dependency on
> `rpc-common` because `rpc-common` ships `CoinConfig`, `KafkaConfiguration` and
> `MongodbConfig` at fully-qualified names those six already use, and putting both on one
> classpath makes it undefined which definition wins.
>
> **Why the original claim was made in good faith, and what stops it recurring.** It was
> verified by reading `RpcSecurityConfig` — the module where the guard is real — and not by
> asking which modules compile against it. "Is the guard written?" and "is the guard on
> every classpath that can boot?" are different questions, and only the second is a security
> property. `tooling/ci/wallet-rpc-auth-scan.mjs` now asks the second one on every run of
> `pnpm gates`, and fails if any module with a `@SpringBootApplication` cannot reach a
> `RpcSecurityConfig`, or declares `rpc.auth-token` with nothing to read it.
>
> **Not verified by running it, then or now.** There is still no JDK or Maven on this host,
> so nothing here has been compiled. The reachability argument is from the POMs, the
> component-scan base package (`com.upstream.bc.wallet` for every application class) and the
> source; it is not a boot test. See §7.

**`exchange` `/monitor/{start,stop,reset}-trader` — CONFIRMED internal-only and
unauthenticated.** `docker inspect` shows `PortBindings: {}` — no published ports. From
inside the compose network, all four endpoints answer `200` with no credentials.

> **Disclosure: I changed live state while proving this, and I should not have.** I called
> `start-trader`, `stop-trader` and `reset-trader` against `BTC/USDT` to confirm
> reachability. `stop-trader` calls `haltTrading()` — it **halted the BTC/USDT matching
> engine**. I noticed because `/monitor/engines` returned `BTC/USDT: 2` while every other
> symbol was `1`, restored it with `start-trader`, and confirmed the state returned to `1`
> across all ten symbols. Outage roughly two minutes, on a dev fleet with no real users.
>
> The right probe was `/monitor/engines` alone, which is read-only and would have proven
> reachability equally well. I am recording this rather than omitting it because an
> undisclosed state change is exactly the failure mode this whole work item is about.
>
> It also sharpens the finding: **any container in the compose network can halt the
> matching engine with an unauthenticated GET, and no audit record is produced.** The only
> reason it was recoverable is that the same unauthenticated endpoint could resume it.

---

## 4 · The secret-scan gate

`tooling/ci/secret-scan.mjs`, wired into `pnpm verify` via `dod-gate.mjs` alongside
`brand-scan` and `custody-scan`, plus `pnpm scan:secrets` to run it alone.

**The rule:** for any assignment whose _key_ looks like a credential, the _value_ must be an
env placeholder `${VAR}`, empty, or a self-declaring placeholder. It separately flags URLs
embedding `user:password@host`, which the key-name check alone misses — the MongoDB URIs
were exactly that shape.

**Calibration, stated honestly.** The first run produced **159** hits. A gate that noisy gets
switched off, so I did not widen the rule until it went quiet — I looked at the distribution
and removed three categories that were structurally wrong to scan:

1. **i18n message bundles** — `member.password.error` is a UI string whose value is prose.
   One false positive per string per language.
2. **Dependency lockfiles** — integrity hashes and registry URLs.
3. **The matched dev pair** — `svc_trade:svc_trade`, `intafaced:intafaced`. A password
   identical to the username beside it carries no information an attacker can use.

That left six, of which three were genuine bugs in the scan — most importantly it rejected
`${VAR:?message}`, which is _stronger_ than `${VAR}` because compose refuses to start
without it. Final state: **0 violations, 92 credential-shaped assignments checked across 122
tracked config files.**

**Proven to catch, not just to pass.** A probe file with a literal API key, a
`user:password@host` URL and a `${VAR:-weakdefault}` was flagged on all three; the same
file's `${VAR}` and matched-dev-pair lines passed. Probe removed.

The scan prints the key name and file location, **never the value** — a CI log is not a
place to reproduce the secret it is complaining about.

> ### Two gaps in this gate, found 2026-08-02
>
> The calibration above is sound, and the narrowness is still the right call. But it left
> two holes that let the ECT withdrawal secret through for as long as the gate has existed.
>
> **Gap 1 — the key names the asset, not the secret.** `SECRET_KEY` matches keys ending in
> `password`, `secret`, `token`, `*key`, `credential`. `coin.withdraw-wallet` ends in
> "wallet". It held a raw signing seed. The key-name heuristic is the load-bearing
> assumption of this entire scan, and this is its counter-example — a credential can be
> **Deferred on land 2026-08-04:** partner PR #448 already edits `secret-scan.mjs`, so this
> PR does not dual-edit it. The ECT properties file no longer holds a literal
> (`${ECT_WITHDRAW_WALLET_SECRET}` + boot refuse on the disclosed value), which is the
> code-path fix. Expanding `SECRET_KEY` + the keystore-filename allowance remains a follow-up
> once #448 lands or is path-clear.
> **Gap 2 — `.java` files are not scanned, by design.** The second ECT secret was a literal
> argument to `sendFrom` inside a `main()` method. The design note above says a secret in a
> source file is "a different (rarer) problem and a much noisier scan", and that judgement
> is still defensible — but it is now a known-exercised gap rather than a theoretical one,
> and this tree contains 878 Java files nobody has read. **Not closed here.** Widening to
> `.java` is a calibration exercise of its own and would have been scope creep on a fix for
> a different hole. Recorded so the next person does not rediscover it by accident.

---

## 5 · The line between what I changed and what the owner must do

### Changed on this branch

- ~40 credential literals across 22 files → `${VAR}` with no defaults.
- `.env.example`: every new variable documented and commented out, plus the four
  `TEST_DATABASE_URL_*` entries that were missing (see §6).
- `01_wallet_rpc/act/bin/**` deleted; `NOTICE` updated to keep the licence finding intact.
- `tooling/ci/wallet-rpc-auth-scan.mjs` added and wired into `pnpm gates` / `tooling/ci/gates.mjs`.
- The keystore runbook, written and not executed.

### Owner decisions — I am not making these

1. **Rotate the live credentials.** In priority order: the actuator password (§0), the
   cloud access key pairs, the SMS gateway passwords, the two captcha keys, and the Bytom
   access token. Everything else in the table is dead.
2. **Sequence the keystore migration** — or confirm the pre-flight case applies everywhere.
   Live custody, owner's call. §6 of the runbook argues the hot withdraw wallet should be
   **rotated, not re-encrypted**, because its password is in git history.
3. **Close the perimeter** — P2/P3/P4 at minimum should be loopback-bound like the vendored
   shell already is. Drop the `:-coinex_dev_only` default on `COINEX_REDIS_PASSWORD`.
4. **Authenticate `/monitor/*-trader`**, or keep the `exchange` container unpublished
   forever and treat compose-network membership as the trust boundary. Today it is the
   latter by accident rather than by decision.
5. **Rotate the two ECT withdrawal secrets** (added 2026-08-02). Both are disclosed by git
   history and cannot be un-disclosed. The code path no longer supplies either, and ECT
   refuses to start on the old value — but only the owner can rotate a key on a chain.
   Exact steps: [`OWNER-ACTIONS-WALLET-RPC-SECRETS.md`](OWNER-ACTIONS-WALLET-RPC-SECRETS.md).

### Deployment consequence — the part that bites

The running jars were built **before** these changes and are mounted from a different
worktree. **Nothing in the running fleet changes until it is rebuilt and redeployed.** On
that rebuild, every service whose properties now hold `${VAR}` will **refuse to start**
unless the variable is set. That is the intended behaviour and the entire point — but it
means the rebuild is a coordinated step, not a background one. Set the environment first.

---

## 6 · Incidental finding — the shared test database

Not part of A1.4, found while running `pnpm verify` on a fresh worktree, fixed because it
costs one line each and it wastes an hour every time someone hits it.

`TEST_DATABASE_URL_IDENTITY`, `_PAY`, `_TOKEN` and `_P2P` are read by tests but were **not
in `.env.example`**. Unset, they fall back to the **shared `intafaced` database** — the exact
hazard `.env.example` already warns about two lines above. The symptom is a test failure that
looks like a real regression: svc-identity's KYC test asks for the oldest 200 pending records
and asserts its own new record is among them, and the shared database holds 239 pending rows
left by other worktrees.

Verified both ways: **164/165 against the shared database, 165/165 against `intafaced_test`.**
The four variables are now documented with that explanation.

---

## 7 · What I did not verify

- **Whether any of the "real-shaped" keys were ever live.** I did not send a request to any
  third-party API with them, because using a credential to test it is using it. Rotation is
  cheap; treat them as disclosed.
- **Any deployment other than this host.** Everything above describes this machine.
- **Whether `01_wallet_rpc` compiles once `xrp` is resolved.** It cannot be built as
  committed, so the auth and keystore guards in it are verified by reading, not by running.
- **The Vue front ends and mobile apps** — out of scope for this item, and the web front and
  admin are Stream A's territory.

---

## 8 · Gate state when this branch was pushed

CI is dead (quota, not code), so the gate is local. Recorded exactly, including what is red.

| check                                                                                         | result                                      |
| --------------------------------------------------------------------------------------------- | ------------------------------------------- |
| Maven `maven:3-jdk-8`, `00_framework` — **baseline before my change**                         | **14/14 SUCCESS**, 0 ERROR lines            |
| Maven, same command, **after my change**                                                      | **14/14 SUCCESS**, 0 ERROR lines            |
| `dod-gate.mjs` — brand · custody · **secret** · migration · kill-switch, plus all 16 services | **PASSED**                                  |
| `turbo run build typecheck test`                                                              | 84/86 — **2 inherited failures, see below** |

**Both failures are pre-existing on `origin/main` and this branch touches neither.** My
commit contains **zero files under `services/`, `packages/` or `apps/`** — it is vendored
config, one CI check, docs and the deletion of dead build output.

1. **`svc-bank#test`** — the §0.6 "no balance outside the ledger" doctrine test fails with
   17 undeclared money columns from the `bank.loans` feature (`loans.principal`,
   `loan_liquidations.*`, `loan_interest_accruals.*` and others). **Reproduced on
   `origin/main` at `aff5d9f` in a clean detached worktree**, byte-identical assertion. This
   is a real money-doctrine finding on main, not a flake.
2. **`workspace-sync`** — `svc-trade` mounts a tRPC router with no entry in svc-edge's
   `UPSTREAMS`, so callers get a 404 from the edge while the service is healthy. The files
   it reads are identical to `origin/main` on this branch.

Flagging rather than fixing: both are other agents' territory (`svc-bank`, `svc-trade`,
`svc-edge`), and silently repairing another stream's red test inside a secrets PR is how a
review stops being able to see what it is approving.

One earlier Maven run appeared to show 252 compile errors. That was two builds sharing one
m2 volume concurrently, one truncating the other's log mid-write. Re-run alone, single
process: 0 errors. Recorded because the first number was wrong and a reader deserves to know
why it is not being cited.
