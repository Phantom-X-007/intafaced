# Claim — D26-P1-T1f

**owner:** Phantom-X-007 (Denon)
**branch:** feat/futures-funding-net-zero
**status:** LIVE
**paths:** `services/svc-trade/src/futures/funding-*.ts` only (path-disjoint from T1c liquidation-*)

Funding accrues on `runFundingTick` from an externally published rate (never invented). Long/short per-position nets are asserted to sum to zero before margin apply (MVP-6). Public door: same rate book peeks on `GET /api/v1/funding-rate/:symbol` then tick settles conserving.
