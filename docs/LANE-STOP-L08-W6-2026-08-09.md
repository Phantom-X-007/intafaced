# LANE STOP — L08 NOTIFY · wave 6 · 2026-08-09

```
LANE: L08 wave 6
shipped:
  #1409 product-law pins + price-alert English keys + refusal matrix tip honesty
       + claim lease hard-clamp under bus ack_wait
  (W5 banked and re-verified: multi-replica rates · reaper · kill · lease ·
   liquidation-only · event-wiring · dark mark refuse · fan-out kill)
in flight: none
parked:
  · live mark feed for alerts — production boots dark on purpose until trade
    public mark (or owner-configured source) is injected into AlertService
  · 429 delivery not-before / bus thrash backoff — pick-up if ops measures thrash
  · gateway credentials Class X — docs/OWNER-ACTIONS-NOTIFY-GATEWAYS.md
  · non-EN translations of notify.alert.price.crossed.* (English base shipped)
Nitro must decide: gateway credentials Class X if out-of-app must go live
SAFE TO CLOSE: yes
tip: 20309e22
```

## Engine A — units this wave

| Unit                                  | Done bar                                                                               | Proof                                  |
| ------------------------------------- | -------------------------------------------------------------------------------------- | -------------------------------------- |
| A0 open notify PR merge               | no open wall PRs at start                                                              | claim-check clear                      |
| VERIFY_TTL + gateway timeout defaults | schema 15m / 5s                                                                        | #1409 `env-defaults-pin`               |
| stuck-pending grace                   | 150s = maxDeliver×ack_wait                                                             | #1409 `stuck-grace-pin`                |
| consent footer                        | notification appends; verify never                                                     | #1409 `consent-footer-pin`             |
| README refusal matrix tip             | all catalog codes; no “in flight” lie                                                  | #1409 README + matrix pin              |
| refusal catalog load-bearing          | includes `delivery_stuck` + `transport_rejected`                                       | #1409 catalog pin extend               |
| price-alert i18n keys                 | human copy not key string                                                              | #1409 catalog + keys pin               |
| claim lease under ack_wait            | production formula ≤ ack_wait−5s                                                       | #1409 `claimLeaseMsFromGatewayTimeout` |
| W5 seals re-verify                    | multi-replica / reaper / kill / lease / rates / liquidation / event-wiring / dark mark | honesty suite RAN-IT on tip worktree   |

## Engine B — chapter pass (README → code)

Checked every load-bearing README chapter against tip `services/svc-notify/**` (and #1409 for matrix/footer/keys/lease):

| Chapter                                                                       | Verdict                                                                               |
| ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| The one thing (attempt ≠ accepted)                                            | **hold** — CHECK + dispatch; no `delivered` overclaim                                 |
| Channels (fail-closed unconfigured; required fatal)                           | **hold** — UnconfiguredChannel + boot env                                             |
| Gateway contract (idempotency-key, no redirect follow, 401 no retry, SMS cap) | **hold** — gateway-wire pins                                                          |
| Addresses (verify-through-channel; rate limits multi-replica)                 | **hold** — target rate windows #1292 class                                            |
| API + mute + deliveries                                                       | **hold** — mute SQL durable; critical ignores mute                                    |
| Events + reaper arms + claim lease                                            | **hold** — reap + lease under ack_wait; stuck = `delivery_stuck`; lease clamp shipped |
| Refusal codes matrix                                                          | **hold** — tip table matches catalog                                                  |
| position.updated liquidated only                                              | **hold** — policy pin                                                                 |
| Price alerts v22 MVP                                                          | **hold** — dark refuse; one-shot fire; keys catalogued                                |
| Kill-switches                                                                 | **hold** — fanout no-write; OOA disabled records                                      |
| Environment + §13 sockets                                                     | **hold** — Class X credentials owner-only                                             |
| Multi-replica SMS                                                             | **hold** — FOR UPDATE rate windows                                                    |

No new §8 invented. Digest stays unwired (pin: dispatch never imports digest).

## Engine C — attack surface (residual after seals)

| Surface             | State                                                    |
| ------------------- | -------------------------------------------------------- |
| Bus subjects        | Class B matrix = 10 consumers; growth pin                |
| Idempotent delivery | claim lease (clamped) + gateway idempotency-key          |
| PII                 | keys in inbox; verify code hashed; footer consent on OOA |
| Rate limits         | register 3 / verify 10 per 15m (shared across replicas)  |
| Fan-out kill        | writes nothing (not false-delivered)                     |
| Dark gateway        | typed refuse; staging/prod required channels fatal       |
| False accepted      | CHECK + adapter receipt only                             |
| 429 thrash          | retryable, no not-before — **parked**                    |

## Parked pick-up (next wave / owner)

1. Inject real `MarkSource` (trade public mark) — product pick of source.
2. Owner gateway URL/token (Class X).
3. Optional 429 not-before if gateway thrash appears.
4. Non-EN alert key translations (catalog lag is expected).

## Wall

`services/svc-notify/**` (+ `packages/i18n` for alert keys only this wave).  
No dual-write of sibling services. Class X credentials never invented.
