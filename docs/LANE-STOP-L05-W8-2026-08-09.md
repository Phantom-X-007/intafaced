# LANE STOP — L05 TOKEN · wave 8 · 2026-08-09

```
LANE: L05 TOKEN wave 8
shipped: #1512 a failed stake no longer loses the money (ambiguous ledger fail keeps pending claim; activate never invents active; comment honesty pot bind / revenueTotal)
in flight: none
parked: emission curve §8 · yield/buyback/governance flywheel sockets · tracker note refresh (features.mjs collides #1494 Denon + #1177 Shehzad) · empty-sweep engine residual is socket schedule · concurrent multi-window houseFees TOCTOU (orphan header, not invent payout)
Nitro must decide: emission curve live vs seed (§8) — or none this wave
SAFE TO CLOSE: yes
tip: 6eb88a74 (#1512 on main; paste-time 5ee00f24 superseded; tip wins)
```

## Packet (plain)

Wave 8 L05 owned **`services/svc-token/**` only**. W7 closed residual-empty on tip seals; this cook re-verified those seals, found **one Class M residual** (stake fail-path DELETE after ambiguous post → post-without-claim), and shipped it as #1512. Denon #1502 / #1494 and Shehzad #1177 do **not** touch the wall. Tracker note honesty stays parked (dual-edit fence). Only Nitro-only / socket product / L6–L7 parks remain after #1512 banks.

## Unit scorecard (W8 Engine A)

| Prio | Unit                           | Verdict on tip         | Evidence                                                                               |
| ---- | ------------------------------ | ---------------------- | -------------------------------------------------------------------------------------- |
| A0   | Open token PR merge            | **N/A → #1512**        | 0 open at cook start; this wave opened #1512                                           |
| A1   | empty-window engine            | **SEALED** (re-verify) | `yield_windows` header; tests empty freeze / late joiner / mismatch refuse             |
| A1   | houseFees invent (over-claim)  | **SEALED** (re-verify) | first-claim pot bind → `token.yield_source_underfunded`                                |
| A1   | stake/unstake crash residual   | **SHIPPED residual**   | W7 sealed M-02/M-04 happy path; W8 sealed fail-path DELETE + activate rowcount (#1512) |
| A2   | socket honesty (status)        | **SEALED**             | tracker status=socket for yield/buyback/governance; README auto-vs-person              |
| A2   | socket honesty (tracker notes) | **PARKED**             | notes still pre-#1353 prose; path collides #1494 + #1177 on `features.mjs`             |
| A2   | EMISSIONS kill                 | **SEALED** (re-verify) | tRPC + internal mint-next + auto-tick gated; pure tests green                          |
| A3   | emission curve numbers         | **PARKED §8**          | inventory drift test; Nitro-only                                                       |
| A3   | Engine B chapter pass          | **this file**          | table below                                                                            |

## Unit card — #1512 (Class M)

1. **Promise:** stake docstring / README — delete pending only when ledger refuses funding; no money-moved-no-row (`token-service.ts` L3-2).
2. **Reachable break:** `catch` deleted pending on **any** post error, including after MemoryLedger/HTTP already applied → orphan principal.
3. **Done bar:** ambiguous fail keeps `pending`; same `stakeId` recovers; vanished claim → `token.stake_claim_missing`, never fake `active`.
4. **Class M**
5. **Paths:** `services/svc-token/**` only (no packages dual-write)
6. **RED first:** flake-after-apply keeps pending; delete-after-post refuses fake active
7. **Collision:** claim-check clear vs open PRs; Denon files disjoint

### Class M body

- Money path: stake claim → ledger post → activate
- Ledger recipes: existing `recipes.stake` only
- No balances outside ledger; no money in `number`
- Adversarial: replay same stakeId · crash mid-write after apply · hostile claim delete · no dark dependency

## Sealed re-verified (do not re-ship)

| Item                                    | Evidence                               |
| --------------------------------------- | -------------------------------------- |
| Empty yield freeze (0004)               | #1353 + named tests                    |
| HouseFees over-claim refuse             | #1353                                  |
| Buyback claim-before-burn (0002)        | #767 + new-runId refuse                |
| Mint claim-before-post + ceiling        | #1353 / #1083                          |
| EMISSIONS kill all mint paths           | #1291 + mount                          |
| Stake claim-before-post + M-02/M-04     | `7b98cd1f` + #1512 fail-path           |
| Socket status honesty                   | tracker + README                       |
| Comment honesty pot bind / revenueTotal | #1512 (was stale; code already sealed) |

## Engine B — promise falsification (W8)

| Claim                                                     | Status                                        |
| --------------------------------------------------------- | --------------------------------------------- |
| Auto vs person table honest                               | **PROVED**                                    |
| Stake / unstake claim ordering + crash resume + fail-path | **PROVED** (#1512 closes fail-path)           |
| Emissions kill blocks mint                                | **PROVED** (144 pure incl internal-emissions) |
| Yield empty window cannot invent later stakers            | **PROVED**                                    |
| Yield operator cannot over-invent pot                     | **PROVED** (under-claim allowed)              |
| Buyback claim-before-burn                                 | **PROVED**                                    |
| Buyback is real market-buy                                | **FALSIFIED** → socket (correct)              |
| Governance outcome executor                               | **FALSIFIED** → socket (correct)              |
| Emission curve numbers agent-owned                        | **OPEN only as §8 Nitro**                     |

## Engine C — attack surface

| Attack                                      | Status           |
| ------------------------------------------- | ---------------- |
| Double mint same epoch                      | sealed           |
| Empty-staker overpay via re-run same window | sealed           |
| Double buyback same window different runId  | sealed           |
| HouseFees over-claim invent                 | sealed           |
| Ambiguous stake post → claim drop           | **sealed #1512** |
| §8 invent curve / flywheel                  | fenced           |

## Parked with pick-up

1. **Emission curve §8** — owner numbers (seed vs live inventory). Agent must not invent.
2. **Flywheel sockets** — `token.yield` aggregation job + caller; `token.buyback` real trade buy; `token.governance` quorum/threshold/executor. Denon invent-risk / product law.
3. **Empty-sweep engine residual** — empty distribute still moves fees into rewards engine; later stakers need **new** window id **and** new houseFees. Paying stranded engine residual without typed amount = socket schedule (W5/W7 park holds).
4. **Tracker note refresh** — rewrite `token.yield` / `token.buyback` notes to match #1353/#767/#1422 after `features.mjs` free of open PR writers (#1494 Denon, #1177). Status must stay `socket`.
5. **Concurrent multi-window houseFees TOCTOU** — two first-claims on different windowIds can both pass pot read then one sweep fails (orphan header, not invent payout). Optional Class M; not ship-blocking honesty residual this wave.

## Local re-verify this cook

| Check                          | Result                                                                                       |
| ------------------------------ | -------------------------------------------------------------------------------------------- |
| Wall open PR intersect (start) | clear                                                                                        |
| Pure suite                     | **144/144** (economics 106 · internal emissions/stake · router mount)                        |
| Typecheck svc-token            | clean                                                                                        |
| Postgres money suite           | **not re-run** — no docker/postgres on host; #1512 adds RED under existing suite for CI seal |
| features.mjs dual-edit         | **refused** — #1494 + #1177                                                                  |
| Denon dual-edit                | none (paths disjoint)                                                                        |

## SAFE TO CLOSE

**yes** — #1512 merged `6eb88a74`. Residual craft L1–L4 empty; only parks above remain.
