# BUILD LANE L13 — TOKEN residual · wave 6 · long cook · go all out

You are Claude Code on INTAFACED (`~/projects/Sovereign` → `Phantom-X-007/intafaced`).
Nitro is AFK. **Needle residual unconstrained** — thrash, pad-to-quota, dual-write burns banned.
**13 of 16** path-walled lanes are live. You own **only** your wall.

## 0 · Auth + tip ritual (every cycle)

```
export PATH="/Users/Nitro/projects/Sovereign/.tools/bin:/opt/homebrew/bin:$PATH"
export GH_TOKEN="$(tr -d '\n\r ' < /Users/Nitro/.grok/agent-auth/github_token)"
cd /Users/Nitro/projects/Sovereign
git fetch origin
git log -1 --oneline origin/main
gh pr list --state open --limit 60
node tooling/ci/claim-check.mjs <paths>   # unsandboxed
```

- Never print GH_TOKEN. Unsandbox gh / claim-check.
- **Never edit main checkout.** `pnpm wt feat/w6-<lane>-<slice>` from origin/main.
- Thrift deleted on tip — ignore stale HARD thrift.
- Paste-time tip: `8da2c561` — **tip wins**. Stop notes are hypotheses.
- Open PRs (re-derive): ~60 open — babysit first: stop PRs #1384 PAY #1395 TRADE #1396 INDEXER #1352 WS #1361 LEDGER #1373 P2P · market #1189/#1387 commerce · identity cluster · shell/ws/notify tests · #1177 Shehzad · dependabot holds — re-derive
- Fan-out: judgment strong; bulk harvest parallel; no escalate-on-uncertainty thrash. Class M always adversarial.
- **Wave 6:** Wave 5 largely banked with parked residuals + many open stop/product PRs. **Babysit your wall's open PRs first** (including docs stop PRs). Fix monorepo red only if **your** suite broke tip — do not dual-write sibling services to clear their red. Do not invent §8.

## Hard fences (wave 6+ law — do not violate)

- **Denon hard-parallel LIVE:** do **not** ship product-complete invent-risk engines (futures/OTC/copy product-complete, PSP/PayFac product-complete, etc.). **Residual honesty / babysit / fail-closed patches only.** Never dual-edit Denon open PR file sets — re-derive `gh pr list --author Phantom-X-007`.
- **Shehzad:** protocol/INTACHAIN implement babysit only (#1177 class).
- **Anti-pad:** never invent Engine A rows to hit 8. Rank by needle. SAFE TO CLOSE when residual is empty or only Nitro-only/L6–L7 parks — not when "8 rows shipped."
- **Token:** unconstrained for **needle** residual; thrash / pad-to-close / dual-write burns are banned.

## Unit card (mandatory before code)

1. Promise quoted (file:line / ADR / SPEC / W5 stop)
2. Reachable break
3. **Done bar**
4. Class N / P / M / X
5. Paths (packages one-writer if touched)
6. RED test first
7. Collision list vs open PR files

### Class M body (when money)

Self-audit: money path? ledger recipes? no balances outside ledger? no money in number?  
Adversarial: replay · crash mid-write · hostile caller · dark dependency — PR body before merge.

## Parallel recipe

1. **8–12 read-only subagents** first half hour
2. Rank: **merge open PRs on wall** → W5 parked → new residual → never greenfield without Done bar
3. **3–5 worktrees hot**
4. Next unit while CI runs
5. Compact → rebuild from git/gh only
6. Cross-service: **contracts/events PR first**
7. Tracker: mountain-event only
8. `pnpm verify` before push; CI seal
9. PR titles: plain words for Nitro

## Loop

```
while true:
  fetch tip · open PRs · path-intersect · claim-check
  merge your greens
  pick highest CLEAR unit with unit card
  RED → implement → verify → PR → adversarial if M → merge
  start next stream while CI runs
  after compact: rebuild from git/gh only
```

## Banned stops

todo empty · free=0 · thrift · "want me to continue?" · wait for Nitro except Class X / blank §8 / grant authority · idle without real collision · "fixing" sibling suites outside your wall · pad Engine A to hit 8 · Denon invent-risk product-complete · HUMAN frontend craft

## Real stop

Residual-empty honesty (not row quota): L1–L4 units shipped or parked with pick-up · Engine B chapter pass · only Nitro-only/L6–L7 left · stop note docs PR · nothing uncommitted →
`docs/LANE-STOP-<ID>-W6-2026-08-09.md` +

```
LANE: <ID> wave 6
shipped: #… plain words
in flight: …
parked: … + why
Nitro must decide: … or none
SAFE TO CLOSE: yes/no
tip: <sha>
```

## Exclusive wall (YOURS)

`services/svc-token/**`

## Fenced

market stake writes · protocol minter

**Siblings W6:** L01 AGENTS · L02 ACADEMY · L03 P2P · L04 PAY · L05 TRADE · L06 BANK · L07 MARKET · L08 NOTIFY · L09 INDEXER · L10 WS · L11 SHELL · L12 IDENTITY · L13 TOKEN · L14 LEDGER · L15 BOARD+TOOLING · L16 VENDOR-SUPPLY.

## Sealed — do not re-ship (re-verify)

Never fail-open stake rescale. Stake claim-before-post (pending → ledger → active) must hold. W5 parks: **do not invent** live flywheel for yield/buyback/governance — they are §13 sockets until Nitro/Denon law. Emission **curve numbers** = §8 park.

## Read first

1. `services/svc-token/README.md` (auto vs person table + known buyback ordering gap)
2. open token / ledger-client token recipe PRs (re-derive — often 0 product)
3. `docs/audit/2026-07-31-denon-wave-deep/03-TOKEN-BANK-EARN.md` (T-01…T-10 holes still craft targets where landable)
4. `token-service.ts` stake/unstake ordering + economics tests

## Engine A — Ranked residual (≥8 rows, Done bar each)

| Prio | Unit                                | Done bar (start — tighten from law)                                                                                                          |
| ---- | ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| A0   | Open token-wall PR merge            | green on tip; no dual-write ledger-client without L14 coordination                                                                           |
| A1   | empty-staker yield residual         | `distributeRevenue` with **zero** active stakes: no overpay, no silent invent; dust conservation holds                                       |
| A1   | mint crash residual                 | kill mid mintEpoch / mint-next: no double supply; epoch row + ledger agree; closed epoch refuse                                              |
| A1   | stake claim-before-post residual    | pending → fund → active; ledger refuse deletes pending; retry same stakeId safe (L3-2 / M-01/M-02 class)                                     |
| A1   | unstake crash residual              | concurrent unstake never double-pays principal; unstaking→closed claim holds (M-04 class)                                                    |
| A2   | stake↔ledger reconcile residual     | sum `token.stakes` active == ledger stake accounts; RED test if drift                                                                        |
| A2   | yield window resume residual        | crash mid multi-recipient distribute: re-run pays **only** missed users; window key conserved                                                |
| A2   | buyback claim-before-burn residual  | **README known gap:** claim `revenueWindow` row **before** burn post — second runId same window cannot burn twice (Class M)                  |
| A2   | yield amount honesty residual       | operator `sources[].amount` must not invent houseFees; prefer read ledger houseFees or explicit refuse + socket doc (T-03 class)             |
| A2   | token_params live drive residual    | buyback/emission params from DB not frozen defaults on mint/split path where code still injects defaults (T-02 class — if still true on tip) |
| A2   | EMISSIONS kill residual             | `EMISSIONS_ENABLED=false` blocks tRPC + `/internal/emissions/mint-next` + auto-tick; zero mint lands                                         |
| A2   | internal stake hot-path residual    | `GET /internal/stake/:userId` auth + numbers match `stakeOf`; no free userId forge                                                           |
| A3   | fee discount governance residual    | edit `token_params` changes access fee discount without redeploy; unreadable row refuses discount                                            |
| A3   | governance ballot residual          | one-vote-per-user; weight = stake snapshot in txn; **no** status machine invention — park executor/quorum §8                                 |
| A3   | buyback split conservation residual | burn+rewards legs = tokensBought floors; DB `buyback_runs_split_conserved_ck` still holds under edge wei                                     |
| A3   | socket honesty residual             | tracker/README still mark `token.yield` / `token.buyback` / `token.governance` as sockets if still operator-only — never re-label done       |
| A3   | Engine B chapter pass               | stop note lists every README chapter falsified + park picks                                                                                  |

## Engine B — Promise falsification (UNBOUNDED — multi-hour)

- README **auto vs person** table — no user-facing copy that claims scheduled flywheel
- Staking ladder · lock tiers · access tiers · fee discount
- Emissions schedule · mint ceiling CK · kill-switch · auto-tick default off
- Yield: operator-only, per-recipient keys, empty window, empty staker set
- Buyback: operator-asserted purchase, claim-before-burn gap, burn account convention
- Governance: ballots only; draft terminal; no silent executor
- Ledger recipes: stake / unstake / sweepFeesToRewards / rewardPay / burn / mintEmission
- Events: stake.created · buyback.completed — no orphan consumers invented
- IFC is ledger asset not chain coin — refuse inventing deposit/withdraw/self-custody

## Engine C — Matrix / attack surface

double stake same id · double unstake · locked early exit · negative stake · mint past schedule · mint when kill off · double distribute same window · double buyback same window different runId · ballot stuffing · free userId on internal stake · MFA skip on treasury · houseFees invent · rewards engine drain order (yield vs buyback) · dual truth stakes table vs ledger · dark trade buy for buyback

## Nitro-only (never invent)

emission curve numbers §8 · live structural flywheel law (yield job / trade buyback / governance quorum·threshold·executor) · grant-as-recipe DIRECTION carve-outs

GO — tip ritual · babysit open PRs · harvest · unit cards · multi-stream · no second briefing.
