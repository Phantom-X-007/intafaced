# Status for Nitro — 27 Jul 2026 (floor after all service PRs)

**All of Denon’s service PRs are on `main`. Zero open PRs. Nothing is a live product yet — routers are not mounted.**

This doc is the **current floor** for every new chat. Re-check live: `gh pr list` (should be empty of Denon service work) and `git log origin/main -1`.

**Main tip (when this was written):** `7f8beec` — includes #31–#39 and handover #40.

---

## Where we are

- **0 open PRs** — merged and on main: #31 trade, #32 audit fixes, #33 p2p, #34 agents, #35 bank, #36 blueprint, #37 protocol, #38 brand-scan, #39 pay, #40 STATUS handover (this doc replaces that stale handover).
- **On main:** ~12 packages, 11 services (identity, ledger, token, matching, trade, pay, p2p, bank, blueprint, agents, protocol) + apps web/admin + packages (contracts, events, ledger-client, i18n, …).
- **Not live:** no service routers are mounted into a running API. Logic + tests exist; there is no public product surface.
- **Do not trust memory** that says “9 open PRs” or “matching/trade only in PRs” — that was true earlier on 27 Jul; it is false now.

---

## Audit items — status table

| #         | Finding                                               | Status                                                                                                                                                                          |
| --------- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **P0-4**  | Multi-stake yield underpay                            | ✅ **Shipped** (#32)                                                                                                                                                            |
| **P0-5**  | `rank.awardXp` was public                             | ✅ **Shipped** (#32) — now scoped write                                                                                                                                         |
| **P1-6**  | DoD money-test false green                            | ✅ **Shipped** (#32) — gate tightened; found real debt                                                                                                                          |
| **P1-7**  | `multiplier_bps` stored, ignored                      | ✅ **Shipped** (#32) — yield weights from stored value                                                                                                                          |
| **P1-12** | `hashTx` separator differs                            | ❌ **Disproved** — both ledgers use `.update(' ')`; no fix needed                                                                                                               |
| **P0-2**  | `order.filled` → `tradeFill` shape gap                | ✅ **Practice shipped in #31**; **ADR accepted this floor** — [`docs/adr/2026-07-27-trade-order-store-source-of-truth.md`](adr/2026-07-27-trade-order-store-source-of-truth.md) |
| **P0-1**  | Routers never mounted                                 | ⏸ **Open** — nothing is a live API                                                                                                                                              |
| **P0-3**  | Commingled hold (`userHold` mixes order + withdrawal) | ⏸ **Deferred on purpose** — needs Nitro decision — [`docs/decisions/P0-3-purpose-keyed-holds.md`](decisions/P0-3-purpose-keyed-holds.md)                                        |

### Short notes (only what still matters)

**P0-1** — Gate to anything running. Everything else is logic and tests with no live API plane.

**P0-3** — Real bug, not live yet (because of P0-1). Fix changes the ledger account model (`purpose` on `AccountRef`). Denon’s proposal is written on the decision card; recommendation is approve and build **after** mount design is known, **before** any deploy.

**P0-2** — svc-trade already treats its order store as source of truth and derives `fillId` from `(marketId, engineSequence)`. The ADR makes that contract explicit so nobody later treats the fill event as self-describing for money posts.

**P1-12** — Not a bug. Leave it.

---

## Suggested next sequence

1. **P0-1 — mount the routers** (design + implement). First thing that makes a product exist.
2. **P0-3 — purpose-keyed holds** (after Nitro decides; before any deployment).
3. **Phase 2 product slices** still claimable when ready: e.g. `trade.convert`, `web.terminal`, `venue.aggregation`; also low-coordination items like `identity.webauthn`, `token.governance` (`pnpm tracker ready`).

Do **not** rebuild matching, trade, pay, p2p, bank, blueprint, agents, or protocol from scratch — they are on main.

---

## Operational hazards

| Hazard                                | What it means                                                                                                                                                    | What to do                                                                                                                                                                                                                               |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Shared Postgres `ledger.*` schema** | svc-ledger tests `TRUNCATE ledger.*` on a shared schema. Two worktrees running those tests at once can fail each other with “insufficient funds” that look real. | Assume collision risk until isolation lands. Fix is **[PR #42](https://github.com/Phantom-X-007/intafaced/pull/42)** (unique schemas via `createTestDb`). Do not treat flaky ledger money failures as product bugs until #42 is on main. |
| **Vercel MCP**                        | If used, needs human auth in the browser.                                                                                                                        | Agents cannot complete Vercel login for you.                                                                                                                                                                                             |

---

## What the next agent / chat must NOT redo

- Do **not** re-open or re-implement #31–#39 work (services already on main).
- Do **not** re-investigate P1-12 as if it were open.
- Do **not** “fix” P0-3 without Nitro’s decision on the card (or a clear override from him).
- Do **not** invent deploy/API status — routers unmounted unless a later PR proves otherwise.
- Do **not** expand graph-engineering research in this floor set; research lives in the other chat. Start orientation from [`docs/GRAPH-RUN-FLOOR.md`](GRAPH-RUN-FLOOR.md).
- Do **not** claim “9 open PRs” or “matching/trade only in open PRs.”

---

## Links this floor owns

| Doc                                                                                                               | Role                                  |
| ----------------------------------------------------------------------------------------------------------------- | ------------------------------------- |
| [`docs/adr/2026-07-27-trade-order-store-source-of-truth.md`](adr/2026-07-27-trade-order-store-source-of-truth.md) | P0-2 accepted ADR                     |
| [`docs/decisions/P0-3-purpose-keyed-holds.md`](decisions/P0-3-purpose-keyed-holds.md)                             | Nitro decision card                   |
| [`docs/GRAPH-RUN-FLOOR.md`](GRAPH-RUN-FLOOR.md)                                                                   | Start line for graph-engineering chat |
| [`docs/START-HERE.md`](START-HERE.md)                                                                             | Plain map of the product              |

Law remains: `INTAFACED_DEFINITIVE_BUILD.md` · agent rules: `AGENTS.md` / `tooling/agent-protocol/AGENT_PROTOCOL.md`.
