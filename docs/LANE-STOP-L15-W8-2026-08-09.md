# LANE STOP — L15 EDGE · wave 8 topup · 2026-08-09

```
LANE: L15 EDGE wave 8 topup
shipped: #1511 — console cannot invent a flag-only gateway kill; /ready names rate-limit posture (multiReplicaShared:false); CORS DELETE honesty fixed; edge.gateway pin tests
in flight: none
parked: multi-replica shared kill store (§13 invent fence) · refresh-token httpOnly cookie CORS/WS re-take (§13) · geo-IP region · edge.gateway flag enforcement (still NOT_ENFORCED — do not pretend it gates until deliberate PR) · request-size env beyond Fastify default · WS route-behind-edge product change (L12 / routing law, not edge micro)
Nitro must decide: none (Class X host/secrets prod only if/when deploy)
SAFE TO CLOSE: yes
tip: 46744962
```

## Re-verify (sealed + this wave)

| Property                                                | Result   | Proof                                                                                      |
| ------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------ |
| Kill-switch fail-closed + release asymmetry             | **hold** | `kill-switch.test.ts` 42 green on tip                                                      |
| Console cannot green-halt `ws`                          | **hold** | #1480 + e2e status `outsideTheDoor.ws`                                                     |
| Multi-replica kill not invented                         | **hold** | `killState.multiReplicaShared: false` always                                               |
| CORS no `*`, no credentials, preflight not route oracle | **hold** | `cors.test.ts` 58; browser suite skips without Chromium                                    |
| `edge.gateway` does not gate the proxy                  | **hold** | #1511 status `flagEdgeGateway.enforced:false` + pin (no `isEnabled` in production sources) |
| Rate-limit posture honest                               | **hold** | `/ready.rateLimit` + `multiReplicaShared:false`; boot WARN when trustProxy unset           |
| CORS methods ≠ proxy methods                            | **hold** | #1511 pin: DELETE not in ALLOWED_METHODS; ALWAYS_ALLOWED_REST still has DELETE             |

## Engine A residual disposition (wave 8)

| Unit                                 | Disposition                                                                      |
| ------------------------------------ | -------------------------------------------------------------------------------- |
| A0 open edge PR merge                | none at start; #1511 shipped this wave                                           |
| A1 kill-switch residual              | **empty** for edge craft — operator halt real; unarmable named                   |
| A1 CORS residual                     | **honesty shipped** — DELETE comment lie fixed; preflight still GET/POST/OPTIONS |
| A1 admin status outside-the-door     | **holds** under #1511 (ws/ledger/matching still named)                           |
| A2 process-local kill durability     | **holds** — file/memory honesty; shared store still park                         |
| A2 WS-behind-edge socket honesty     | **holds** — named residual not fake closed                                       |
| A2 edge.gateway NOT_ENFORCED honesty | **shipped** #1511 — status + pin tests                                           |
| A2 control-plane residual            | **empty** — suite green + new status fields                                      |
| A3 rate-limit honesty residual       | **shipped** #1511 — `/ready.rateLimit`                                           |
| A3 Engine B chapter pass             | **done** this note                                                               |
| A3 path-intersect L12 WS             | **clear** — no dual-write; Denon #1502/#1494 no edge paths                       |
| A3 L6 multi-replica / cookie         | **named park** §13                                                               |

## Engine B (promise falsify — unbounded)

| Chapter              | Verdict                                                                                                                                  |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Why it exists        | **holds** — sole producer of principal signature for `scopedProcedure`                                                                   |
| API contract         | **holds** — route table matches UPSTREAMS (README pin test); `/metrics` + `/admin/*` documented                                          |
| Security properties  | **holds** — strip reserved first; region server-side; bearer never forwarded; bad token → anonymous; no DB/bus/`INTERNAL_SERVICE_SECRET` |
| Browser origins CORS | **holds** — no `*`; no credentials; OPTIONS not proxied; `/admin` not CORS surface                                                       |
| Kill-switch          | **holds** — operator surface live; `edge.gateway` NOT_ENFORCED and now status-named; release asymmetry + path-resolve + batch            |
| Configuration        | **holds** — rate limit + kill path + trust proxy documented; `/ready` reports rate-limit posture                                         |
| Not built yet        | **holds** — only true residuals named; no pad rows                                                                                       |

## Engine C (attack surface)

| Vector                    | State                                                                         |
| ------------------------- | ----------------------------------------------------------------------------- |
| kill fail-open            | refused (undecidable → 503)                                                   |
| CORS free origin          | refused (no `*`; enforced envs closed door)                                   |
| fake WS close             | refused to arm; status names gap                                              |
| admin green while dead    | ledgerConfigured boolean; freeze pass-through; flagEdgeGateway.enforced false |
| secret leak               | edge still holds no service secret / DB / bus                                 |
| invent multi-replica kill | refused — multiReplicaShared always false on status + ready                   |

## Anti-pad

No Engine A row invented to hit a count. SAFE TO CLOSE = residual-empty honesty for **edge craft L1–L4**, not “zero §13 forever.”

## Path-intersect this wave

- Denon open: #1502 gate, #1494 support — **no** `services/svc-edge/**` files
- Siblings: L12 WS owns socket product residual; edge only names outside-the-door honesty
- claim-check: clear for `services/svc-edge` before #1511
