# Engine B — svc-p2p promise falsification · wave 4 · 2026-08-09

Tip at cook start: `f1167abc` → later `6a9de1db`. Wall: `services/svc-p2p/**`.

Method: read README + ADR D-S-08 + audit 2026-08-08 + code/tests on tip; try to falsify each load-bearing promise.

## Chapter results

| Promise                                           | Verdict                      | Notes                                                                                |
| ------------------------------------------------- | ---------------------------- | ------------------------------------------------------------------------------------ |
| Decide-then-post                                  | **HOLDS**                    | resolution committed before ledger post; crash → sweep. Sealed. Do not re-ship.      |
| Re-drive escrow (created)                         | **HOLDS**                    | re-call `escrowLock`; refund unreachable without re-drive. Named test in #1298.      |
| fiat_sent never auto-release                      | **HOLDS**                    | timeout → open_dispute. Tests pin.                                                   |
| disputed never machine-rules                      | **HOLDS**                    | escalate_and_hold; natural-person moderator invariant (0003).                        |
| Instruments disclose only under held escrow + log | **HOLDS** (mechanism)        | #428; residual = empty registry content + Class X KMS.                               |
| Merchants badges → real limits                    | **PARTIAL**                  | Stage 1–2 mechanism on main; env unset = unlimited. Stage 3 cut to identity.apikeys. |
| created-refund-impossible                         | **HOLDS**                    | holdsEscrow + tests.                                                                 |
| sweepSettlements re-drive                         | **HOLDS**                    | #1069 announce-before-stamp; crash sim tests.                                        |
| escrowIntegrity ledger agrees                     | **WAS BROKEN → FIXED #1283** | seller aggregation hid equal-and-opposite drift; now per-trade.                      |
| XP no double-award                                | **HOLDS**                    | business keys + settle gate; identity ON CONFLICT when consumer wired.               |
| State machine total / acyclic                     | **HOLDS**                    | pure tests + #1298 created pin.                                                      |
| README pooled escrow                              | **WAS DRIFT → FIXED #1283**  | pots are per-trade.                                                                  |

## Parked (not agent invent)

| Item                                           | Why                                |
| ---------------------------------------------- | ---------------------------------- |
| `p2p:moderate` scope mint                      | DIRECTION §3 / Nitro               |
| Method registry seed content                   | Jurisdictional product law         |
| Instrument encryption at rest                  | Class X KMS                        |
| apps/admin dispute console                     | Outside svc-p2p wall (shell/admin) |
| Transactional outbox (audit P1)                | packages/events design             |
| Timeout-opened dispute `opened_by` attribution | Schema vs natural-person invariant |
| Numeric offer ceilings                         | Owner env policy                   |

## Attack surface (Engine C) residual

Instrument theft residual is **residual risk of small takes for harvesting** — inherent P2P; access log + min amount mitigate. Collusion wash-trading reputation not addressed (two accounts). Moderator is allowlist or admin:compliance — power is intentional after human reachability stage.
