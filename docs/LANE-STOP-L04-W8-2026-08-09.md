# LANE STOP — L04 NOTIFY · wave 8 · 2026-08-09

```
LANE: L04 wave 8
shipped:
  · A0 open notify PR merge — none on wall at start (claim-check clear)
  · #1504 price-watch sweep re-verified HOLD (sweep mount, dark mark, canFire
    disclosure, /ready lastAt, activeMarkets, one-shot key)
  · W6/W7 seals re-verified HOLD — multi-replica rates · reaper/stuck · fan-out
    kill (bus) · claim lease under ack_wait · credentials refuse · refusal
    matrix · event-wiring Class B
  · #1521 fire-before-notify + gateway-timeout lease cover + rate-window wording
       plain: price watch no longer dies under kill; gateway budget cannot beat
       the lease (no multi-replica double free SMS at high timeout)
  · Engine B README chapter pass — all HOLD after #1521
in flight: none
parked:
  · live mark feed for alerts — prod boots dark on purpose until trade public
    mark (or owner-configured source) is injected into AlertService
  · 429 delivery not-before / bus thrash backoff — retryable honesty holds;
    pick-up if ops measures thrash (no invent without thrash)
  · gateway credentials Class X — docs/OWNER-ACTIONS-NOTIFY-GATEWAYS.md
  · non-EN translations of notify.alert.price.crossed.* — EN base shipped;
    only EN catalog exists OS-wide (packages/i18n)
  · crash after gateway 2xx before settle — gateway must honor
    idempotency-key; optional settle-if-still-owner is a future residual if
    a real gateway ignores the key
  · alert sweep /ready lastAt only stamps success — continuous fail leaves a
    stale healthy lastAt; ops polish, not user-facing lie; pick-up if needed
Nitro must decide: gateway credentials Class X if out-of-app must go live
SAFE TO CLOSE: yes
tip: c2af7881
```

## Engine A — units this wave

| Unit                            | Done bar                                   | Status           | Proof                                                                            |
| ------------------------------- | ------------------------------------------ | ---------------- | -------------------------------------------------------------------------------- |
| A0 open notify PR merge         | green or none                              | **none open**    | claim-check clear on `services/svc-notify/**` at start                           |
| A1 price watch / alert residual | #1504 holds; no silent skip                | **SEALED**       | sweep-mounted-pin · dark refuse · evaluation disclosure · activeMarkets          |
| A1 multi-replica SMS            | no N× free verification codes under 2 pods | **SEALED**       | PostgresTargetRateLimiter FOR UPDATE + concurrent last-slot                      |
| A1 reaper stuck                 | no fake delivered forever                  | **SEALED**       | reapExhausted arm1/arm2 · stuck-grace pin · reaper interval pin                  |
| A2 fan-out kill                 | kill honest                                | **SEALED+fixed** | bus path fanout-off-pin; **alert path** fixed in #1521 (no markFired under kill) |
| A2 claim lease                  | no double free SMS                         | **SEALED+fixed** | lease clamp + **#1521 env max = lease ceiling** so lease ≥ timeout always        |
| A2 refusal matrix               | complete                                   | **SEALED**       | readme-refusal-matrix-pin · refusal-write-catalog-pin                            |
| A3 gateway credentials live     | park Class X                               | **PARK Class X** | owner action doc only                                                            |

**Anti-pad:** one craft PR (#1521) for two reachable honesty breaks. No quota fill. No invent §8 rates. No dual-edit Denon (#1502 / #1494).

## Engine B — chapter pass (README → code on tip after #1521)

| Chapter                            | Verdict                                                                                                   |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------- |
| The one thing (attempt ≠ accepted) | **hold** — CHECK + settle discipline; no `delivered` overclaim                                            |
| Channels                           | **hold** — four adapters; unconfigured refuse; required fatal in staging/prod                             |
| Addresses                          | **hold** — own targets; verify-through-channel; shared fixed-window rates (wording fixed)                 |
| API                                | **hold** — full tRPC surface; self-only; deliveries user-facing                                           |
| Events                             | **hold** — 10 consumers match matrix; nak only retryable; critical records no-target                      |
| Reaper                             | **hold** — arm1 attempts_exhausted · arm2 delivery_stuck                                                  |
| Price alerts                       | **hold** — sweep mounted; dark refuse; fire-before-notify after #1521                                     |
| Kill-switches                      | **hold** — fanout no-write; OOA off writes channel.disabled; alert path no longer burns under fanout kill |
| Environment                        | **hold** — gateway timeout max 25s = lease ceiling                                                        |
| §13 sockets                        | **hold** — adapters real; waiting on owner credentials only (Class X)                                     |

No new §8 invented. Digest stays unwired.

## Engine C — attack surface

| Surface             | State                                                                                           |
| ------------------- | ----------------------------------------------------------------------------------------------- |
| Replica free SMS    | **mitigated** — FOR UPDATE rates + lease always covers attempt (#1521)                          |
| Fake delivered      | **mitigated** — accepted only on transport receipt + DB CHECK                                   |
| Kill lie            | **mitigated** — bus no-write; OOA named refuse; **alert fire no longer marks fired under kill** |
| Orphan events       | **mitigated** — Class B growth pin + pending consumer report                                    |
| Dark gateway / mark | **mitigated** — typed refuse; zero invent                                                       |
| 429 thrash          | **parked** — retryable + attempt budget; no not-before                                          |

## RAN-IT this wave

Worktree `feat/w8-l04-notify-residual` then tip after merge:

```
pnpm exec turbo run test --filter=@intafaced/svc-notify
→ Test Files  38 passed | 4 skipped
→ Tests       297 passed | 46 skipped  (PG suites skip without TEST DB)

pnpm gates → 34 doctrine gates green
CI on #1521 → all required checks green → squash-merged 0acbdbeb
```

## Parked pick-up (unchanged + named)

1. Inject real `MarkSource` (trade public mark) — product pick of source.
2. Owner gateway URL/token (Class X) — [`docs/OWNER-ACTIONS-NOTIFY-GATEWAYS.md`](./OWNER-ACTIONS-NOTIFY-GATEWAYS.md).
3. Optional 429 not-before if gateway thrash appears in ops.
4. Non-EN alert key translations when non-EN catalogs land in `packages/i18n`.
5. Optional: stamp alert-sweep `/ready.lastAt` on error ticks (ops polish).
6. Optional: settle-if-still-owner if a real gateway ignores idempotency-key.

## Wall

`services/svc-notify/**` only. No dual-write. Class X credentials never invented.
No packages/** craft this wave.
