# ─────────────────────────────────────────────────────────────────────────────
# THE COVERAGE MATRIX, MACHINE-READABLE.
#
# INTAFACED_DEFINITIVE_BUILD.md §25 line 740 names this file by path and says CI
# carries a `coverage-check` against it. Until 2026-08-03 neither existed, and
# the cost of that absence is written down in
# docs/audit/BUILD-COVERAGE-AUDIT-2026-08-03.md: forty law-specified
# capabilities with no tracker row, twenty of them the entire v2.1 and v2.2
# chapters — 100% of the law written after v2.0, invisible to the board, with
# nothing anywhere saying so.
#
# This file is the map from law to board. `tooling/ci/coverage-check.mjs` reads
# it and fails when either direction breaks: a law section with no row and no
# written exemption, or a row claiming a law that does not say it.
#
# HOW TO ADD SOMETHING
#   · new law chapter  → add it to `sections:` with a disposition. There is no
#     way to add a chapter and have nobody notice; that is the point.
#   · new capability   → add it to `capabilities:` with a law cite that resolves
#     and exactly ONE of `tracker:` / `exempt:` / `gap:`.
#   · new tracker row  → either a capability claims it, or it goes in
#     `tracker_extra:` with a basis that exists on disk.
#
# THE THREE STATES, and why there is no fourth
#   tracker:  the law line is on the board. The row carries owner, phase, status.
#   exempt:   somebody decided this is not in scope, and wrote down why and where.
#             "§27–§37 are not in scope for phase 2" is a legitimate answer.
#   gap:      it is missing, we know it is missing, and it is counted.
#   Silence is not a state. That is the whole disagreement with the old world.
# ─────────────────────────────────────────────────────────────────────────────

meta:
  law: INTAFACED_DEFINITIVE_BUILD.md
  law_version: '2.2 FINAL'
  tracker: tooling/tracker/features.mjs
  built_from: docs/audit/BUILD-COVERAGE-AUDIT-2026-08-03.md
  spec: '25:740'
  deviations:
    - >-
      §25:740 says "every named feature in Vol. I chapters I–XIX". Vol. I is a PDF at the repo root and
      is not machine-readable from here, so the enumerated axis is §25's own matrix — which the law says
      already contains every Vol. I feature exactly once — plus the §30 and §38 addenda that extend it.
      Checking against §25 rather than Vol. I means a Vol. I feature that never reached §25 is invisible
      to this gate. That is a limit of the source, not of the design, and it is stated rather than hidden.
    - >-
      §25:740 says each feature appears "with an owner and phase". Owner and phase are NOT duplicated
      into this file. They live on the tracker row, which is where ownership is actually claimed and
      handed over; copying them here would create a second place to be wrong. The gate resolves owner
      and phase through `tracker:` instead.
    - >-
      Outside §25, capabilities are enumerated at the granularity the audit established — one entry per
      identified obligation — not one per line of law. Every CHAPTER is accounted for; not every
      sentence is. A sentence-level map of 862 lines of prose would be a second law, and would rot.
    - >-
      "Any feature without a green DoD at its phase gate blocks the drop phase that promised it" is
      implemented against `drop_phases:` below and fires only when a phase is marked `promised: true`.
      Firing on unpromised phases would fail every run from day one and the gate would be switched off
      inside a day — see the ratchet note on `baseline:`.

# ── THE LAW'S CHAPTERS ───────────────────────────────────────────────────────
# Every `## N ·` heading in the law must appear here. This is the check that
# would have caught §27–§37: eleven chapters were written, none of them was
# listed anywhere, and no gate noticed.
sections:
  - id: 0
    title: DOCTRINE
    disposition: exempt
    reason: >-
      Doctrine is the constraint every capability is built under, not a capability. §0.6 (ledger is law)
      is enforced by custody-scan and vendor-java-money-scan; §0.7 (branding) by brand-scan. A tracker
      row for "doctrine" would be a row nobody could ever finish.
  - id: 1
    title: STACK
    disposition: exempt
    reason: >-
      Technology choices — language, database, bus, framework. Enforced by the repo existing and by
      workspace-sync, not by a board row. `infra.monorepo` already carries the scaffold.
  - id: 2
    title: MONOREPO LAYOUT
    disposition: exempt
    reason: >-
      Directory layout and the cross-service import rule. Enforced by workspace-sync and custody-scan.
      NOTE for the owner's pen — the audit (§B5 row 6) found svc-edge, svc-ws, svc-notify and four
      packages exist and appear in no §2 layout. That is a law amendment, not a coverage gap.
  - id: 3
    title: PHASE 0 — FOUNDATIONS
    disposition: mapped
  - id: 4
    title: PHASE 1 — THE CORE
    disposition: mapped
  - id: 5
    title: PHASE 2 — TRADE
    disposition: mapped
  - id: 6
    title: PHASE 3 — PAY + P2P
    disposition: mapped
  - id: 7
    title: PHASE 4 — SOVEREIGN BLUEPRINT
    disposition: mapped
  - id: 8
    title: PHASE 5 — THE REMAINING SURFACES
    disposition: mapped
  - id: 9
    title: CROSS-CUTTING SYSTEMS
    disposition: mapped
  - id: 10
    title: DATA & EVENT LAW
    disposition: mapped
  - id: 11
    title: LAUNCH-SEQUENCE MAPPING
    disposition: mapped
  - id: 12
    title: BUILD PHASES SUMMARY
    disposition: exempt
    reason: >-
      A summary table of §3–§8 with their gates. It introduces no capability of its own; every row of it
      is already carried by the chapter it summarises. Superseded for the protocol lane by §21.
  - id: 13
    title: WHAT IS DELIBERATELY NOT IN v1
    disposition: mapped
  - id: 14
    title: DEFINITION OF DONE
    disposition: mapped
  - id: 15
    title: AGENT EXECUTION PROTOCOL
    disposition: exempt
    reason: >-
      Process for agents, not product. Its five rules live in tooling/agent-protocol/AGENT_PROTOCOL.md
      and AGENTS.md, and the agent-autoload gate asserts they stay in the files a cold agent loads.
  - id: 16
    title: ADDITIONAL DOCTRINES
    disposition: exempt
    reason: >-
      Doctrines 8–10 (two planes, sovereignty by architecture, custody boundary in code). Doctrine 10 is
      the only one with a machine form and it already has one — custody-scan. The rest constrain
      capabilities listed elsewhere; they are not capabilities.
  - id: 17
    title: INTACHAIN — THE SOVEREIGN CHAIN
    disposition: mapped
  - id: 18
    title: THE SOVEREIGN CARD
    disposition: mapped
  - id: 19
    title: WEB4 — THE THIRD PLANE
    disposition: mapped
  - id: 20
    title: BEAT-THE-LEADER TARGETS
    disposition: exempt
    reason: >-
      Engineering SLOs (throughput, finality, latency, auth budget) attached to capabilities carried
      elsewhere — matching, INTACORE, the card, onboarding. They are acceptance numbers on those rows.
      The instrumentation that would let anyone check them is tracked as the §14.5 dashboards gap.
  - id: 21
    title: REVISED PHASE MAP
    disposition: exempt
    reason: >-
      The phase table extended for the protocol lane (3P/4P/5P). Phases are a property of every tracker
      row already. NOTE for the owner's pen — audit §B5 row 10: neither §12 nor §21 has a place for
      §27–§37, which is one structural reason twenty capabilities were never rowed.
  - id: 22
    title: THE SOVEREIGNTY LAW
    disposition: mapped
  - id: 23
    title: SOVEREIGN BANKING
    disposition: mapped
  - id: 24
    title: SOVEREIGN RAILS
    disposition: mapped
  - id: 25
    title: FULL COVERAGE MATRIX
    disposition: mapped
  - id: 26
    title: PRIVACY STACK
    disposition: mapped
  - id: 27
    title: INTAFACED CONNECT
    disposition: mapped
  - id: 28
    title: INTAFACED EXECUTION
    disposition: mapped
  - id: 29
    title: INTAFACED QUANT
    disposition: mapped
  - id: 30
    title: INTEGRATION & REVENUE ADDENDA
    disposition: mapped
  - id: 31
    title: GAP CLOSURES
    disposition: mapped
  - id: 32
    title: MODULE XII — INTAFACED PREDICT
    disposition: mapped
  - id: 33
    title: CREW VAULTS
    disposition: mapped
  - id: 34
    title: LEGACY VAULTS
    disposition: mapped
  - id: 35
    title: LAUNCH TRUST LAYER
    disposition: mapped
  - id: 36
    title: TREASURY YIELD ON STABLES
    disposition: mapped
  - id: 37
    title: INTAFACED INFRA
    disposition: mapped
  - id: 38
    title: ADDENDA
    disposition: mapped

# ── THE CAPABILITIES ─────────────────────────────────────────────────────────
# `law:` is "<section>:<line>" and must resolve — the section must exist and the
# line must fall inside it. One law line, one capability (§25:740, "exactly once").
capabilities:
  # ── §3 PHASE 0 · FOUNDATIONS ───────────────────────────────────────────────
  - id: foundations.monorepo
    capability: Monorepo scaffolded per §2, pnpm + turbo build green
    law: '3:94'
    tracker: [infra.monorepo]
  - id: foundations.compose
    capability: docker compose boots Postgres, Redis, NATS JetStream, Grafana
    law: '3:95'
    tracker: [infra.compose]
  - id: foundations.config
    capability: Typed env loader, feature flags, JURISDICTION_MATRIX
    law: '3:96'
    tracker: [infra.config]
  - id: foundations.events
    capability: NATS subject naming law, versioned payloads
    law: '3:97'
    tracker: [infra.events]
  - id: foundations.contracts
    capability: zod-first contract pattern with one example router
    law: '3:98'
    tracker: [infra.contracts]
  - id: foundations.ci
    capability: CI — lint, typecheck, unit tests, migration check, DoD gate
    law: '3:99'
    tracker: [infra.gates, infra.db-pkg]
  - id: foundations.design-tokens
    capability: Design tokens locked; console/HUD component primitives
    law: '3:100'
    tracker: [infra.ui-tokens]

  # ── §4 PHASE 1 · THE CORE ──────────────────────────────────────────────────
  - id: core.identity
    capability: svc-identity — one account, one verification, one rank
    law: '4:110'
    tracker: [identity.accounts, identity.kyc, infra.auth-pkg]
  - id: core.rank
    capability: Rank mechanics — XP events, single writer, machine-readable perk table
    law: '4:131'
    tracker: [identity.rank]
  - id: core.identity-api
    capability: Auth, KYC, rank and scoped API-key surfaces
    law: '4:137'
    tracker: [identity.apikeys, identity.webauthn]
  - id: core.ledger
    capability: svc-ledger — the single double-entry wallet graph
    law: '4:143'
    tracker: [ledger.double-entry]
  - id: core.ledger-recipes
    capability: Standard flows as ledger recipes — deposit, withdraw, tradeFill, escrow, stake, fees
    law: '4:171'
    tracker: [ledger.recipes, pay.user-money]
  - id: core.token
    capability: svc-token — the native economy, sources, sinks, flywheel as code
    law: '4:180'
    tracker: [token.emissions]
  - id: core.token-mechanics
    capability: Fee currency, access tiers, real-yield staking, buyback and burn, gas
    law: '4:199'
    tracker: [token.staking, token.yield, token.buyback, token.governance]

  # ── §5 PHASE 2 · TRADE ─────────────────────────────────────────────────────
  - id: trade.matching-engine
    capability: svc-matching — isolated, journalled, replayable, deterministic
    law: '5:219'
    tracker: [matching.engine, matching.determinism]
  - id: trade.product-layer
    capability: svc-trade — markets, orders, fills, positions, funding, the money path
    law: '5:229'
    tracker: [trade.spot, trade.futures]
  - id: trade.surfaces
    capability: Pro terminal, Convert card, Copy surfaces, ws-gateway streams
    law: '5:261'
    tracker: [web.terminal, web.shell, ws.depth, ws.gateway]

  # ── §6 PHASE 3 · PAY + P2P ─────────────────────────────────────────────────
  - id: pay.core
    capability: svc-pay — three modes, one core (Gateway, PSP, PayFac)
    law: '6:277'
    tracker: [pay.gateway, pay.psp, pay.payfac]
  - id: pay.rail-adapter
    capability: RailAdapter interface — Doctrine 4, the whole point
    law: '6:298'
    tracker: [pay.rails]
  - id: p2p.core
    capability: svc-p2p — offers, trades, disputes, reputation, merchants
    law: '6:316'
    tracker: [p2p.offers, p2p.escrow, p2p.disputes, p2p.reputation, p2p.merchants]

  # ── §7 PHASE 4 · BLUEPRINT ─────────────────────────────────────────────────
  - id: blueprint.engine
    # The engine vendor's name is what §7:344 calls it; brand-scan (§0.7) forbids
    # repeating it outside the allowlisted paths, and tooling/ is not one of them.
    capability: svc-blueprint — the Neural Engine consumed as an internal service
    law: '7:344'
    tracker: [blueprint.onboarding]
  - id: blueprint.flow
    capability: Signup to reveal card to crew placement to mentor shortlist
    law: '7:361'
    tracker: [blueprint.card, blueprint.crews]
  - id: blueprint.ownership
    capability: Export and hard-delete — portable, deletable
    law: '7:364'
    tracker: [blueprint.ownership]

  # ── §8 PHASE 5 · THE REMAINING SURFACES ────────────────────────────────────
  - id: bank.service
    capability: svc-bank — accounts, loans, earn, cards over the existing ledger
    law: '8:377'
    tracker: [bank.accounts, bank.loans, bank.earn, bank.cards, bank.ramps]
  - id: agents.v1-fleet
    capability: svc-agents — model gateway, fleet runtime, v1 fleet (Navigator, Support, Scanner, Merchant)
    law: '8:384'
    tracker: [agents.gateway, agents.navigator, agents.support, agents.scanner, agents.merchant]
  - id: agents.v2-fleet
    capability: v2 agent fleet — Portfolio, Launch, Risk & Compliance, Coach, Growth
    law: '8:388'
    tracker: [agents.portfolio, agents.launch, agents.risk-compliance, agents.coach, agents.growth]
    note: >-
      Gap closed 2026-08-08. The law names ten agents; the board carried five plus the gateway, with
      Copy-Intel already rowed. One row per agent rather than one bundle row, matching the granularity
      the five v1 agents already use — a bundle would have made "which of the five is missing" an
      unanswerable question again. All five are blocked, each on the surface it would be grounded in
      rather than on each other: Portfolio on ops.portfolio, Launch on launch.trust-layer, Risk &
      Compliance on ops.compliance, Coach on academy.curriculum, Growth on ops.analytics. agents.coach
      is also claimed by m.academy.coach, which §25:708 gives its own matrix row.
  - id: academy.service
    capability: svc-academy — lobbies, spatial layer, ambassadors, curriculum, certifications, tournaments
    law: '8:390'
    tracker:
      [
        academy.lobbies,
        academy.spatial,
        academy.ambassadors,
        academy.curriculum,
        academy.certs,
        academy.tournaments,
        academy.paper-trading,
      ]
  - id: launch.service
    capability: svc-launch — token factory, meme factory, launchpad raises, NFT, RWA
    law: '8:398'
    tracker: [launch.token-factory, launch.meme-factory, launch.launchpad, launch.nft, launch.rwa]
  - id: mining.service
    capability: svc-mining-pool — share protocol, solo and pooled, PPLNS payouts, dashboards
    law: '8:405'
    tracker: [mining.pool]
  - id: dex.service
    capability: svc-dex — non-custodial wallet, AMM pools, smart order router, no-KYC lane
    law: '8:410'
    tracker: [protocol.smart-accounts, protocol.amm, protocol.router]
    note: >-
      §17.5:539 absorbs svc-dex into the Protocol Plane, so these rows are the protocol.* ones. The
      services/svc-dex directory that runs in compose today still has no row of its own — audit §A4.
  - id: market.service
    capability: svc-market — vendor lifecycle, listings, subscriptions, purchases, house commission
    law: '8:417'
    tracker: [market.vendors, market.commerce]
  - id: coreops.service
    capability: svc-core-ops — support, CRM, affiliates, compliance ops, analytics, admin app
    law: '8:422'
    tracker: [ops.support, ops.affiliates, ops.compliance, ops.analytics, ops.admin]

  # ── §9 CROSS-CUTTING SYSTEMS ───────────────────────────────────────────────
  - id: cross.public-api-gateway
    capability: Public API — one gateway in front of trade, pay and data, with keys, scopes and sandbox
    law: '9:430'
    tracker: [api.gateway]
    note: >-
      Gap closed 2026-08-08. The law says ONE gateway; the board had pay.public-api (pay-scoped,
      blocked) and trade.ccxt-api (trade-scoped, and the one recorded orphan), and no owner for the
      single surface. The new row is blocked on both and carries a FLAG rather than a design: §9:430
      and the accepted pay-public-api ADR disagree. That ADR deliberately rejects a shared error
      taxonomy — svc-trade speaks CCXT because bots already do, and payments have no equivalent
      lingua franca — and one gateway with two taxonomies is not one gateway. The row states the two
      readings and picks neither; the key, scope and sandbox plane is the part that is shared either
      way, and identity.apikeys already issues named scoped keys.
  - id: cross.notifications
    capability: Notifications — event-driven fan-out (in-app, push, email/SMS adapters)
    law: '9:431'
    tracker: [ops.notifications]
  - id: cross.i18n
    capability: i18n — all surfaces keyed from day one, 100+ languages as translation files
    law: '9:432'
    tracker: [infra.i18n]
  - id: cross.security
    capability: Security — argon2id, TOTP, WebAuthn, allow-lists, rate limits, three-layer custody ops
    law: '9:433'
    tracker: [identity.accounts, identity.webauthn]
    note: The three-layer custody ops runbook half of this line is tracked separately as core.custody-ops.
  - id: cross.compliance
    capability: Compliance — KYC tiers gate limits per JURISDICTION_MATRIX, checked by every module
    law: '9:434'
    tracker: [identity.kyc, ops.compliance, infra.config]
  - id: cross.observability
    capability: Observability — every recipe, order and payment traced end to end
    law: '9:435'
    tracker: [infra.compose]
    note: infra.compose carries OTel and Grafana. The per-module SLO dashboard half is dod.slo-dashboards.

  # ── §10 DATA & EVENT LAW ───────────────────────────────────────────────────
  - id: data.subjects
    capability: NATS subject law, versioned payloads, idempotent consumers
    law: '10:441'
    tracker: [infra.events]
  - id: data.event-sourcing
    capability: Event sourcing where money moves — ledger, matching journal, payment_events
    law: '10:442'
    tracker: [ledger.double-entry, matching.engine]
  - id: data.pii-isolation
    capability: PII isolation — KYC documents in a separate encrypted store, services get flags only
    law: '10:443'
    tracker: [identity.pii-isolation]
    note: >-
      Gap closed 2026-08-08. Compliance-load-bearing and unrowed since the law was written. The
      services-get-flags half already holds in code — svc-identity's kyc_records carries one opaque
      provider_ref documented against §10:443 by name — but it holds VACUOUSLY, because no
      verification provider is integrated and there is no document anywhere to isolate. The row says
      so: an invariant that holds only because the feature is empty is a coincidence with a deadline,
      and the store plus a gate asserting no service schema grows a document column have to exist
      before the first document arrives. One of only three rows in this wave that is genuinely
      claimable today; which provider is the owner's, and Class X.

  # ── §11 LAUNCH-SEQUENCE MAPPING ────────────────────────────────────────────
  - id: core.drop-flags
    capability: Drop phases 0–V as feature flags — waitlist, referral queue, founding badges, season engine, limited drops
    law: '11:456'
    tracker: [infra.drop-flags]
    note: >-
      Gap closed 2026-08-08. "The drop sequence is configuration, not deployment risk" — and the
      configuration did not exist, while drop_phases below already named this capability under four of
      the six drop gates. The row is the SWITCH, not the features behind it: it depends only on
      infra.config (done) and reads ready, because putting launch.nft in its dependencies for the
      founding-badge mint would have parked a phase-1 framework behind a phase-5 row and recreated the
      inverted-edge defect this PR removed from venue.aggregation. A flag can exist and be OFF for a
      feature that does not — that is what dark shipping means — but the row carries the done-bar rule
      that an OFF flag for an unbuilt feature must read as unbuilt, not as ready.

  # ── §13 WHAT IS DELIBERATELY NOT IN v1 ─────────────────────────────────────
  - id: socket.rust-port
    capability: Rust engine port — interface ready, port when volume demands
    law: '13:470'
    tracker: [socket.rust-matching, chain.rust-core]
  - id: socket.venue-aggregation
    capability: External liquidity venue aggregation — adapter interface ready
    law: '13:471'
    tracker: [venue.aggregation]
  - id: socket.live-rails
    capability: Live card issuer / bank / PSP partner rails — adapters ready, crypto-native real from day one
    law: '13:472'
    tracker: [socket.live-issuer, socket.psp-partners]
  - id: socket.vr-client
    capability: VR lobby client — scene state ready
    law: '13:473'
    tracker: [socket.vr-client]
  - id: socket.mpc-custody
    capability: MPC custody for DEX wallets
    law: '13:474'
    tracker: [socket.mpc-custody]

  # ── §14 DEFINITION OF DONE ─────────────────────────────────────────────────
  - id: dod.slo-dashboards
    capability: Observability — traces plus at least one SLO dashboard panel, per module
    law: '14:483'
    # BOARDED 2026-08-08. Was a gap ("infra.compose stops at the OTel and Grafana
    # containers", audit BUILD-COVERAGE-AUDIT-2026-08-03.md · A1.a #17) and is no
    # longer: traces land via packages/telemetry/src/start.ts, and the metrics
    # half now exists — a /metrics endpoint on svc-edge, a scrape target that was
    # verified UP against real prom/prometheus:v3.0.1, and a provisioned Grafana
    # dashboard real grafana:11.4.0 loaded with all five panels returning series.
    #
    # THE "per module" HALF OF THIS CAPABILITY IS NOT CLOSED, and the row is
    # mapped rather than exempt so it stays visible. One of nineteen services
    # emits metrics. The panel is an edge-measured SLO covering 15 modules as
    # label values; svc-ws, svc-ledger and svc-matching sit outside the edge and
    # are not covered at all. `infra.slo-dashboards`'s note carries the full
    # scope statement, including what a reader must NOT infer from `done`.
    tracker: [infra.slo-dashboards]
  - id: dod.gate
    capability: The Definition of Done itself — enforced per module, never half done
    law: '14:479'
    tracker: [infra.gates]

  # ── §17 INTACHAIN ──────────────────────────────────────────────────────────
  - id: chain.intacore
    capability: INTACORE — fully on-chain CLOB, perp and spot, protocol margining, one-block finality
    law: '17:510'
    tracker: [chain.mainnet, socket.clob-contracts]
  - id: chain.intaevm
    capability: INTAEVM — EVM environment secured by the same validators, reading INTACORE state
    law: '17:511'
    tracker: [chain.evm]
  - id: chain.contracts-p0
    capability: P0 — sovereign contract suite on proven rails (smart accounts, AMM, lending, on-chain escrow)
    law: '17:514'
    tracker: [protocol.smart-accounts, protocol.amm, protocol.lending, protocol.escrow]
  - id: chain.decentralisation
    capability: P3 — validator set opening on a published schedule, IFC-weighted governance handover
    law: '17:517'
    tracker: [chain.validators, chain.governance]
  - id: chain.ifc-onchain
    capability: IFC on-chain — gas asset, staking security budget, one supply across two planes via the bridge
    law: '17:520'
    tracker: [bridge.canonical, token.staking]
  - id: chain.smart-accounts
    capability: Passkey-native smart accounts — session-scoped permissions, never withdrawal rights
    law: '17:525'
    tracker: [protocol.smart-accounts]
  - id: chain.new-services
    capability: svc-chain, svc-indexer, svc-bridge, svc-protocol
    law: '17:531'
    tracker: [chain.mainnet, indexer.readmodels, bridge.canonical, protocol.merchant]
    note: >-
      services/svc-chain and services/svc-bridge do not exist on disk. audit §B4 — chain.mainnet
      declares a dependency block that is not the real blocker, which is an untaken §17.2 decision.

  # ── §18 THE SOVEREIGN CARD ─────────────────────────────────────────────────
  - id: card.sovereign
    capability: Sovereign card — self-custody funded, JIT conversion at authorization, tiered access
    law: '18:544'
    tracker: [bank.sovereign-card]
  - id: card.adapter-isolated
    capability: Card rides CardIssuerAdapter — issuer risk is a swappable module
    law: '18:546'
    tracker: [bank.cards, socket.live-issuer]

  # ── §19 WEB4 ───────────────────────────────────────────────────────────────
  - id: web4.attestations
    capability: Portable sovereign identity — rank, reputation and Blueprint as zero-PII on-chain attestations
    law: '19:551'
    tracker: [blueprint.attestations]
  - id: web4.agents-both-planes
    capability: Agents across both planes — Navigator executes on INTACORE via user-granted session keys
    law: '19:552'
    tracker: [agents.navigator, protocol.smart-accounts]
  - id: web4.one-economy
    capability: One economy — one IFC supply, one buyback fed by both planes, escrow custodial or sovereign at user choice
    law: '19:553'
    tracker: [token.buyback, p2p.escrow, protocol.escrow]

  # ── §22 THE SOVEREIGNTY LAW ────────────────────────────────────────────────
  - id: sovereign.routing-rule
    capability: Zero-KYC follows custody — permissionless where the platform never holds the asset
    law: '22:585'
    tracker: [dex.permissionless-access]
    note: >-
      WAS A GAP, closed by main on 2026-08-03 rather than by this PR. The audit's finding was never
      that the rule was unbuilt — packages/config/jurisdiction.ts and contracts/trpc.ts have carried
      it for weeks — but that it was absent from the board, so nothing could report on it. The `dex`
      module put it on the board: `dex.permissionless-access` names §503 and §585 in its own title,
      and checkAccess short-circuits the declared non-custodial plane to allowed.permissionless
      before any tier is read. Both this and §25:642 now cite it; one law rule written in two places
      is two capabilities and one row, which is the shape the mapping is for.
  - id: sovereign.tiered-fiat
    capability: Where the platform holds the asset or touches card rails — tiered verification per JURISDICTION_MATRIX
    law: '22:586'
    tracker: [identity.kyc, ops.compliance]

  # ── §23 SOVEREIGN BANKING ──────────────────────────────────────────────────
  - id: sovereign.vaults
    capability: Smart-account vaults — multi-asset self-custody balances with banking-grade UX
    law: '23:593'
    tracker: [protocol.smart-accounts]
  - id: sovereign.swap
    capability: Sovereign convert — smart router across INTACORE books and AMM pools via session key
    law: '23:594'
    tracker: [protocol.router, trade.convert]
  - id: sovereign.loans
    capability: On-chain lending markets — collateral in contracts, keeper liquidations, no identity
    law: '23:595'
    tracker: [protocol.lending]
  - id: sovereign.yield
    capability: On-chain yield only on this plane — validator staking, supply APY, LP fees, protocol real yield
    law: '23:596'
    tracker: [token.yield, protocol.amm]
  - id: sovereign.p2p-fiat-bridge
    capability: Non-custodial P2P fiat bridge — contract escrow, counterparty pays their own bank rail
    law: '23:598'
    tracker: [protocol.escrow]
  - id: sovereign.pay-anyone
    capability: Handle-based transfers, payment requests, split bills — all contract transfers
    law: '23:599'
    tracker: [protocol.merchant, blueprint.attestations]

  # ── §24 SOVEREIGN RAILS ────────────────────────────────────────────────────
  - id: rails.lane-a-merchant
    capability: Lane A — merchant contract on the user's own smart account, zero KYB, no flow of funds
    law: '24:606'
    tracker: [protocol.merchant]
  - id: rails.lane-a-surfaces
    capability: Lane A payment links, hosted checkout, QR, invoices, subscriptions as contract interactions
    law: '24:607'
    tracker: [protocol.merchant, pay.subscriptions]
  - id: rails.lane-b-fiat
    capability: Lane B — in-house gateway, PSP core, PayFac trees, routing, fraud engine, checkout, analytics
    law: '24:613'
    tracker: [pay.gateway, pay.psp, pay.payfac, pay.routing, pay.fraud, socket.psp-partners]
  - id: rails.lane-b-kyb
    capability: Lane B tiered KYB — email to KYB to live in a day
    law: '24:614'
    tracker: [pay.psp, identity.kyc]

  # ── §25 FULL COVERAGE MATRIX · TRADE ───────────────────────────────────────
  - id: m.trade.spot
    capability: Spot order book (majors, ERC-20, custom listings, fiat pairs)
    law: '25:624'
    plane: B
    phase: 2 / 4P
    tracker: [trade.spot, matching.engine]
  - id: m.trade.futures
    capability: Futures — cross/isolated, insurance fund, funding
    law: '25:625'
    plane: B
    phase: 2 / 4P
    tracker: [trade.futures]
  - id: m.trade.options
    capability: Options — calls/puts, strategy builder
    law: '25:626'
    plane: F to P
    phase: 2 / 5P
    tracker: [trade.options]
  - id: m.trade.otc
    capability: OTC desk — RFQ, staked gate
    law: '25:627'
    plane: F
    phase: 2
    tracker: [trade.otc]
  - id: m.trade.convert
    capability: Convert one-tap
    law: '25:628'
    plane: B
    phase: 2 / 3P
    tracker: [trade.convert]
  - id: m.trade.copy
    capability: Copy trading — audited leaders, profit share
    law: '25:629'
    plane: B
    phase: 2 / 5P
    tracker: [trade.copy, agents.copy-intel, socket.copy-auto-mirror-place]
    note: >-
      Audit §B2 — the law line and the row both still say "profit share", which
      SPEC-SOVEREIGN-ROUTING-AND-COPY-2026-08-01.md:95 forbids in v1. Reported for the owner's pen; the
      law is not edited from here. socket.copy-auto-mirror-place is the §13 refuse-closed place half
      after planMirror (D26-P1-T3) — never invent follower fills.
  - id: m.trade.forex
    capability: Forex pairs
    law: '25:630'
    plane: F
    phase: 2
    tracker: [trade.forex, socket.forex-settlement]
    note: >-
      socket.forex-settlement is the §13 refuse-closed half (D26-P1-T7) until D26-P0-05 settlement asset
      law + fiat settle rails; trade.forex stays the product row. Never invent settlement asset.
  - id: m.trade.matching
    capability: Matching engine, full depth
    law: '25:631'
    plane: B
    phase: 2 / 4P
    tracker: [matching.engine, matching.determinism]
  - id: m.trade.liquidity
    capability: Deep liquidity — internal MM day one, venue aggregation later
    law: '25:632'
    plane: B
    phase: 2
    tracker: [trade.mm-bot, venue.aggregation]
  - id: m.trade.algo
    capability: Algo execution TWAP/VWAP/EWAP/PVOL
    law: '25:633'
    plane: F
    phase: 2
    tracker: [trade.algo]
    note: §30:795 moves this row to svc-execution and generalises it cross-venue. That service has no row.
  - id: m.trade.subaccounts
    capability: Sub-accounts plus consolidated reporting
    law: '25:634'
    plane: B
    phase: 1
    tracker: [identity.apikeys]
    note: >-
      Audit §A1.d — folded into the identity.apikeys title, while SPEC-SUBACCOUNTS-2026-08-02.md treats
      it as its own M5 product. Visible here, not visible on the board.
  - id: m.trade.terminal
    capability: Pro terminal
    law: '25:635'
    plane: B
    phase: 2
    tracker: [web.terminal]
    note: The "native mobile" half of this law row is the separate gap core.mobile-apps (§25:727).
  - id: m.trade.languages
    capability: 100+ languages
    law: '25:636'
    plane: B
    phase: 0
    tracker: [infra.i18n]

  # ── §25 · DEX (absorbed into the Protocol Plane) ───────────────────────────
  - id: m.dex.wallet
    capability: Web3 self-custody wallet — multi-chain, MFA
    law: '25:639'
    plane: P
    phase: 3P
    tracker: [protocol.smart-accounts]
  - id: m.dex.onchain-trading
    capability: Smart-contract trading — on-chain settlement
    law: '25:640'
    plane: P
    phase: 3P / 4P
    tracker: [socket.clob-contracts, chain.mainnet]
  - id: m.dex.pools
    capability: Liquidity pools — AMM, IFC pairs seeded
    law: '25:641'
    plane: P
    phase: 3P
    tracker: [protocol.amm]
  - id: m.dex.no-kyc
    capability: No-KYC lane — the entire plane, by architecture
    law: '25:642'
    plane: P
    phase: 3P
    tracker: [dex.permissionless-access]
    note: >-
      WAS A GAP, closed by main on 2026-08-03. The finding was that the platform's differentiating
      claim had no board presence beyond one clause inside another row's note, while being real in
      code at jurisdiction.ts:363 and trpc.ts:114-175. `dex.permissionless-access` is that row. Same
      closure as §22:585 above — the doctrine side and the matrix side of one rule.
  - id: m.dex.routing
    capability: Smart routing — book versus pool best execution
    law: '25:643'
    plane: P
    phase: 3P
    tracker: [protocol.router, dex.route-preview, dex.quote-router]
    note: >-
      The two dex.* rows arrived on main 2026-08-03 and are the cross-venue half of this line:
      route-preview is the arithmetic over caller-supplied quotes, quote-router sources prices from
      live venues and refuses rather than falling back. Neither is done, and quote-router cannot be
      until socket.dex-venue-set is decided — which is a venue decision, not code.
  - id: m.dex.oversight
    capability: Operator oversight — analytics, zero custody
    law: '25:644'
    plane: P
    phase: 4P
    tracker: [indexer.readmodels, ops.admin]

  # ── §25 · P2P ──────────────────────────────────────────────────────────────
  - id: m.p2p.direct
    capability: Direct maker/taker trading
    law: '25:647'
    plane: B
    phase: 3
    tracker: [p2p.offers]
  - id: m.p2p.escrow
    capability: Escrow protection plus moderated disputes
    law: '25:648'
    plane: B
    phase: 3 / 3P
    tracker: [p2p.escrow, p2p.disputes, protocol.escrow]
  - id: m.p2p.fiat
    capability: 100+ fiat currencies, any payment method
    law: '25:649'
    plane: B
    phase: 3
    # "any payment method" is two halves and only one of them was on the board.
    # `p2p.offers` + `infra.config` carry the currency registry and the method
    # NAME on an offer; `p2p.payment-instruments` carries the destination the
    # buyer actually sends money to. Without the second, a trade could reach
    # every terminal state and still never complete.
    tracker: [p2p.offers, infra.config, p2p.payment-instruments]
  - id: m.p2p.reputation
    capability: Reputation system on the same XP graph
    law: '25:650'
    plane: B
    phase: 3
    tracker: [p2p.reputation, identity.rank]
  - id: m.p2p.merchants
    capability: Merchant programme — badges, limits, API
    law: '25:651'
    plane: B
    phase: 3
    tracker: [p2p.merchants]

  # ── §25 · LAUNCH ───────────────────────────────────────────────────────────
  - id: m.launch.launchpad
    capability: Token launchpad — ICO/IDO, vesting, staked allocation tiers
    law: '25:654'
    plane: P
    phase: 5 / 3P
    tracker: [launch.launchpad]
  - id: m.launch.meme
    capability: Meme factory — one-click, instant market plus LP
    law: '25:655'
    plane: P
    phase: 5P
    tracker: [launch.meme-factory]
  - id: m.launch.custom-asset
    capability: Custom asset creation — ERC-20, multi-chain, admin
    law: '25:656'
    plane: B
    phase: 5
    tracker: [launch.token-factory]
  - id: m.launch.instant-listing
    capability: Instant listing
    law: '25:657'
    plane: B
    phase: 5
    tracker: [launch.token-factory, trade.spot]
    note: Audit §A1.d — adjacent to launch.token-factory, never named again on the board.
  - id: m.launch.fundraising
    capability: Fundraising module — milestones, investor management
    law: '25:658'
    plane: F
    phase: 5
    tracker: [launch.fundraising]
    note: >-
      Gap closed 2026-08-07. Row added deliberately UNOWNED — the product surface is fiat plane and
      putting it on the chain owner would widen him past his sole mountain; the on-chain milestone
      escrow and vesting legs sit under launch.trust-layer.
  - id: m.launch.nft
    capability: NFT marketplace — mint/auction, on-chain royalties
    law: '25:659'
    plane: P
    phase: 5
    tracker: [launch.nft]
  - id: m.launch.rwa
    capability: RWA tokenisation — licence-gated
    law: '25:660'
    plane: F
    phase: 5
    tracker: [launch.rwa]
  - id: m.launch.structured
    capability: Structured issuance — wrapped, synthetic, structured products
    law: '25:661'
    plane: F
    phase: 5
    tracker: [launch.structured]
    note: >-
      Gap closed 2026-08-07 with the socket row the entry itself asked for, so §14.8 can check it.
      Unowned for the same reason as launch.fundraising: the product is fiat plane, the wrapper
      contracts are chain.

  # ── §25 · BANK ─────────────────────────────────────────────────────────────
  - id: m.bank.accounts
    capability: Multi-currency accounts
    law: '25:664'
    plane: B
    phase: 5 / 3P
    tracker: [bank.accounts, protocol.smart-accounts]
  - id: m.bank.loans
    capability: Crypto loans — LTV, margin calls, liquidation
    law: '25:665'
    plane: B
    phase: 5 / 3P
    tracker: [bank.loans, protocol.lending]
    note: >-
      Audit §A2 — bank.loans is scored done while marking off last trade, because §8.1's "svc-trade
      index prices" names a component nobody built. The row does not say so.
  - id: m.bank.earn
    capability: Earn and staking — flexible/fixed, native real yield
    law: '25:666'
    plane: B
    phase: 1–5 / 3P
    tracker: [bank.earn, token.staking, token.yield]
  - id: m.bank.virtual-cards
    capability: Virtual cards — instant, Apple/Google Pay
    law: '25:667'
    plane: B
    phase: 5
    tracker: [bank.cards]
  - id: m.bank.physical-cards
    capability: Physical cards — rank-linked tiers, controls
    law: '25:668'
    plane: B
    phase: 5
    tracker: [bank.cards, bank.sovereign-card]
  - id: m.bank.cashback
    capability: Cashback in token
    law: '25:669'
    plane: B
    phase: 5
    tracker: [bank.cards, ledger.recipes]
    note: Audit §A1.d — swallowed by bank.cards; the rewardPay leg is not separately visible.
  - id: m.bank.ramps
    capability: Fiat on/off ramp
    law: '25:670'
    plane: F
    phase: 3–5
    tracker: [bank.ramps, pay.rails]
  - id: m.bank.defi
    capability: DeFi modules — P2P lending, strategies
    law: '25:671'
    plane: P
    phase: 5P
    tracker: [protocol.lending]
  - id: m.bank.loan-automation
    capability: Smart-contract loan automation
    law: '25:672'
    plane: P
    phase: 3P
    tracker: [protocol.lending]

  # ── §25 · PAY ──────────────────────────────────────────────────────────────
  - id: m.pay.gateway
    capability: Branded gateway — zero third-party branding
    law: '25:675'
    plane: F
    phase: 3
    tracker: [pay.gateway]
  - id: m.pay.psp
    capability: PSP mode — own the merchant, digital KYC/KYB, custom pricing
    law: '25:676'
    plane: F
    phase: 3
    tracker: [pay.psp]
  - id: m.pay.payfac
    capability: PayFac mode — sub-merchants, 14 permission areas, settlement control
    law: '25:677'
    plane: F
    phase: 3
    tracker: [pay.payfac]
  - id: m.pay.routing
    capability: Smart routing — geo, card, risk, approval rate
    law: '25:678'
    plane: F
    phase: 3 / 5
    tracker: [pay.routing, agents.merchant]
  - id: m.pay.checkout-channels
    capability: Multi-channel checkout — PayPal, Stripe rails, 100+ crypto assets, external wallets, guest checkout
    law: '25:679'
    plane: B
    phase: 3
    tracker: [pay.rails, socket.psp-partners]
  - id: m.pay.onboarding
    capability: Merchant onboarding — 3-step, tiered limits, live in a day
    law: '25:680'
    plane: F
    phase: 3
    tracker: [pay.psp, identity.kyc]
  - id: m.pay.settlement
    capability: Dual settlement — bank IBAN/IFSC or crypto
    law: '25:681'
    plane: F
    phase: 3
    tracker: [pay.settlement]
  - id: m.pay.api-first
    capability: API-first — keys, scopes, webhooks, whitelist, sandbox
    law: '25:682'
    plane: B
    phase: 3
    tracker: [pay.public-api, identity.apikeys]
  - id: m.pay.fraud
    capability: Fraud and risk — scoring, chargeback, decline recovery
    law: '25:683'
    plane: F
    phase: 3 / 5
    tracker: [pay.fraud, agents.merchant]
  - id: m.pay.checkout-builder
    capability: Checkout builder — fields, fee routing, discounts, shipping
    law: '25:684'
    plane: B
    phase: 3
    tracker: [pay.gateway]
    note: Audit §A1.d — folded into pay.gateway and never named again.
  - id: m.pay.subscriptions
    capability: Subscriptions — card and crypto recurring
    law: '25:685'
    plane: B
    phase: 3 / 3P
    tracker: [pay.subscriptions]
  - id: m.pay.high-risk
    capability: High-risk verticals — Lane B policy, Lane A permissionless
    law: '25:686'
    plane: B
    phase: 3 / 3P
    tracker: [pay.psp, protocol.merchant]
  - id: m.pay.analytics
    capability: Revenue analytics and export
    law: '25:687'
    plane: B
    phase: 3
    tracker: [pay.gateway, ops.analytics]
  - id: m.pay.links
    capability: Payment links plus hosted checkout
    law: '25:688'
    plane: B
    phase: 3 / 3P
    tracker: [pay.gateway, protocol.merchant]
  - id: m.pay.any-user-merchant
    capability: Merchant crypto acceptance for any user — zero KYB
    law: '25:689'
    plane: P
    phase: 3P
    tracker: [protocol.merchant]
  - id: m.pay.plugins
    capability: Commerce plugins — Woo, Magento, OpenCart
    law: '25:690'
    plane: B
    phase: 3
    tracker: [pay.plugins]

  # ── §25 · MARKET ───────────────────────────────────────────────────────────
  - id: m.market.categories
    capability: All 8 vendor categories
    law: '25:693'
    plane: B
    phase: 5
    tracker: [market.vendors]
  - id: m.market.vendor-model
    capability: Vendor model — vet, listing fees, commissions, premium placement, stake-gated slots
    law: '25:694'
    plane: B
    phase: 5
    tracker: [market.vendors, market.commerce, token.staking]

  # ── §25 · ACADEMY ──────────────────────────────────────────────────────────
  - id: m.academy.lobbies
    capability: Live session lobbies — capacity tiers including penthouse
    law: '25:697'
    plane: B
    phase: 5
    tracker: [academy.lobbies]
  - id: m.academy.spatial
    capability: Spatial meta layer — avatars, stage, presence, VR-ready
    law: '25:698'
    plane: B
    phase: 5
    tracker: [academy.spatial, socket.vr-client]
  - id: m.academy.ambassadors
    capability: Ambassador residencies — IFC pay, revenue share, displayed rank
    law: '25:699'
    plane: B
    phase: 5
    tracker: [academy.ambassadors]
  - id: m.academy.rooms
    capability: Specialist rooms — futures, options, meme war room, forex, DeFi lab, merchant clinic
    law: '25:700'
    plane: B
    phase: 5
    tracker: [academy.lobbies]
    note: Audit §A1.d — a room taxonomy carried inside a lobby row whose note says it is not built here.
  - id: m.academy.crew-sessions
    capability: Crew sessions — private lobbies, leaderboards, challenges
    law: '25:701'
    plane: B
    phase: 4 / 5
    tracker: [blueprint.crews, academy.lobbies]
  - id: m.academy.clips
    capability: Stream-native design — clippable
    law: '25:702'
    plane: B
    phase: 5
    tracker: [academy.lobbies, socket.stream-provider]
    note: Audit §A1.d — the clip export pipeline has no row of its own.
  - id: m.academy.tournaments
    capability: Events and tournaments — competitions, paper leagues, prize pools, card-tier prizes
    law: '25:703'
    plane: B
    phase: 5
    tracker: [academy.tournaments, academy.paper-trading]
  - id: m.academy.paths
    capability: Structured paths — Blueprint-sequenced
    law: '25:704'
    plane: B
    phase: 5
    tracker: [academy.curriculum, blueprint.onboarding]
  - id: m.academy.library
    capability: Playbooks and workbooks — 20 plus 3 proprietary library imported day one
    law: '25:705'
    plane: B
    phase: 5
    tracker: [academy.curriculum, academy.paper-trading]
  - id: m.academy.certs
    capability: Certification ranks to real perks
    law: '25:706'
    plane: B
    phase: 1 / 5
    tracker: [academy.certs, identity.rank]
  - id: m.academy.video
    capability: Video library
    law: '25:707'
    plane: B
    phase: 5
    tracker: [academy.video]
    note: >-
      Gap closed 2026-08-08. A named media service in the matrix with no row and no service on disk.
      Blocked on academy.curriculum, because §25:705 puts the playbook and workbook library in the
      curriculum spine and a video library is the media half of that same shelf. The row separates two
      blockers that would otherwise hide behind each other: socket.stream-provider is LIVE streaming
      (LiveKit, owner API key), while this is stored playback, whose real precondition is object
      storage and a transcode path — neither of which exists, the same unprovisioned dependency
      blueprint.card records for its rasterizer. Done-bar condition on the row: §25:710 gates Academy
      access by tier and stake, so signed, expiring, tier-checked delivery or it is not done.
  - id: m.academy.coach
    capability: AI Coach
    law: '25:708'
    plane: B
    phase: 5
    tracker: [agents.coach]
    note: >-
      Gap closed 2026-08-08. One of the ten agents §8.2 names; the board carried five. Still listed
      separately from agents.v2-fleet because §25:708 gives the Coach its own matrix row, and both
      capabilities now claim the same agents.coach row — which is the honest shape rather than a
      duplicate. Blocked on academy.curriculum: a coach grounded in nothing is a chatbot. The row flags
      the one question an agent must not answer by itself — whether the Coach may reference the user's
      own positions, which turns education into a recommendation about live money.
  - id: m.academy.community
    capability: Community and mentorship — matched crews, boards, leader pipeline
    law: '25:709'
    plane: B
    phase: 4 / 5
    tracker: [blueprint.crews, academy.lobbies]
  - id: m.academy.access
    capability: Free tier, premium, staked lobbies, ambassador subscriptions
    law: '25:710'
    plane: B
    phase: 5
    tracker: [academy.lobbies, academy.ambassadors, token.staking]

  # ── §25 · CORE ─────────────────────────────────────────────────────────────
  - id: m.core.support
    capability: Support desk — AI first-line plus KB
    law: '25:713'
    phase: 5
    tracker: [ops.support, agents.support]
  - id: m.core.business-systems
    capability: CRM, HR and team, Finance with live revenue fused, Project engine
    law: '25:714'
    phase: 5
    tracker: [ops.business-systems]
    note: >-
      Gap closed 2026-08-08. Four named internal systems against one adjacent row (ops.support, which
      is tickets and KB). Blocked on ops.support and ops.analytics — a CRM sits on the support desk's
      contact and case data, and "Finance with live revenue fused" is the warehouse read rather than a
      second aggregation. The matrix gives plane "—"; the row states F as operator-side and
      identity-bearing and says explicitly that this is not a plane ruling. FLAGGED for scope, not
      design: CRM and Finance sit next to revenue we already record, but HR & team and a Project engine
      are internal business software with no customer edge, and nothing says we build rather than buy
      those. That call is worth taking before anyone starts.
  - id: m.core.compliance
    capability: Compliance — geo KYC/AML, geo-block, VPN/Tor detect, per-jurisdiction
    law: '25:715'
    plane: F
    phase: 0–5
    tracker: [ops.compliance, identity.kyc, infra.config]
  - id: m.core.analytics
    capability: Analytics — volume, cohorts, funnels, export
    law: '25:716'
    phase: 5
    tracker: [ops.analytics]
  - id: m.core.affiliates
    capability: Affiliate and IB network — multi-tier, sub-trees, payout automation
    law: '25:717'
    phase: 5
    tracker: [ops.affiliates]
  - id: m.core.referrals
    capability: Referrals — token-paid, tracked
    law: '25:718'
    phase: 5
    tracker: [ops.affiliates]
    note: Audit §A1.d — adjacent to ops.affiliates, not separately visible.
  - id: m.core.marketing
    capability: Marketing engine plus Growth Agent
    law: '25:719'
    phase: 5
    tracker: [ops.marketing, agents.growth]
    note: >-
      Gap closed 2026-08-08. There was no row for the engine, and Growth was one of the five agents also
      missing — so this capability claims both halves the matrix line names. Blocked on ops.analytics,
      ops.affiliates and agents.growth: a marketing engine with no attribution is a send button, and
      attribution already lives in the affiliate trees. The row carries the constraint that matters
      more than the feature set — returns-ranked leaderboards and curve-fit marketing are already
      banned in three places, and a campaign template with a performance figure in it breaches all
      three quietly. It also records that the outbound rail does not currently deliver: email, push and
      SMS refuse with channel.not_configured until the owner supplies gateway credentials.
  - id: m.core.kb-workflow
    capability: Knowledge base and workflow automation
    law: '25:720'
    phase: 5
    tracker: [ops.kb-workflow]
    note: >-
      Gap closed 2026-08-08. ops.support carried "tickets, KB" in its TITLE, which is exactly how this
      stayed invisible — a word in another row's title is not a row — and the workflow automation engine
      beside it had no board presence at all. Blocked on ops.support, which is the right edge in both
      directions. The row separates the two capabilities the matrix line fuses, because they are not the
      same size: a versioned knowledge base is a content surface, while a workflow automation engine
      running user-defined triggers across modules is an agent runtime with a different name, and it
      must not become a second one beside agents.gateway or the Agentic Law has a hole in it. Worth an
      owner ruling on whether the automation half ships at all.
  - id: m.core.mining-ops
    capability: Mining pool ops — solo and pooled, dashboards, payouts
    law: '25:721'
    plane: B
    phase: 5
    tracker: [mining.pool]
  - id: m.core.custody-ops
    capability: Custody — cold/warm/hot wallets, multi-sig approval workflow
    law: '25:722'
    plane: F
    phase: 2
    tracker: [ops.custody]
    note: >-
      Gap closed 2026-08-07. Split rather than given one owner: the multi-sig contract and the
      hot-wallet perimeter are the chain owner's (board S-B2), the tiering policy and approval
      console are agent work, and real keys stay Class X.
  - id: m.core.portfolio
    capability: Portfolio suite — users and house
    law: '25:723'
    plane: B
    phase: 5
    tracker: [ops.portfolio]
    note: >-
      Gap closed 2026-08-08. Portfolio views over ledger plus indexer — no row, and no service claimed
      it. Blocked on indexer.readmodels, which is precisely the second half of the substrate the matrix
      names: the ledger half is done, the on-chain half is not, and a portfolio showing only the
      custodial side of a two-plane platform is misleading rather than partial. The row carries that as
      its load-bearing rule — a holding the platform cannot read is ABSENT AND NAMED, never zero, the
      same principle D-S-18 fixed for venues — and notes that a portfolio is a view under §0.6 and
      holds no balance of its own. It also blocks agents.portfolio, which cannot rebalance against a
      portfolio nobody can read.
  - id: m.core.site-builder
    capability: Site builder — drag-drop, live asset APIs, domains, SSL
    law: '25:724'
    phase: 5
    gap:
      audit: 'BUILD-COVERAGE-AUDIT-2026-08-03.md · A1.a #5'
      note: >-
        A named svc-core-ops surface with no row. DELIBERATELY LEFT AS A GAP on 2026-08-08, when the
        other twenty-two on this list were boarded, and the reason is worth writing down rather than
        leaving as an omission. Every other capability boarded that day had either a real dependency
        edge on an existing row — so `blocked` computed and was honest — or a written decision that
        made its state honest. This one has neither. A drag-drop website builder with live asset APIs,
        custom domains and SSL touches nothing else on the board, so there is no edge to express; with
        no edge it would render `ready`, which is the tracker saying "claim this now" about a product
        nobody has decided to build. `socket` is equally wrong: it means deliberately not in v1, and no
        such decision exists — the interface does not exist either. Domains and SSL are also a
        registrar and ACME dependency on an external party, which is a second thing nobody has chosen.
        NEEDS AN OWNER SCOPE RULING, and until then a gap honestly recorded says more than a row that
        misrepresents what is decided. The ratchet keeps it visible and keeps the set from growing.
  - id: m.core.social-promotion
    capability: Social promotion — one-tap share, tracked attribution, every surface
    law: '25:725'
    plane: B
    phase: 4 / 5
    tracker: [ops.social-promotion]
    note: >-
      Gap closed 2026-08-08. blueprint.card covers rendering the acquisition artefact and is done; its
      distribution half — the share pipeline and attribution tracking on every surface — had no row.
      The matrix gives phase 4/5 and the registry has no range bucket, so the row sits at 5: the phase-4
      half IS the card, which already shipped, and phasing the remainder at 4 would have put its
      phase-5 dependency (ops.affiliates) in front of a phase-4 row — the same inverted-edge defect this
      PR removed from venue.aggregation and v22.alerts. Blocked on ops.affiliates, which owns the
      multi-tier trees attribution belongs in. Flagged on the row: a share URL that outlives a deleted
      Blueprint breaks §26 and blueprint.ownership, so the attribution token has to be revocable, and
      that is an ADR-shaped question rather than a default.
  - id: m.core.developer-platform
    capability: Developer platform — public APIs, docs, sandbox
    law: '25:726'
    plane: B
    phase: 3
    tracker: [pay.public-api]
    note: The single-gateway obligation behind this row is the separate gap cross.public-api-gateway (§9:430).
  - id: core.mobile-apps
    capability: Mobile apps — iOS and Android, own name, zero attribution
    law: '25:727'
    plane: B
    phase: 2–5
    tracker: [web.mobile-apps]
    note: >-
      Gap closed 2026-08-08. Named twice in the matrix (:635 and :727) and phased from 2, with zero rows
      and zero code. The row sits at 2 rather than 5 — the lower bound the matrix gives, and the phase
      being worked — because filing it at 5 would have hidden that it is already late; the registry has
      no "2–5" bucket so the range is recorded on the row. Blocked on web.terminal, which is wip:
      mirroring a surface that is mid-port is how two front ends become three. FLAGGED for a product
      ruling before any code, because the law and an accepted ADR disagree — §25:727 specifies a React
      Native app in apps/, while the retire-apps-web ADR empties apps/ and makes the served surface the
      vendored Vue shell. Distribution under our own name is a separate owner item: an app-store listing
      for a crypto app is reviewed on jurisdiction, not only on code.

  # ── §25 · TOKEN, BLUEPRINT, AGENTS ─────────────────────────────────────────
  - id: m.token.all
    capability: Fee currency, access tiers, PoC mining, real-yield staking, governance, gas, rewards, burn-buyback
    law: '25:730'
    plane: B
    phase: 1 / 5 / 4P
    tracker:
      [token.emissions, token.staking, token.yield, token.buyback, token.governance, mining.pool]
  - id: m.blueprint.all
    capability: Blueprint — session, reveal card, matching, crews, mentor fit, curriculum, artifact, owned and private
    law: '25:731'
    plane: B
    phase: 4
    tracker:
      [
        blueprint.onboarding,
        blueprint.card,
        blueprint.crews,
        blueprint.ownership,
        blueprint.attestations,
      ]
  - id: m.agents.all
    capability: All 10 agents plus Agentic Law, model-agnostic, premium subscriptions
    law: '25:732'
    plane: B
    phase: 5
    tracker:
      [
        agents.gateway,
        agents.navigator,
        agents.support,
        agents.scanner,
        agents.merchant,
        agents.copy-intel,
      ]
    note: Six of the ten. The other five are the gap agents.v2-fleet (§8:388) plus m.academy.coach (§25:708).

  # ── §25 · REVENUE, FLYWHEEL, DROP, ROADMAP ─────────────────────────────────
  - id: m.revenue.streams
    capability: All 20 revenue streams
    law: '25:735'
    phase: per phase
    exempt:
      reason: >-
        Revenue streams are fee parameters on the service rows that earn them, admin-configurable per
        §8.8 — the law's own answer in this very row. There is no deliverable to track separately, and
        a row called "20 revenue streams" could never be finished or falsified.
      decided: '25:735'
  - id: m.revenue.flywheel
    capability: Token sources, sinks and flywheel — one buyback, both planes
    law: '25:736'
    phase: 1 / 4P
    tracker: [token.buyback, token.yield, bridge.canonical]
  - id: m.drop.flags
    capability: Drop phases 0–V as feature flags
    law: '25:737'
    phase: launch
    exempt:
      reason: >-
        This matrix row is a pointer to the §11 mapping, and §11 is carried as its own capability
        core.drop-flags — which is an open gap. Tracking the same thing twice would double-count it and
        break the "exactly once" rule this file enforces.
      decided: '25:737'
  - id: m.roadmap.vol1
    capability: Vol. I 8-week roadmap items
    law: '25:738'
    exempt:
      reason: >-
        The law says of this row, in the row itself, "superseded by §12/§21 phase gates — same content,
        honest sequence". There is nothing here the phase map does not already carry.
      decided: '25:738'
  - id: m.coverage.check
    capability: coverage-check in CI against a machine-readable tooling/coverage.yaml
    law: '25:740'
    tracker: [infra.gates]
    note: >-
      This file and tooling/ci/coverage-check.mjs. Audit §A1.a #19 counted it as one of the forty gaps;
      it is the one the PR that created this file closes. See baseline: for the arithmetic.

  # ── §26 PRIVACY STACK ──────────────────────────────────────────────────────
  - id: privacy.attestations
    capability: Zero-PII attestations for rank and Blueprint on-chain; PII never leaves the Fiat Plane store
    law: '26:743'
    tracker: [blueprint.attestations]
  - id: privacy.no-resale
    capability: No ads, no data resale, ever — the business model is fees, stated as law
    law: '26:745'
    exempt:
      reason: >-
        A prohibition, not a deliverable. Nothing gets built to satisfy it; it is violated by shipping
        something, and the thing that would violate it (an ads or data-sale surface) would need its own
        row and would be refused at review. A tracker row here could never reach done.
      decided: '26:745'
  - id: privacy.stealth-and-client-analytics
    capability: Optional stealth handles on the Protocol Plane, and client-side analytics for sovereign vaults
    law: '26:746'
    tracker: [protocol.stealth-handles]
    note: >-
      Gap closed 2026-08-07. blueprint.attestations covers the zero-PII half; the stealth half — one
      human, two unlinkable presentations — and aggregate-only indexer analytics are now a row owned
      by the chain owner. Neither can be retrofitted once addresses are public.

  # ── §27 INTAFACED CONNECT · v2.1, 0% tracked ───────────────────────────────
  - id: connect.venue-fabric
    capability: Unified venue schema and adapter classes across CEX, DEX and FX/CFD rails
    law: '27:758'
    tracker: [venue.aggregation]
    note: >-
      The only §27 line with any board presence, and the row's title says "via CCXT" while this section
      forbids a third-party connectivity library in the money path. Audit §A2 and §B5 row 8.
  - id: connect.latency-grading
    capability: Latency grading — every adapter scored live, feeding execution routing weights
    law: '27:760'
    tracker: [connect.latency-grading]
    note: >-
      Gap closed 2026-08-08. §30:793 phases Connect at 2 — the phase being worked — and this had no
      board presence at all. The row carries D-S-18's rule verbatim: latency grading is a measurement,
      never an estimate, so an unmeasured adapter has no score and receives no routing weight. It
      computes blocked on venue.aggregation, which is one public market-data adapter with the trading
      half deliberately unbuilt; there is nothing to grade against anything.
  - id: connect.venue-vault
    capability: Venue Vault — per-user encrypted external API keys, HSM-backed, withdrawal permission refused
    law: '27:761'
    tracker: [connect.venue-vault]
    note: >-
      Gap closed 2026-08-07. venue.aggregation had admitted "Venue Vault absent" in its own note
      since 2026-08-02 with no row behind it. Owned by the chain owner because it is key custody,
      not because it is protocol plane — and it is the hard blocker under socket.dex-execution.
  - id: connect.data-lake
    capability: Unified data lake — normalised ticks, books and fills to a time-series store
    law: '27:762'
    tracker: [connect.data-lake]
    note: >-
      Gap closed 2026-08-08. The law calls it our data moat and the backtest fuel for §29, and D-S-18
      records the same edge from the other side — Quant is blocked on this lake, the lake on §27's
      adapters. Capture only is decided; the store is not — no time-series database is chosen or in
      either compose file. Blocked on venue.aggregation.

  # ── §28 INTAFACED EXECUTION · v2.1, 0% tracked ─────────────────────────────
  - id: execution.sor
    capability: svc-execution — cross-venue Smart Order Router, OMS/EMS, execution reports
    law: '28:770'
    tracker: [execution.sor]
    note: >-
      Gap closed 2026-08-08. §30:793 phases this at 2 — the phase being worked — and svc-dex refuses by
      name because "svc-execution does not exist"
      (services/svc-dex/src/quote/market-data-source.ts:190). The row is explicit that the ranking rule
      is NOT re-opened: packages/venue-adapter already ranks on effective price with one bounded, tested
      5 bps internal tie-break (D-S-06 accepted), and this capability extends that cost model with
      impact, latency grade and transfer cost rather than proposing a second thumb on the scale. No
      services/svc-execution is authorised by the row existing.
  - id: execution.algo-engine
    capability: Algo engine generalised cross-venue — TWAP/VWAP/EWAP/PVOL, iceberg, sniper, pairs, basis
    law: '28:771'
    tracker: [trade.algo]
    note: §30:795 moves the §5.2 algo suite here. trade.algo is the single-venue ancestor of this row.
  - id: execution.arbitrage
    capability: Arbitrage engine — cross-exchange, triangular, spot-perp basis, funding, DEX to CEX with inventory
    law: '28:772'
    tracker: [execution.arbitrage]
    note: >-
      Gap closed 2026-08-08. Rides the one ranking rule under execution.sor — no per-leg preference —
      and keeps the law's own honesty term, "no bridge-latency fantasy": a DEX-to-CEX opportunity priced
      on a bridge completing inside the spread is refused, not sized. What is not decided is capital:
      every opportunity class needs house inventory both sides and no inventory policy or exposure cap
      exists. Blocked on execution.sor.
  - id: execution.market-making
    capability: Market-making engine — internal MM and external-venue MM, one engine
    law: '28:773'
    tracker: [execution.market-making]
    note: >-
      Gap closed 2026-08-08, with the tension named on the row rather than left to be discovered:
      trade.mm-bot covers internal book seeding only, working the street is a different product, and
      §28:773 wants one engine for both — which collides with the futures mark law, "a price that moves
      money is never supplied by the party it pays". mark-from-depth.ts records the measured version of
      that collision (two dust orders minted a payout-grade mid worth 2,000 USDT). Internal quotes may
      seed liquidity; they may not become a mark. Spread and inventory magnitudes stay owner numbers.
  - id: execution.risk-spine
    capability: Risk spine — pre-trade checks per strategy, venue exposure caps, drawdown halts, global kill-switch
    law: '28:775'
    tracker: [ops.admin]
    note: >-
      The global kill-switch half lives in ops.admin, whose own state is that every switch is browser
      useState (audit §C3 row 1). The per-strategy pre-trade checks belong to the unrowed svc-execution.
  - id: execution.house-tenant
    capability: House desk sealed private tenant — the Throne Law
    law: '28:777'
    tracker: [execution.house-tenant]
    note: >-
      Gap closed 2026-08-08 — FLAGGED, NOT DESIGNED. A tenancy isolation requirement (separate keys,
      separate namespace, strategies never in this repo) with no test asserting the seal holds. A house
      desk trading alongside customers is a conflict-of-interest and fairness question, so the row says
      an owner ADR precedes any code and deliberately proposes no answer — not what the desk may see of
      customer flow, not whether it may quote markets customers trade. §30:795 gives this row's plane as
      "—"; the row's F is custody of house money, not a plane ruling.

  # ── §29 INTAFACED QUANT · v2.1, 0% tracked ─────────────────────────────────
  - id: quant.studio
    capability: Strategy Studio — no-code visual builder with mandatory risk blocks, Blueprint-aware defaults
    law: '29:783'
    tracker: [quant.studio]
    note: >-
      Gap closed 2026-08-08. The law calls this the surface no exchange ships properly and it had no row
      and no service. Phase 5 comes from §30:793 verbatim, not from convenience. The blocker is not
      inferred either: `docs/adr/2026-08-04-predict-quant-connect-law.md` (D-S-18, Accepted) rules that
      §29 is blocked on §27's data lake, which is blocked on §27's adapters — so the row depends on
      connect.data-lake. The row also records what the same ADR leaves open under "what still needs the
      owner": WHETHER QUANT SHIPS TO USERS AT ALL. Boarding it stops the capability being invisible; it
      does not assert the product is going ahead, and no svc-quant should be scaffolded off it.
  - id: quant.sdk
    capability: Code SDK — TypeScript and Python against a sandboxed strategy runtime, deterministic replay
    law: '29:784'
    tracker: [quant.sdk]
    note: >-
      Gap closed 2026-08-08. Listed with the studio in the audit and kept separate here because the
      sandbox is its own security surface — §30:797 adds a sandbox-escape test suite to the DoD for it,
      and §29:787 requires V8 isolates or WASM with no raw network, which means a runtime that can reach
      the network is not a sandbox with a gap. Blocked on quant.studio and execution.sor, because
      §29:784 executes "via the same OMS" and svc-execution does not exist. §30:793 phases SDK and
      Marketplace at "5+" while §30:795 lists the SDK at 5; the row sits at 5 and records the
      inconsistency rather than inventing a 5+ phase key the by-phase render has no bucket for. The row
      also keeps the Venue Vault as a DONE-BAR condition for the external-venue half rather than a
      blocking edge, and states how that differs from the inverted edge removed from venue.aggregation.
  - id: quant.backtest
    capability: Backtest engine — event-level on the data lake, walk-forward, Monte Carlo, overfitting honesty
    law: '29:785'
    tracker: [quant.backtest]
    note: >-
      Gap closed 2026-08-08. It depends on the §27 data lake, which now has a row of its own, so the
      edge is expressed rather than described. The honesty regime finally has somewhere to live, and the
      row carries it verbatim from D-S-18 because this is the surface where the doctrine is under the
      most pressure: a TRUTHFUL number computed over real history can still be the most misleading thing
      on the screen, so the dishonesty is in the framing rather than the arithmetic. Binding from line
      one and not re-openable by a claimant: no out-of-sample verdict means the result does not render;
      unmodelled fees or slippage means the run is refused rather than caveated; the variant count is
      displayed; live P&L is never shown beside a backtest unlabelled; and no leaderboard ranked by
      historical return, in any room. §30:797 adds a backtest determinism test to the DoD.
  - id: quant.marketplace
    capability: Strategy marketplace and compute tiers — subscriptions, profit share, token-gated tiers
    law: '29:788'
    tracker: [quant.marketplace]
    note: >-
      Gap closed 2026-08-08. Copy-trading generalised to systems, commissioned per §8.7, and neither
      market.commerce nor trade.copy claimed it. Blocked on quant.studio and market.commerce — a second
      commerce path would be a second answer to the same question. FLAGGED, because a contradiction the
      board already carries once arrives here a second time: §29:788 offers publication as subscriptions
      "or profit-share", while SPEC-SOVEREIGN-ROUTING-AND-COPY forbids any fee computed from follower
      P&L in v1 in any form, and audit §B5 #1 records the law being stale the same way for trade.copy.
      If leaders may not charge on P&L, a strategy marketplace that does is the same structure with a
      different noun, and nobody has ruled that systems differ from leaders. An ADR settles it, not a
      PR. D-S-18's ban on ranking by historical return also removes the obvious ranking signal, so it
      has to be designed rather than defaulted. Compute-tier magnitudes are owner-only under D-S-14.

  # ── §30 INTEGRATION & REVENUE ADDENDA ──────────────────────────────────────
  - id: addenda.v21-matrix
    capability: v2.1 coverage matrix additions — venue fabric, SOR, arb, MM, studio, SDK, marketplace
    law: '30:795'
    exempt:
      reason: >-
        Every row this addendum adds is carried individually above under §27, §28 and §29. Listing them
        again here would claim the same capability twice, which §25:740 forbids and this gate enforces.
      decided: '30:795'
  - id: addenda.v21-revenue
    capability: New revenue streams — quant subscriptions, marketplace commissions, execution fees, house PnL
    law: '30:796'
    exempt:
      reason: >-
        Same reasoning as m.revenue.streams — revenue is a fee parameter on the capability that earns
        it, not a separate deliverable. The capabilities themselves are the §27–§29 gaps above.
      decided: '25:735'
  - id: addenda.v21-dod
    capability: DoD additions — venue adapter conformance, backtest determinism, sandbox escape, key-scope audit
    law: '30:797'
    exempt:
      reason: >-
        These are acceptance criteria that attach to connect.venue-fabric, quant.backtest, quant.sdk and
        connect.venue-vault. They become §14 conditions on those rows when the rows exist; a DoD clause
        with no capability under it is not a capability.
      decided: '30:797'

  # ── §31 GAP CLOSURES · v2.2, 0% tracked ────────────────────────────────────
  - id: v22.auto-invest
    capability: Auto-invest — DCA schedules, card round-ups, threshold sweeps, both planes
    law: '31:805'
    tracker: [bank.auto-invest]
    note: >-
      Gap closed 2026-08-08. Table stakes against every neobank, phased 5 by the law, and no row on the
      board. The row says plainly that the FIAT half is buildable today — bank.accounts, bank.cards,
      bank.earn and trade.convert are all done, so scheduled buys, card round-ups and threshold sweeps
      are ordinary ledger-recipe work — and that it still reads blocked because §31:805 specifies BOTH
      planes and the sovereign half funds from a vault via standing session-key allowance, which is
      protocol.smart-accounts and not done. A standing allowance simulated custodially would be the
      opposite of what the law asked for. Nothing here needs an owner magnitude: round-up granularity
      and sweep thresholds are user settings, so D-S-14 does not gate this one.
  - id: v22.tax-engine
    capability: svc-tax — per-jurisdiction reporting, lot accounting, export packs, owned not vendored
    law: '31:807'
    tracker: [tax.engine]
    note: >-
      Gap closed 2026-08-08. A new service named in the law, owned not vendored. The advantage the law
      claims — nothing to re-import, because it reads the ledger and the data lake directly — is exactly
      what is missing: the ledger half is done, connect.data-lake does not exist, and on-chain history
      needs indexer.readmodels, so the row is blocked on the two reads that make it worth building. It
      also records that the load-bearing half is COUNSEL, not engineering: which lot method applies in
      which jurisdiction, and what a spend-disposal is when a card converts crypto at the till, are legal
      determinations of the same class as the sanctions list. An agent may build the lot-accounting
      engine and the export format; a wrong jurisdiction mapping shipped confidently is worse than no
      tax product, so the honest v1 boundary refuses by name outside a small mapped set.
  - id: v22.alerts
    capability: Alerts and watchlists — price, funding, liquidation proximity, whale flow, portfolio digests
    law: '31:809'
    tracker: [v22.alerts]
    note: >-
      Gap closed 2026-08-08. The law phases the ALERTS CORE at Phase 2 — the phase being worked — while
      ops.notifications is fan-out, not alerting: nothing evaluated a condition and decided to notify,
      and nothing said so. The row keeps the two halves apart (core at 2, intelligence tiers at 5) and
      carries the refusal rule: an alert compared against a price the platform cannot source refuses
      rather than firing on a stale number. DEPENDENCY CORRECTED the same day: the row had depended on
      ops.notifications, which is phase 5, so a phase-5 prerequisite stood in front of a capability the
      law phases at 2. §31:809 is explicit on the phase and §38:854 repeats it, so the edge was argued
      down rather than the phase moved. What separates ops.notifications from done is out-of-app gateway
      credentials the owner must obtain — a done-bar residual on a supplier, not a start-bar dependency
      on the alerts core — and it now sits on this row's own done bar instead: no `done` while an alert
      can only reach an in-app inbox.
  - id: v22.business-banking
    capability: svc-bank-biz — corporate accounts, maker/checker, expense cards, invoicing, crypto payroll
    law: '31:811'
    tracker: [bank.business]
    note: >-
      Gap closed 2026-08-08, and this one is the clearest case on the list for why the gate exists:
      §38:854 added a payroll-run integrity test (multi-recipient atomicity) to the DoD, and there is no
      svc-bank-biz, so the law has carried a TEST FOR A SERVICE THAT DOES NOT EXIST since the day it was
      written. A DoD condition with no capability under it is a condition nobody can fail. The row says
      so on its face. Blocked on pay.gateway because §31:811 wires invoicing to Pay; bank.accounts and
      bank.cards are done and are what it builds on. Two things are written onto the row now rather than
      discovered later: under §0.6 a payroll run is one ledger movement or it is refused, because a run
      that pays 40 of 50 and reports success is precisely the failure §38:854 names; and §31:811 requires
      KYB per Lane B, which exists nowhere on the board — identity.kyc is individual KYC, and a corporate
      account holder is not a person.

  # ── §32 MODULE XII · INTAFACED PREDICT ─────────────────────────────────────
  - id: v22.predict
    capability: INTAFACED PREDICT — prediction markets as a first-class INTACORE market type
    law: '32:815'
    tracker: [predict.markets]
    note: >-
      Gap closed 2026-08-08. An entire named module of the product — the twelfth room — with zero board
      presence: §38:853 records the module count moving 11 to 12 and the tracker did not know it existed.
      Phase 5P and plane P are the law's. The owner is the chain owner because an accepted ADR says so
      rather than by inference: D-S-18 states "§32 Predict — Phase 5P. Not agent work, and not mine" and
      lists every §32 decision under what needs the owner, so agents babysit only. Blocked on
      chain.mainnet, because prediction markets are an INTACORE market type and INTACORE does not exist.
      The row carries the two properties D-S-18 fixed so they are not decided by whoever writes the first
      line: RESOLUTION IS THE PRODUCT and is specced before the market type is built, and every bond and
      slashing magnitude is owner-only. §38:854's resolution-dispute game-theory test suite is the second
      DoD addition on this list with nothing to attach to.

  # ── §33 CREW VAULTS ────────────────────────────────────────────────────────
  - id: v22.crew-vaults
    capability: Crew vaults — shared multi-sig crew treasuries, member shares, threshold spending, split on exit
    law: '33:825'
    tracker: [protocol.crew-vaults]
    note: >-
      Gap closed 2026-08-07. It turns blueprint.crews from a social object into a balance sheet, so
      it is a money path and a Class M row; the exit split must be designed before anyone deposits,
      not after.

  # ── §34 LEGACY VAULTS ──────────────────────────────────────────────────────
  - id: v22.legacy-vaults
    capability: Legacy vaults — time-locked inheritance, guardian M-of-N recovery, staged heir release
    law: '34:829'
    tracker: [protocol.legacy-vaults]
    note: >-
      Gap closed 2026-08-07, and the contradiction is carried onto the row rather than buried:
      §34:829 describes guardian M-of-N recovery, socket.social-recovery states the platform must
      never be a guardian. The row names the only shape that satisfies both — heirs and time locks
      the user sets, no platform key ever eligible — and says an ADR settles it, not a PR.

  # ── §35 LAUNCH TRUST LAYER ─────────────────────────────────────────────────
  - id: v22.launch-trust
    capability: Launch trust layer — enforced LP locks, vesting proofs, deployer reputation, honest-market badges
    law: '35:834'
    tracker: [launch.trust-layer]
    note: >-
      Gap closed 2026-08-07. The token factory already removes the crudest rug — no mint, no owner,
      no pause, no upgrade selector in the deployed bytecode — but the pool half (LP locks, vesting
      proofs, deployer reputation) had no row, so the anti-rug architecture was missing without
      being recorded as missing.

  # ── §36 TREASURY YIELD ON STABLES ──────────────────────────────────────────
  - id: v22.treasury-yield
    capability: Tokenized T-bill vaults — stable balances opt into RWA treasury yield, licence-gated
    law: '36:841'
    tracker: [launch.treasury-yield]
    note: >-
      Gap closed 2026-08-07. launch.rwa recorded the licensing blocker for the issuance registry
      while this half of the pair had no row at all. Contract half is the chain owner's; the licence
      is Class X and no contract makes that go away.

  # ── §37 INTAFACED INFRA ────────────────────────────────────────────────────
  - id: v22.infra-b2b
    capability: INTAFACED INFRA — embeddable ramp widget and white-label tiers, the B2B line
    law: '37:845'
    tracker: [ops.infra-b2b]
    note: >-
      Gap closed 2026-08-08. The law calls this potentially the largest single revenue line in the stack
      and says it is packaging rather than new core, and it had zero rows — while §38:854 already added a
      white-label tenant isolation audit to the DoD with nothing to attach it to, the third such orphaned
      DoD clause on this list. Blocked on pay.payfac and bank.ramps, which are the existing multi-tenancy
      §37:848 says it runs on; the white-label half additionally needs the §28:777 execution tenancy,
      which is execution.house-tenant and behind an owner ADR of its own. §37:848 says "Phase 5+"; the
      row sits at 5 and records the wording, because the registry has no 5+ bucket and an unknown phase
      key drops a row out of the by-phase render. FLAGGED, and the flag is not technical: licensing the
      OS to an operator under their brand means our engine moving their customers' money under someone
      else's compliance posture. "Packaging, not new core" is true of the code and false of the risk.

  # ── §38 ADDENDA ────────────────────────────────────────────────────────────
  - id: addenda.v22-matrix
    capability: v2.2 coverage matrix additions — auto-invest, tax, alerts, biz, Predict, crew, legacy, launch trust, treasury, infra
    law: '38:851'
    exempt:
      reason: >-
        All ten are carried individually above under §31–§37, each as its own gap with its own note.
        Listing them again here would claim the same capabilities twice, which the exactly-once rule
        forbids.
      decided: '38:851'
  - id: addenda.v22-revenue
    capability: Revenue engine extends to 30 streams
    law: '38:852'
    exempt:
      reason: >-
        Same reasoning as m.revenue.streams and addenda.v21-revenue — a revenue stream is a fee
        parameter on the capability that earns it. Every capability behind these six streams is an open
        gap above.
      decided: '25:735'
  - id: addenda.v22-lineup
    capability: Vol. I lineup gains PREDICT, BIZ, LEGACY, INFRA — modules 11 to 12, products 24 to 28
    law: '38:853'
    exempt:
      reason: >-
        A restatement of the module count implied by §31, §32, §34 and §37, all four of which are
        carried as gaps above. The count itself is not a deliverable.
      decided: '38:853'
  - id: addenda.v22-dod
    capability: DoD additions — payroll atomicity, resolution-dispute game theory, guardian recovery, tenant isolation
    law: '38:854'
    exempt:
      reason: >-
        Acceptance criteria that attach to v22.business-banking, v22.predict, v22.legacy-vaults and
        v22.infra-b2b. They become §14 conditions on those rows when the rows exist. A DoD clause with
        no capability under it is not a capability.
      decided: '30:797'

# ── ROWS ON THE BOARD THAT DO NOT IMPLEMENT A LAW LINE ───────────────────────
# The other direction. A row here is not wrong — the law is a document and the
# repo is alive — but it must answer to SOMETHING written down. `basis: orphan`
# means it answers to nothing, and orphans are ratcheted.
#
#   adr | spec | decision  → `source:` must be a document that exists on disk
#   law-extension          → `extends:` must be a section number that exists
#   orphan                 → nobody decided it. Counted in baseline.orphans.
tracker_extra:
  - id: trade.ccxt-api
    basis: orphan
    reason: >-
      No law basis, no ADR, no spec — `grep -l CCXT docs/adr/ docs/SPEC-*.md` returns nothing. It is
      acknowledged in passing at docs/DIRECTION-2026-07-31.md:21 as an existing fact, never decided as
      product. It is now the de-facto public trade API, with no owner and no spec, and §27:755 forbids
      a third-party connectivity library in the money path — which is what its name promises.
  - id: identity.kyc-review
    basis: decision
    source: docs/decisions/kyc-posture.md
    reason: >-
      Routed KYC with an operator approve/reject queue is an operational shape §4.1 does not describe.
      The posture decision is written down and the row implements it.
  - id: identity.step-up
    basis: decision
    source: docs/decisions/kyc-posture.md
    reason: >-
      Step-up challenge minting a short-lived trade:withdraw scope. §9:433 requires withdrawal
      allow-lists and delay tiers but not this mechanism; the row's own note explains the bug it closed.
  - id: infra.worktrees
    basis: decision
    source: CONTRIBUTING.md
    reason: >-
      Worktree tooling and the GitHub Flow it enforces. Pure developer process, not product law — and
      it is the mechanism that keeps two agents out of one working directory.
  - id: socket.contract-toolchain
    basis: law-extension
    extends: 13
    reason: >-
      Foundry plus a contract test suite in CI. §13 lists five deliberate non-v1 items and this is not
      one of them, so §14.8's "zero TODOs without a §13 socket entry" is checked against a list that
      does not contain it. Legitimate evolution, unamended law — audit §B5 row 3.
  - id: socket.contract-audit
    basis: law-extension
    extends: 13
    reason: >-
      External audit of the account and factory suite, blocking every mainnet deploy. Same §13
      arithmetic as socket.contract-toolchain — the law lists five sockets, the board carries nineteen.
  - id: socket.userop-differential-test
    basis: law-extension
    extends: 13
    reason: >-
      getUserOperationHash checked against a live EntryPoint. A §17.4 correctness obligation held open
      as a socket, which §13 as written has no room for.
  - id: socket.p256-verifier
    basis: law-extension
    extends: 13
    reason: >-
      Passkey P-256 owner verifier contract. §17.4:525 promises passkey-native accounts; the verifier
      that makes it true is held as a socket the law does not list.
  - id: socket.paymaster-policy
    basis: law-extension
    extends: 13
    reason: >-
      Who pays gas. §17.4:525 promises passkey-native accounts and says nothing about gas, so an
      account that is provably the user's and cannot move is inside the law as written. The paymaster
      fields already exist in the user operation this repo builds; the policy and the funded account
      behind it do not, and §13 has no room for that gap.
  - id: socket.bundler-policy
    basis: law-extension
    extends: 13
    reason: >-
      Bundler dependency, public relay or self-hosted. The law assumes a user operation reaches the
      chain and never names who relays it. A public bundler can censor or reorder that operation,
      which is a sovereignty question §16-§17 implies and does not decide.
  - id: socket.price-oracle
    basis: law-extension
    extends: 13
    reason: >-
      Price oracle for on-chain marks and liquidations. §17.5 lending markets require marks and the
      law names no source; a guessed mark liquidates real collateral, so this is held open rather
      than invented. Blocks protocol.lending and couples to protocol.amm.
  - id: socket.deployment-registry
    basis: law-extension
    extends: 13
    reason: >-
      Which contracts are deployed where, verified against which source. §17.2 P0 assumes contracts
      on proven rails; nothing in the law asks for the record of what was deployed, and without it a
      deployed address cannot be reconstructed or audited later.
  - id: socket.ledger-sharding
    basis: law-extension
    extends: 13
    reason: >-
      Per-asset hash chains with a cross-shard anchor. A scaling path for §4.2's single hash chain,
      deliberately deferred, and §13 does not list it.
  - id: socket.evm-rpc
    basis: law-extension
    extends: 13
    reason: >-
      A real EVM ChainSource with RPC and log decoding. §17.2 P0 assumes proven rails exist; the row
      holds the seam open. Marked done — the socket is filled, and the entry stays until §13 is amended.
  - id: socket.clob-contracts
    basis: law-extension
    extends: 13
    reason: >-
      An audited venue contract emitting the indexed event surface. §17.1 describes INTACORE; nothing
      in §13 reserves the contract that would emit it.
  - id: socket.dex-venue-set
    basis: law-extension
    extends: 13
    reason: >-
      A venue this platform actually quotes. Arrived on main 2026-08-03 with the dex module. §13
      reserves sockets for rails that must be filled from outside; it does not carry this one, and
      the blocker is explicitly a decision rather than code — svc-dex can read three kinds of venue
      and no ADR or spec names which one the platform quotes.
  - id: socket.options-settlement-asset-law
    basis: law-extension
    extends: 13
    reason: >-
      Options / forex settlement asset law (D26-P0-05). Named 2026-08-12 with D26-P1-T6: svc-trade
      refuses kind=options listing while TRADE_OPTIONS_SETTLEMENT_ASSET_LAW is empty rather than
      inventing live set, settlement asset, or refuse matrix. §13 reserves owner-gated sockets;
      the ADR content is the owner decision, not craft — §13 does not list this seam by name.
  - id: socket.dex-fee-source
    basis: law-extension
    extends: 13
    reason: >-
      An authoritative per-venue fee and settlement schedule. Fees are configured rather than sourced,
      and the settlement cost is a declared understatement, so the seam is held open rather than
      pretended shut. §2 forbids reading svc-trade's markets table, which is where the real schedule
      lives — the deferral is law-shaped and §13 does not list it.
  - id: socket.dex-execution
    basis: law-extension
    extends: 13
    reason: >-
      Execution against a quoted venue. §27 names the vault and §28 the OMS, so the destination is
      law; the socket is the seam between a router that can only price and a service that can fill.
  - id: socket.indexer-stream
    basis: law-extension
    extends: 13
    reason: >-
      A live book and tape feed from the projection. §5.3 promises ws-gateway streams; the protocol-side
      transport for them is deferred as a socket §13 does not carry.
  - id: socket.stream-provider
    basis: law-extension
    extends: 13
    reason: >-
      A real WebRTC SFU behind StreamProvider. §8.3 names self-hosted LiveKit as v1, so the interface is
      law but the deferral is not — §13 does not list it.
  - id: socket.notify-push
    basis: law-extension
    extends: 13
    reason: >-
      Push channel, blocked on gateway credentials. §9:431 makes notification adapters law; §13 does not
      reserve the credential-blocked ones, so these three sockets extend it.
  - id: socket.notify-email
    basis: law-extension
    extends: 13
    reason: >-
      Email channel, blocked on gateway credentials. Same §13 arithmetic as socket.notify-push.
  - id: socket.notify-sms
    basis: law-extension
    extends: 13
    reason: >-
      SMS channel, blocked on gateway credentials. Same §13 arithmetic as socket.notify-push.
  - id: socket.pay-precharge-notify
    basis: law-extension
    extends: 13
    reason: >-
      SPEC-PAY-VERTICALS §4 requires every recurring charge notified before it lands. Delivery is not
      on tip (due runner opens invoices with no pre-charge hook); §13 does not name this socket, so the
      board row extends it. Inventing a silent upcoming event is forbidden until a real notify/journal path exists.
  - id: socket.geo-region-resolution
    basis: law-extension
    extends: 13
    reason: >-
      Per-request region resolution from a trusted upstream geo header. svc-edge reads DEFAULT_REGION
      once and stamps that one value onto the principal of every request, so §24 Lane A's sanctions
      screening — which is law, and is correctly implemented in packages/config — evaluates ONE
      CONSTANT REGION for all traffic. §13 reserves five deliberate non-v1 items and does not carry
      this one, and the deferral is real: region must never be caller-supplied, so closing it is a
      deployment-topology decision about which CDN header is trusted and what happens when it is
      absent, not a header read. Recorded as a socket rather than left silent because the undeclared
      version of this gap reads as a claim that traffic is screened.
  - id: socket.social-recovery
    basis: law-extension
    extends: 13
    reason: >-
      CONTRADICTS THE LAW, not merely extending it. The row says a guardian is a second party who can
      take the account and the platform must never be one. §17.4:525 lists social recovery as a property
      of the smart account, §34:829 specs guardian M-of-N as a premium product, and §38:854 adds a
      guardian-recovery DoD test. A tracker row overrode two law sections with no ADR — audit §B5 row 2.
      Recorded here so the contradiction is visible rather than buried in a note; the owner's pen resolves it.
  - id: socket.pay-chargeback-ledger-wire
    basis: law-extension
    extends: 13
    reason: >-
      Chargeback ledger recipes exist in packages/ledger-client (chargebackOpen / Shortfall / Won /
      ShortfallRecovered) with an explicit Class M owner-sign-off banner. svc-pay records dispute cases
      and projects payment → disputed (D26-P1-P5 mechanism) but must not call those recipes until the
      four named questions in chargeback.ts are signed. Closing this socket = wire dispute open to
      post; inventing split legs or shortfall policy is forbidden. Blocklist / sanctions list content
      remains Class X and is out of scope. Named §13 residual row in features.mjs — not a stale
      tracker_extra pointing at a missing id.

# ── §11 DROP PHASES ──────────────────────────────────────────────────────────
# §25:740 — "any Vol. I feature without a green DoD at its phase gate blocks the
# drop phase that promised it." A phase blocks only once `promised: true`. That
# is the difference between a gate that enforces a promise and a gate that fails
# every run before anyone has promised anything.
drop_phases:
  - id: '0'
    name: Tease
    law: '11:449'
    promised: false
    capabilities: [core.drop-flags, m.core.mining-ops]
  - id: I
    name: Blueprint
    law: '11:450'
    promised: false
    capabilities: [m.blueprint.all, core.drop-flags, m.core.social-promotion]
  - id: II
    name: Lobby preview
    law: '11:451'
    promised: false
    capabilities: [m.academy.lobbies, m.core.mining-ops, m.bank.virtual-cards]
  - id: III
    name: Soft launch
    law: '11:452'
    promised: false
    capabilities: [core.drop-flags, m.launch.meme]
  - id: IV
    name: Public drop
    law: '11:453'
    promised: false
    capabilities: [m.trade.spot, m.token.all, m.academy.tournaments]
  - id: V
    name: Seasons
    law: '11:454'
    promised: false
    capabilities: [core.drop-flags, m.academy.tournaments]

# ── THE RATCHET ──────────────────────────────────────────────────────────────
# Both lists must EQUAL, BY NAME, what the gate finds.
#
#   a gap not in this list  → new drift. This is the failure the gate exists for.
#   a line no longer a gap  → you closed something. Delete the line in the same
#                             PR. A baseline parked above the truth is a window
#                             left open, and it is how allowlists rot into
#                             wallpaper.
#
# NAMED, NOT COUNTED — and this is not a formatting preference. These were two
# integers, `gaps: 41` and `orphans: 1`, and an integer cannot tell "nothing
# changed" from "one closed and one opened in the same PR". A swap leaves the
# count at 41 and the gate goes green over precisely the new drift it was built
# to stop. Identity has no such hole. It also makes the debt legible: `41` names
# nobody, and a reviewer cannot see which one a PR is moving.
#
# Landing note: a strict gate would be red on main the hour it merged — forty
# missing rows is forty failures — and a gate that arrives red gets switched off
# within a day. So today's gaps are written down, named, and frozen. New drift
# fails immediately; the existing backlog is visible and shrinking rather than
# blocking everyone. Precedent: BASELINE in tooling/ci/fabricated-money-scan.mjs,
# where each frozen finding is likewise held by its exact text rather than a tally.
baseline:
  # Law capabilities with no tracker row and no exemption decision.
  #
  # The audit counted 40 by hand. This file lists 41, and the difference is
  # three deliberate bookkeeping decisions, none of them a new finding:
  #
  #   −1  §25:740 itself — coverage.yaml and coverage-check — was audit gap #19.
  #       It is the one gap this PR closes.                            → 39
  #   +1  The no-KYC lane is written in two places the gate cites separately:
  #       §22:585 (the doctrine) and §25:642 (the matrix row). Audit item #1
  #       counted it once. One law line, one capability, so it is two.  → 40
  #   +1  Audit item #28 is "Strategy Studio + Code SDK". §29 gives them
  #       separate lines and §30:797 puts the sandbox-escape suite only on the
  #       SDK, so they are split — the sandbox is its own security surface. → 41
  #
  # 2026-08-08: this list went from twenty-three entries to one, across two PRs
  # that overlapped by accident and are worth distinguishing here.
  #
  #   · #1167 closed dod.slo-dashboards by BUILDING it — /metrics on svc-edge, a
  #     scrape target verified UP against real prom/prometheus, a provisioned
  #     Grafana dashboard whose panels return live series. `infra.slo-dashboards`
  #     is `done` on that evidence, and its own note records the honest residual:
  #     1 of 19 services instrumented, 18 to go.
  #   · This PR closed the remaining twenty-one by BOARDING them — a tracker row
  #     each, expressing the real dependency edge and letting `blocked` compute.
  #     Twenty-one rows for twenty-one capabilities is not the row count, because
  #     the v2 agent fleet is five rows and two capabilities share one row.
  #
  # Both directions of the ratchet did their job during that overlap: this branch
  # had also written a `ready` row for dod.slo-dashboards, and rebasing surfaced it
  # as a duplicate id against #1167's `done` one. The `ready` row was dropped — a
  # capability that has shipped must not be re-boarded as claimable, which is the
  # same failure mode as a baseline sitting above the truth, arriving from the
  # opposite side.
  #
  # m.core.site-builder stays a gap DELIBERATELY, and its capability note says why
  # at length. Short version: every other capability boarded that day had either a
  # real dependency edge on an existing row, so `blocked` computed honestly, or a
  # written decision that made its status honest. The site builder has neither — no
  # edge to express, so it would render `ready` and invite a claim on a product
  # nobody decided to build, and `socket` would assert a not-in-v1 decision that
  # does not exist. It needs an owner scope ruling. A gap honestly recorded says
  # more than a row that misrepresents what is decided, and this list is where that
  # stays visible.
  gaps:
    - m.core.site-builder
  # Tracker rows with no law basis and no written decision anywhere.
  # The audit's headline says 18 extra-legal rows; its own class breakdown
  # (1 orphan · 1 contradiction · 13 §13-extensions · 3 decided evolutions)
  # double-counts socket.social-recovery, which is both the contradiction and
  # one of the thirteen. The distinct count is 17, which is what tracker_extra
  # carries. Only the orphan class is ratcheted — a §13 extension with a
  # written reason is evolution; an orphan is nobody having decided.
  orphans:
    - trade.ccxt-api
