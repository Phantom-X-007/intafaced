# LANE STOP — L14 P2P · wave 8 topup · 2026-08-09

```
LANE: L14 P2P wave 8 topup
shipped: honesty residual (boot comment + README API/ops/health surface) · this Engine B re-falsify
in flight: none on wall (open p2p PRs = 0 at re-verify)
parked: p2p:moderate who (Class X) · method registry seed (product) · KMS instrument encrypt (Class X) · offer ceiling env magnitudes (product) · auto-ruling (law forbids) · rank fee discount / p2pLimitMultiplier apply (identity + product) · chat product · Stage-3 erase pseudonymise · floating mark wire (cross-service) · apps/admin dispute console (outside wall) · outbox (events plane)
Nitro must decide: none for code; optional later — moderate allowlist/scope mint, registry seed content, ceiling env numbers, KMS
SAFE TO CLOSE: yes
tip: 5ee00f24 (re-derive at merge)
```

## Unit cards (W8 deep falsify — no pad)

| Prio    | Unit                                 | Verdict             | Proof                                                                                                            |
| ------- | ------------------------------------ | ------------------- | ---------------------------------------------------------------------------------------------------------------- |
| A0      | Open p2p PR merge                    | **none open**       | claim-check clear; 0 p2p open PRs among 6 total                                                                  |
| A1      | created timeout re-drive             | **HOLDS**           | `timeoutActionFor('created')→settle_or_void`; unwind re-drives lock then refund/void                             |
| A1      | sweepSettlements                     | **HOLDS**           | decide-then-post; second sweep moves nothing; recipe keys                                                        |
| A1      | escrowIntegrity                      | **HOLDS**           | per-trade pots (#1283); equal-and-opposite drift flagged                                                         |
| A2      | fee service-only                     | **HOLDS**           | no feeBps on take wire; constructor/env only                                                                     |
| A2      | disputes allowlist                   | **HOLDS**           | empty → `p2p.moderation_unreachable`; natural-person rulings only                                                |
| A2      | instruments disclose                 | **HOLDS** mechanism | held-escrow only + atomic access log; free peek closed; KMS park                                                 |
| A2      | merchants badges→limits              | **PARTIAL** honest  | Stage 1–2 mechanism; env unset = unlimited (no invent magnitudes)                                                |
| A3      | state machine matrix                 | **HOLDS**           | `state.test.ts` graph + README matrix                                                                            |
| A3      | kill-switch                          | **HOLDS**           | blocks create/take/resume only; settlement continues                                                             |
| A3      | Engine B chapter                     | **this doc**        | below                                                                                                            |
| A3      | moderate who / KMS / chat / ceilings | **parked**          | Class X / product                                                                                                |
| Honesty | boot + API surface                   | **shipped**         | index.ts no longer claims disputed ends on a clock; README lists merchants + ops.lateSettlements + health fields |

## Sealed (do not re-open)

Decide-then-post · re-drive created (`settle_or_void`) · announce-before-stamp · escalate-and-hold disputes · instruments mechanism · XP business keys · fee not per-take · #1298 naming · #1283 integrity/fee/dust/pause/last_settle_error/IDOR.

## Engine B (W8 falsification on tip `5ee00f24`)

| Promise                                                                | Verdict                                                                 |
| ---------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| One thing this service is for (no strand; late OK)                     | **HOLDS**                                                               |
| Trade state machine                                                    | **HOLDS**                                                               |
| API (scopes · IDOR NOT_FOUND · fee wire closed · merchants/ops listed) | **HOLDS** after honesty ship                                            |
| Events (business keys; dispute.resolved automatic=false)               | **HOLDS**                                                               |
| Ledger recipes only; fee from escrow; no fee on refund                 | **HOLDS**                                                               |
| Can funds ever be stranded?                                            | **HOLDS** (late + durable last_settle_error; disputed holds on purpose) |
| DB constraints                                                         | **HOLDS**                                                               |
| Fiat = config                                                          | **HOLDS**                                                               |
| Kill-switch does not freeze settlement                                 | **HOLDS**                                                               |
| Merchants badges → limits                                              | **PARTIAL** (mechanism; policy open)                                    |
| Instruments under held escrow + log                                    | **HOLDS** mechanism; KMS residual named                                 |

## Engine C (attack)

| Surface                   | Note                                                                               |
| ------------------------- | ---------------------------------------------------------------------------------- |
| Stranded funds            | Closed — clocks + decide-then-post + sweeps; disputed = hold-until-human by design |
| Double settle             | Closed — recipe keys + terminal guards                                             |
| Fee invent                | Closed — service env only                                                          |
| Illegal state jump        | Closed — assertTransition + DB                                                     |
| Instrument free peek      | Closed mechanism; harvest residual named in README                                 |
| Empty moderator allowlist | Honest refuse — no soft open                                                       |
| Machine ruling            | Blocked — escalate + natural-person + DB trigger                                   |

## Soft (not craft residual)

- `escrowIntegrity` SELECT omits `created`+lock-posted window until status advances or ~2 min timeout — monitoring lag only; funds still re-driven.
- Kill-switch also blocks `resumeOffer` (stricter than “offers+takes” wording; correct under kill).

## Local re-verify (this stop)

- Pure suite subset (state · pricing · reputation · release-postable · moderation-auth · merchant-limits · env-retention): **66 passed** (full pure suite previously 131; some files need workspace package builds)
- Money suite: sealed on CI / #1283 path (not re-run Postgres in this env)
- Denon open PRs path-intersect `services/svc-p2p/**`: **none** (#1502 gates · #1494 support)
- Shehzad #1177: babysit only; not on wall

## Next agent

1. Do not invent moderate who / registry seed / KMS / ceilings / auto-ruling / rank multiplier apply.
2. Cross-service floating mark only via contracts/events PR first.
3. Babysit only if a new p2p PR appears on the wall.
4. Do not pad Engine A — residual-empty stop is the win.
