# Claim D26-P1-M1 — market.commerce Class M residual (refuse blank commission)

**Lane:** `denon-d26-p1-m1-commerce` / this session  
**Tracker:** `market.commerce` (already `done` C1+C2 #1189)  
**Branch:** `feat/d26-p1-m1-market-commerce`  
**Scope:** `services/svc-market/**` · compose `svc-market` ledger wire · board/tracker honesty  
**Do not touch:** `packages/ledger-client/src/recipes/market*` — open **#1761** (D26-P1-M2)

## Done bar

Product-complete after agent Class M residual clears; refuse blank commission.

## What this cook seals

C1+C2 money path and M2 lifecycle refuse doors are on tip. Agent Class M residual
holes that still invented a "working" marketplace under blank / miswired deploy:

1. Compose `svc-market` never wired `LEDGER_URL` → container defaulted to
   `localhost:4001` (dead on the compose network). When the owner sets a rate,
   purchase could not honestly post `marketPurchase` to `svc-ledger`.
2. Public-door mount proves blank createListing / empty catalogue / purchase
   map to `PRECONDITION_FAILED` without inventing success.

Explicit `0` remains an owner free-commission decision. Rate magnitude stays Nitro
(`MARKET_HOUSE_COMMISSION_BPS` unset in compose on purpose). C3 subscriptions /
ranking stay parked.

## Coord with #1761 (M2 ledger)

Listing/premium fee recipes + any named blank/NaN `marketPurchase` message
deepen land on **#1761** — this PR does not dual-edit `packages/ledger-client`.
Tip already refuses non-integer / out-of-range bps via `Number.isInteger`.

## Non-goals

- No invent commission bps / ranking / C3 subscription law.
- No second money book; no balances in market.
- Tracker mountain stays `done`; this is the Denon board Class M residual seal.

## Leverage

Phase A **IN**: extend `services/svc-market` + compose ledger URL. No second SPA /
second book / invented rates. Ledger recipe extend deferred to #1761.
