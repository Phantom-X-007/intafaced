# Mega audit paste — after Denon (or agent) ship waves without babysitting

**When:** main moved a lot after the last peace floor, and you cannot sit and re-prompt.  
**Not:** full A→E archaeology restart (that closed 2026-07-29).  
**Is:** complete-enough **delta mega-wave** — every new money/auth/deploy surface judged, P0 fixed, scoreboard honest.

**Floor files:** `docs/PEACE-OF-MIND-AUDIT-CURRENT.md` · `docs/WAVE-AUDIT.md` · `docs/POST-MERGE-RESIDUAL-AFTER-86.md`  
**Method depth:** `docs/FULL-AUDIT-PROGRAM-2026-07-29.md` · `docs/PLAN-META-AUDIT-FULL-AUDIT-PROGRAM-2026-07-29.md`

---

## PASTE FROM HERE (new chat only)

```
MEGA WAVE AUDIT — full autonomy (Nitro operator mode)

You have full autonomous control. I am non-technical, evening/AFK, tired of
re-prompting. Infer unspoken needs. Do not hand me git homework, technical
multiple-choice, or “should I run verify?”. Escalate ONLY on the whitelist.

═══════════════════════════════════════
WHAT “GOING ALL OUT” MEANS (this chat)
═══════════════════════════════════════
YES — complete risk coverage of EVERYTHING that landed since the last peace
baseline that can lose money or trust (money recipes, auth, edge/mounts,
deploy ports, tracker honesty, vendor residue on delta).
YES — L0 machine truth first; proof over narrative; fail closed on money/auth.
YES — maker-checker (fresh read-only critic) on every new P0/P1; prefer
cross-family critic when available; critic never implements.
YES — false-done / cheat-diff on any fix you ship (no assertion strip, empty
catch, type-suppression on money files).
YES — durable docs + claim-tags; update PEACE scoreboard; chat = verdict only.
YES — worktree only; open PRs for P0 fixes; run pnpm verify and report real
output.
YES — pre-audit OPEN Denon release PRs as well as merged tip (do not wait for
merge to start reading).
NO — full monorepo line archaeology of vendor media.
NO — rebuild services “properly” from scratch.
NO — Strix / live exploit frameworks without my explicit go + non-prod target.
NO — messaging Denon unless I asked.
NO — re-opening closed P0s from #80/#81/#86 unless you prove regression.

═══════════════════════════════════════
READ FIRST (order)
═══════════════════════════════════════
1. docs/PEACE-OF-MIND-AUDIT-CURRENT.md
2. docs/WAVE-AUDIT.md
3. docs/POST-MERGE-RESIDUAL-AFTER-86.md
4. AGENTS.md · AGENT_PROTOCOL.md · INTAFACED_DEFINITIVE_BUILD.md §0
5. Live: git fetch; origin/main SHA; gh pr list (open + recent merged)

═══════════════════════════════════════
FREEZE
═══════════════════════════════════════
- SINCE_SHA = tip recorded in PEACE (or last audited SHA — re-derive live).
  If PEACE is stale vs origin/main, SINCE_SHA = last fully audited merge
  (historically #86 = 60031cf until a mega-wave re-floors it).
- TIP_SHA = origin/main at T0.
- Also freeze OPEN money/release PRs (e.g. Denon release/*) as PRE-MERGE targets.
- All findings cite SHA or PR number. Never invent.

═══════════════════════════════════════
PHASES (run all; do not stop at inventory)
═══════════════════════════════════════
A · Ground truth
  - Worktree at origin/main (never dirty main checkout)
  - L0: pnpm scan:brand, scan:custody, format:check, tracker:check,
    CI status on tip + open release PRs, pnpm verify if env allows
  - Delta inventory: commits/PRs + every touched money/auth/deploy path named

B · Risk layers on DELTA ONLY (parallel; strong model for judgment)
  L1 Doctrine/money law on changed files
  L2 Auth & principal (publicProcedure, WebAuthn, ownership, unauth routes)
  L3 Money paths E2E (convert, stake/unstake, mint, governance, pay, AMM,
      hold claim-before-post, reverse atomicity)
  L4 Plane split (protocol/AMM non-custodial claims; dual-book vendor)
  L5 Deploy/edge/ports/Dockerfile/compose on delta
  L6 Mount honesty (router built + registered; edge route exists)
  L7 WS/terminal surfaces on delta
  L8 Tracker done honesty for touched features
  L9 Vendor residual only if delta touches vendor
  L10 Open PR pre-merge audit (Denon release branch) — same layers on that
      branch tip

  Finding shape: id, layer, system, title, severity P0|P1|P2|P3,
  evidence path/PR, doctrine_ref, repro, blast_radius, fix_shape, confidence

C · Adversarial
  - Fresh-context critic on every P0/P1 (read-only, assume broken)
  - Reject or confirm; fail closed if evidence thin

D · Fix sprint
  - P0 without waiting for me; one concern per PR when possible
  - P1 money with tests; park P2 with reason
  - Every fix: regression test + cheat-diff + verify evidence

E · Scoreboard
  - Update PEACE-OF-MIND tip SHA + scoreboard + residual
  - Write docs/audit/YYYY-MM-DD-wave/ (00-BASELINE, 01-DELTA, 02-FINDINGS,
    03-ADVERSARIAL, 04-L0)
  - Link from START-HERE only if entry needs a new pointer (one home per fact)
  - Enhance WAVE-AUDIT.md if the standing loop learned something durable

═══════════════════════════════════════
UNSPOKEN NEEDS (enforce silent)
═══════════════════════════════════════
- Control without literacy → one peace scoreboard
- Not the bottleneck → auto-decide technical forks; safer doctrine default
- Fear of silent money loss → fail closed; proof tests
- Fear of theater audit → machine L0 + critic + cheat-diff
- Survive compact → durable docs, claim-tags
- Partnership → do not stomp Denon open branches; pre-audit then fix on
  Nitro branches if needed
- AFK → open PRs, update board, leave next move obvious without a debrief

═══════════════════════════════════════
ESCALATION WHITELIST ONLY
═══════════════════════════════════════
- Vendor becomes product money books (policy flip)
- Live custody / real-money go-live
- Jurisdiction beyond doctrine
- Real spend / production credentials
- Merge multi-asset / money enum without Denon when that rule still stands

═══════════════════════════════════════
DONE WHEN
═══════════════════════════════════════
1. TIP_SHA + SINCE_SHA recorded; open release PRs named
2. L0 stated with real command output (or honest env gap)
3. Every new money/auth surface in the delta judged (name the set)
4. Each P0 fixed or blocked with written reason + PR link
5. Adversarial note per P0/P1
6. PEACE updated so I open one file and know: keep building / not go-live /
   what is still open
7. Chat reply: verdict first, PR links, residual only — no process theater

First actions:
1. git fetch; freeze SHAs; worktree
2. L0 machine + delta inventory
3. Parallel layers B; pre-merge open Denon release
4. Critic C; fix D; scoreboard E
```

## END PASTE
