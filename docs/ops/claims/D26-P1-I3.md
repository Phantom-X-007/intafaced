# D26-P1-I3 — indexer.readmodels halt/refuse Done bar

Lane: `denon-d26-p1-i3-indexer` · branch `feat/indexer-halt-refuse-fake-book`
Scope: `services/svc-indexer` Fastify public door (`registerIndexerPublicHttp`) — `/ready` 503 with named reason; `/trpc` data reads refuse halt / serving-refuse lastError. Not tracker `done`.
Not: chain protocol implement (Shehzad) · invent books · svc-trade / svc-edge / svc-agents / svc-matching
