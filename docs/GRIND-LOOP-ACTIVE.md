# GRIND LOOP — ACTIVE (compaction-safe)

**Status:** RUNNING · Nitro AFK · agents + 45m scheduler own the loop  
**Scheduler:** every 45 minutes re-reads this file and ships NEXT QUEUE  
**Last tip:** re-check `git rev-parse origin/main` (past account/balance REST)

---

## How to survive compaction / new chat

1. Read **this file** on `origin/main` first.
2. Read `docs/GRIND-PLAN-2026-07-30.md`.
3. `git fetch origin main && git log --oneline origin/main -20`
4. `gh pr list --state open`
5. Continue **NEXT QUEUE** — never re-ship MERGED.
6. Every 3–4 product PRs: local brand/custody/vendor-shell/workspace/tracker wave.
7. Update this file after each batch.

---

## Enhanced AFK prompt (paste)

```
AFK GRIND LOOP — full autonomous (Nitro).

READ: docs/GRIND-LOOP-ACTIVE.md then docs/GRIND-PLAN-2026-07-30.md then live git/gh.
RULES: worktree only; parallel independent mountains; light gates every PR; wave audit every 3–4 PRs; CI may be billing-blocked → local green + admin merge; update GRIND-LOOP-ACTIVE before you stop; front-run Denon without fake done.
UNSPOKEN: volume + quality + bots can trade + notifications + peace of mind scoreboard.
GO from NEXT QUEUE #1.
```

---

## MERGED (do not redo) — high water

#110–#143 area including:
- Money surfaces: yield/buyback, convert (code), stake/gov earlier
- Auth: webauthn, apikeys exchange, edge ifc_
- Trade: private orders/fills WS, history, cancelAll, fills.forOrder
- Pay: payment links + list/deactivate + **hosted checkout HTML**
- Notify: svc-notify inbox + i18n + edge
- Protocol: factory honesty (still not done)
- **CCXT REST:** public markets/orderbook/ticker/trades/tickers; private open/create/cancel/get/closed/trades/cancel-all/fees/**balance**
- Docs: grind plan, loop, wave audits

---

## NEXT QUEUE (agent)

1. **OHLCV public REST** — only if candle source exists; else document socket
2. **trade.convert** tracker honesty (tests exist; CI billing blocks green-CI claim)
3. **positions** — futures only; spot = N/A note on tracker
4. Reassess **DRAINED** when only human blockers remain

## Human-only

- GitHub Actions billing
- Real chain factory + RPC for smart-accounts done
- Licences, wallet secrets, counsel list, kill drill, multi-asset

---

## Exit (DRAINED)

Agent queue empty or each item human-blocked · doctrine green · Status line DRAINED · scoreboard updated.

**Next agent after compact: NEXT QUEUE #1 (OHLCV).**
