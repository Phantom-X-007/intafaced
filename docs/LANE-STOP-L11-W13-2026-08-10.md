# LANE STOP — L11 IDENTITY PII · wave 13 · 2026-08-10

```
LANE: L11 wave 13 product-velocity ~2x substance
shipped: #1639 KYC vault blocks free cross-user document reads · #1649 vault records which operator stored the file
in flight: none
parked: production index.ts wire of bootKycVault (Denon #1626 dual-write on index/auth-service) · KYC vendor webhook Class X · pay:* grants never invent · L04 affiliates dual-write (#1636 sibling)
Nitro must decide: which verification vendor (Class X) — none else
SAFE TO CLOSE: no — one agent residual remains after Denon #1626 clears (spread bootKycVault into createIdentityRouter options)
tip: d047d147
```

## Wall

`services/svc-identity/**` · mountain `identity.pii-isolation` · path-intersect L04 affiliates (left free — L04 open)

## Product Done bars this wave

| Unit                    | Done bar                                                               | Proof                      |
| ----------------------- | ---------------------------------------------------------------------- | -------------------------- |
| A0 path-intersect Denon | no dual-write auth-service / index / identity.test                     | claim-check + PR path list |
| A1 free cross-user PII  | `get(id)` removed; `getFor` owner\|compliance; wrong-owner = not_found | #1639 · 89→92 tests        |
| A1 operator surface     | storeDocument / listDocuments / bindDocument meta only, never bytes    | #1639                      |
| A1 provider_ref bind    | same-user document only; pending only                                  | #1639 provider-ref-bind    |
| A1 monorepo gate        | no foreign service touches kyc_documents                               | pii-isolation.gate.test    |
| A1 operator audit       | `stored_by` on vault rows                                              | #1649 · mig 0014           |
| A3 mountain-event       | tracker wip @ZenYoda3                                                  | #1639 features.mjs         |
| A3 Class X              | parked                                                                 | vendor not chosen          |

## Engine B — promise pass

| Claim                                 | Verdict                                                                    |
| ------------------------------------- | -------------------------------------------------------------------------- |
| Separate encrypted store              | Holds (#1348 + this wave)                                                  |
| Services get flags never documents    | Holds — kyc.status still strips providerRef; no bytes on wire              |
| No free cross-user PII read           | **Fixed** this wave (was free get-by-id)                                   |
| Production vault procedures reachable | **Residual** — procedures refuse-closed until index spreads `bootKycVault` |
| Live vendor                           | Class X park                                                               |

## Pickup after Denon #1626

```ts
const vault = bootKycVault(sql, env.IDENTITY_KYC_DOC_KEY);
export const appRouter = createIdentityRouter(auth, rank, {
  // …
  ...(vault ?? {}),
});
```

Helper already on main: `services/svc-identity/src/kyc/boot-vault.ts`.

## Nitro-only

KYC verification vendor · pay grants — **none decided this wave**.
