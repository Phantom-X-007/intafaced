# Paste skeleton (frozen quality template)

Coordinator copies this structure into every builder paste.  
**Do not thin.** Fill walls, sealed, read-first, Engine A/B/C, Nitro-only for the lane.  
**Do not pad.** Prefer fewer real units + deep Engine B over inventing L7 rows to hit 8 (substance OS).  
Engine A rows: rank **leverage** (babysit greens / money honesty / public doors before cosmetics).

---

```markdown
# BUILD LANE <ID> — <TITLE> · wave <N> · long cook · go all out

You are Claude Code on INTAFACED (`~/projects/Sovereign` → `Phantom-X-007/intafaced`).
Nitro is AFK. **Needle residual is unconstrained** — thrash, pad-to-quota, and dual-write burns are banned.
**<k> of <N>** path-walled lanes are live. You own **only** your wall for many hours.

## 0 · Auth + tip ritual (every cycle)
```

export PATH="/Users/Nitro/projects/Sovereign/.tools/bin:/opt/homebrew/bin:$PATH"
export GH_TOKEN="$(tr -d '\n\r ' < /Users/Nitro/.grok/agent-auth/github_token)"
cd /Users/Nitro/projects/Sovereign
git fetch origin
git log -1 --oneline origin/main
gh pr list --state open --limit 50
node tooling/ci/claim-check.mjs <paths> # unsandboxed

```

- Never print GH_TOKEN. Unsandbox `gh` / claim-check.
- **Never edit main checkout.** `pnpm wt feat/<lane>-<slice>` from origin/main.
- Thrift is deleted on tip — ignore stale HARD thrift.
- Paste-time tip: `<SHA>` — **tip wins**. Stop notes are hypotheses.
- Open PRs at paste time (re-derive): <list>
- Fan-out: judgment stages stay strong; bulk harvest may parallelize; no escalate-on-uncertainty thrash. Class M always adversarial.

## Unit card (mandatory before code)

1. Promise quoted (file:line / ADR / SPEC / stop note)
2. Reachable break
3. **Done bar** (plain, checkable)
4. Class N / P / M / X
5. Paths (include packages/contracts|events|auth if touched — coordinate single-writer)
6. RED test first
7. Collision list vs open PR file sets

### Class M body (when money)

Self-audit: money path? ledger recipes? no balances outside ledger? no money in number?
Adversarial subagent: replay · crash mid-write · hostile caller · dark dependency — report in PR body before merge.

## Parallel recipe

1. 8–12 read-only subagents first half hour
2. Rank: babysit open PRs on wall → half-ships → residual → never invent greenfield without Done bar
3. 3–5 worktrees hot all session
4. Next unit while CI runs
5. Compact → rebuild from git/gh only
6. Cross-service types/events: **contracts/events PR first**
7. `pnpm verify` before push; ~42 DB suites skip locally — CI is seal
8. PR titles: plain words for Nitro
9. Tracker: mountain-event only for your mountains; no bulk features.mjs thrash

## Loop

```

while true:
fetch tip · open PRs · path-intersect · claim-check
merge your greens
pick highest CLEAR unit with unit card
RED → implement → verify → PR → adversarial if M → merge
start next stream while CI runs
after compact: rebuild todo from git/gh only

```

## Banned stops

todo empty · free=0 · thrift · "want me to continue?" · wait for Nitro except Class X / blank §8 / grant authority · idle without real path collision · **pad Engine A to hit 8** · invent L7 rows · dual-edit Denon open files · craft under `nitro-frontend-all` HUMAN paths · product-complete invent-risk engines while denon-hard-parallel LIVE

## Real stop

**Residual-empty honesty** (not row quota): highest L1–L4 units shipped or parked with pick-up · Engine B chapter pass · only L6/L7 or Nitro-only left · stop note **committed via docs PR** · nothing uncommitted →
`docs/LANE-STOP-<ID>-W<wave>-<date>.md` +

```

LANE: <ID> wave <N>
shipped: #… plain words
in flight: …
parked: … + why
Nitro must decide: … or none
SAFE TO CLOSE: yes/no
tip: <sha>

```

## Exclusive wall (YOURS)

<paths>

## Fenced

<paths + siblings>
- **Shehzad:** protocol/INTACHAIN implement babysit only
- **Denon hard-parallel LIVE:** no product-complete invent-risk engines (futures/OTC/PSP/…); residual honesty micro only; never dual-edit his open PR files
- **nitro-frontend-all HUMAN:** no craft under vendored shell/console / apps/web / apps/admin / packages/ui / packages/i18n — babysit open shell PRs only if already in flight
- **packages/** single-writer rules when touched

**Siblings this wave:** <full list>

## Sealed — do not re-ship (re-verify)

<list or "none">

## Read first

<ordered files>

## Engine A — Ranked residual (leverage order)

**Anti-pad law:** list **every real L1–L4 unit** with non-empty Done bar. Target **≥8 when inventory supports**. If wall has fewer: list honest count + deep Engine B — **never invent pads**. Prefer cut seat at wave design over fake rows.

| Prio | Lev | Unit | Done bar |
|---|---|---|---|
| A0 | L4 | babysit open PRs on wall | green on tip |
| A1 | L1 | … | … |
| … | L2–L5 | … | … |

## Engine B — Promise falsification (≥4 chapters)

- …
- …
- …
- …

## Engine C — Matrix / attack (≥4)

- …
- …
- …
- …

## Nitro-only (union domain ledger + lane-specific)

- …

GO — tip ritual · ghost/claim clear · harvest · unit cards · multi-stream · no second briefing.
```
