# Audit — uiproof PR #169 + #172 (code-only vs GO packet)

**Date:** 2026-07-30  
**Auditor role:** Stream A planner/adversarial (planner agent) — **not** the executor who wrote the code  
**Worktree:** `.worktrees/feat-uiproof-proof-green`  
**Commits under review (on main):**

- `#169` · `e8c1ffa` · `feat(uiproof): PR-1 Stream A boot.mjs`
- `#172` · `d3874a5` · `feat(uiproof): PR-2 harness + design bar`

**Law scored against (read this turn):**

1. `docs/FRONTEND-OPERATING-PLAN-2026-07-30.md` — §2.4 seven boot behaviours, §2.5 five assertions, §2.6 routes, GO packet A1–A6 / B1–B6
2. `docs/FRONTEND-OPERATING-PLAN-GROK-AUDIT-2026-07-30.md`
3. `docs/STREAM-A-DESIGN-BAR.md`
4. `docs/RUNNING-STREAM-A.md`
5. `tooling/uiproof/*` on disk
6. root `package.json` scripts/deps for `ui:boot` / `ui:proof` only

**Method:** read-only code audit. **No** production shell UI edits. **No** long-lived servers started this turn. Live acceptance rows that require a machine run are marked **UNKNOWN**, not inventively green.

**Companion status already on main:** `docs/AFK-COOK-SCOREBOARD-2026-07-30.md` and `docs/GRIND-LOOP-ACTIVE.md` already record PROOF.md as **honest unverified** (Chromium SEGV in agent sandbox). This audit does not override that; it scores _code vs GO packet_ separately from _live PROOF_.

---

## 1 · Verdicts (code quality vs GO packet — not browser PROOF)

| PR                                   | Verdict           | One line                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------ | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **#169 (PR-1 boot)**                 | **ship**          | Implements all seven §2.4 behaviours plus a necessary Node-18 resolver; shape matches the GO packet; residual risks are operational (reuse probes only `/`, pidfile not port-keyed), not missing core design.                                                                                                                                  |
| **#172 (PR-2 harness + design bar)** | **ship-with-fix** | Right stack (Playwright outside vendor, matrix, five assertion classes, chromium-only, no `webServer`). **Not** ready to claim “B green”: B3 is softened vs plan, `report.mjs` can declare PASS without a Playwright result, B4 canary has no evidence of ever going red, live PROOF still unverified, network allowlist is path-in-text only. |

**Do not claim Stream A visual “done.”** Per §4.2 and the Grok audit: without a machine-produced green `PROOF.md` **and** a demonstrated red canary (B4), the functional gate is incomplete.

---

## 2 · Acceptance matrix

### 2.1 · A1–A6 (PR-1 / #169)

| #      | GO packet expectation                                                        | Code verdict                               | Evidence                                                                                                                                                                                                                                                                |
| ------ | ---------------------------------------------------------------------------- | ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **A1** | Fresh worktree, no `node_modules` → exit **1**, message names dir + `npm ci` | **PASS** (code)                            | `tooling/uiproof/boot.mjs` L192–198: `existsSync(join(shellDir, 'node_modules'))` else `fail(...)` with `cd "${shellDir}" && PATH=... npm ci`.                                                                                                                          |
| **A2** | After `npm ci`, `pnpm ui:boot` → exit **0** ≤240s; `/app.js` 200             | **PASS** (code) / **UNKNOWN** (live)       | Two-stage ready: `waitReady()` requires `GET /` **and** `GET /app.js` both 200 (`boot.mjs` L164–174, L239–247). Timeout 240s + last 40 log lines (L44–45, L241–244). Live boot not re-run this audit.                                                                   |
| **A3** | Immediate second `pnpm ui:boot` → exit **0** &lt;3s, prints `reusing`        | **PASS** (code) / **UNKNOWN** (live)       | L177–182: if `httpStatus('/') === 200` → log `reusing existing server (pid …)` and `process.exit(0)`. **Gap:** reuse does **not** re-check `/app.js` (plan §2.4 item 1 literally only probes `/` — compliant with letter, weak on half-dead servers).                   |
| **A4** | `time pnpm ui:boot` **returns** (no hang)                                    | **PASS** (code) / **UNKNOWN** (live)       | Detached spawn + `child.unref()` (L211–237); boot process only polls readiness then exits — does not await the child lifecycle. Direct fix for §1.5.                                                                                                                    |
| **A5** | `PORT=8091 pnpm ui:boot` boots 8091, leaves 8090 alone                       | **PASS** (code) / **UNKNOWN** (live)       | `PORT = Number(process.env.PORT \|\| 8090)` (L41); `BASE` and spawn `env.PORT` use it. **Residual:** single pidfile/log path (`.artifacts/uiproof/devserver.pid`) is **not** port-keyed — dual-port runs clobber metadata (does not by itself kill the other listener). |
| **A6** | `pnpm verify` unchanged (not gated on UI)                                    | **PASS** (code) / **UNKNOWN** (live paste) | Root `package.json` `"verify"` is still `turbo run build typecheck test && pnpm scan:workspace && node tooling/ci/dod-gate.mjs` — no `ui:proof`. `ui:boot` / `ui:proof` are extra scripts only (L45–46).                                                                |

**§2.4 seven behaviours — code checklist**

| #   | Behaviour                                       | Present? | Where                                                                     |
| --- | ----------------------------------------------- | -------- | ------------------------------------------------------------------------- |
| 1   | Reuse before spawn                              | **Yes**  | `boot.mjs` L177–182                                                       |
| 2   | Detached spawn + unref, stdio → log             | **Yes**  | L205–237 (`detached: true`, `stdio: ['ignore', outFd, outFd]`, `unref()`) |
| 3   | cwd = repo + `vendor/*/05_Web_Front`            | **Yes**  | `resolveShellDir()` L60–74; spawn `cwd: shellDir` L212                    |
| 4   | Missing `node_modules` → exit 1 + exact command | **Yes**  | L192–198                                                                  |
| 5   | Ready = `/` 200 **and** `/app.js` 200, poll ~2s | **Yes**  | L44–45, L164–174                                                          |
| 6   | 240s timeout → exit 1 + last 40 log lines       | **Yes**  | L241–244, `tailLog(40)`                                                   |
| 7   | Pidfile + log under `.artifacts/uiproof/`       | **Yes**  | L47–49, L203, L233–234                                                    |

**Necessary adaptation (not in original GO text, correctly documented):** Node 18 resolver (`STREAM_A_NODE` / `.tools/node18` / sibling path) — webpack 3 dies on Node 20+. Documented in `docs/RUNNING-STREAM-A.md`. Hardcoded sibling worktree path is a convenience, not a contract.

**PR-1 file set vs GO packet**

| Expected                             | On disk                                                                         |
| ------------------------------------ | ------------------------------------------------------------------------------- |
| `tooling/uiproof/boot.mjs`           | Yes                                                                             |
| `docs/RUNNING-STREAM-A.md`           | Yes (expanded with Node 18 + proof notes — good)                                |
| `package.json` `ui:boot` only (PR-1) | `ui:boot` present; `ui:proof` + `@playwright/test` arrived with PR-2 as planned |
| `.gitignore` `.artifacts/`           | Yes (line 44); also `.tools/` (line 47)                                         |

---

### 2.2 · B1–B6 (PR-2 / #172)

| #      | GO packet expectation                                                                              | Code verdict                                           | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------ | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **B1** | `pnpm ui:proof` clean → exit **0**; every `PROOF.md` row PASS                                      | **UNKNOWN** (live) / **conditional PASS** (code shape) | Script chain: `boot.mjs && playwright test -c tooling/uiproof/playwright.config.mjs && node tooling/uiproof/report.mjs` (`package.json` L46). Report writes `.artifacts/uiproof/PROOF.md` and exits 1 if any cell not PASS (`report.mjs` L140). **Live PROOF not present** in this worktree (`.artifacts/` absent). Repo status docs: **honest unverified** (sandbox Chromium SEGV).                                                                                                                                                                                                                                                                                                         |
| **B2** | `ls .artifacts/uiproof/*.png` → exactly **10** files                                               | **FAIL** vs literal GO path / **PASS** vs intent       | Spec takes shots to `.artifacts/uiproof/shots/${routeId}__${viewport}.png` (`proof.spec.mjs` L24–26, L126–128; `matrix.mjs` `shotName`). Matrix = 5 routes × 2 viewports = **10** names. Literal `ls .artifacts/uiproof/*.png` would see **0** (subdir). Intent is correct; acceptance command in the plan is stale relative to implementation.                                                                                                                                                                                                                                                                                                                                              |
| **B3** | `/uc/account` asserts **redirect to `/login`**, not account UI                                     | **FAIL** vs letter / **WEAK PASS** vs spirit           | Plan §2.6: “asserts the redirect to `/login`”. Code (`proof.spec.mjs` L95–107) accepts **`onLogin \|\| loginFormVisible`** and comments that without backends MemberCenter may still mount. That is a deliberate softening: a page can stay on `/uc/account` URL with a password field and still pass. **Not** the GO packet assertion as written.                                                                                                                                                                                                                                                                                                                                           |
| **B4** | Negative control: inject `throw new Error('uiproof canary')`, expect non-zero + route FAIL; revert | **FAIL as proven gate** / **PASS as mechanism**        | Mechanism: `page.on('pageerror')` → `expect(pageErrors).toEqual([])` (`proof.spec.mjs` L51–53, L110–111). A thrown error in a mounted component **should** fail. **No artefact, PR body paste, or in-repo record of B4 ever going red** was found this audit. Grok audit §6 item 4: “Canary (B4) is sacred.” Plan: “A gate that has never gone red is not a gate.” → **B4 not satisfied as an acceptance row.**                                                                                                                                                                                                                                                                              |
| **B5** | Fleet **down** → still exit 0; network allowlisted, non-network not                                | **UNKNOWN** (live) / **RISK** (code)                   | Allowlist: `NETWORK_ALLOW_PREFIXES` = `/uc`, `/market`, `/exchange`, `/otc`, `/api` (`matrix.mjs` L44). Console filter requires “looks network” **and** prefix substring in **message text** only (`proof.spec.mjs` L28–40). Chromium often emits `Failed to load resource: net::ERR_CONNECTION_REFUSED` **without** the URL in `msg.text()` — those would **not** match prefixes → false FAIL risk under true backends-down. Conversely, any message containing both `fetch` and e.g. `/api` is allowlisted even if not a pure transport failure → false PASS risk. `pageerror` path has **no** network allowlist (correct for canaries; may flake on unhandled rejection from failed XHR). |
| **B6** | `pnpm verify` unchanged                                                                            | **PASS** (code) / **UNKNOWN** (live paste)             | Same as A6 — `ui:proof` not wired into `verify`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

**§2.5 five assertions — code checklist**

| #   | Assertion                                    | Present?                       | Notes                                                                                                                                                                                                                                                                                                                                                   |
| --- | -------------------------------------------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | No uncaught page errors                      | **Yes**                        | `pageerror` collector + strict empty expect                                                                                                                                                                                                                                                                                                             |
| 2   | No console errors except allowlisted network | **Yes, weak allowlist**        | See B5                                                                                                                                                                                                                                                                                                                                                  |
| 3   | Vue mounted (`#app` has children)            | **Yes**                        | L85–90                                                                                                                                                                                                                                                                                                                                                  |
| 4   | Brand honesty at runtime                     | **Yes, partial vs brand-scan** | `FORBIDDEN_DOM` via split `ban()` to avoid static self-hit (`matrix.mjs` L51–66). Mirrors most of `tooling/ci/brand-scan.mjs` FORBIDDEN; **missing** runtime checks for rail-partner short codes and upstream qq.com contacts (lower priority for SPA DOM text). Scans `title + body.innerText` only — not attributes/scripts (intentional, commented). |
| 5   | Full-page screenshot, deterministic name     | **Yes**                        | Under `shots/`, not artefact root                                                                                                                                                                                                                                                                                                                       |

**§2.6 route set**

| Route                             | In matrix?                                                         |
| --------------------------------- | ------------------------------------------------------------------ |
| `/`                               | Yes · `index`                                                      |
| `/exchange/btc_usdt`              | Yes · `exchange-btc-usdt`                                          |
| `/dex`                            | Yes · `dex`                                                        |
| `/login`                          | Yes · `login`                                                      |
| `/uc/account` + login-gate intent | Yes · `uc-account` + `expectLoginRedirect: true` (softened assert) |

Viewports: desktop 1440×900, mobile 390×844 — **match plan**.

**Playwright config vs plan**

| Rule                                  | Status                                                        |
| ------------------------------------- | ------------------------------------------------------------- |
| chromium only                         | **Yes** (`playwright.config.mjs` L50–51)                      |
| **no** `webServer` (boot owns boot)   | **Yes** — not configured                                      |
| `outputDir` → `.artifacts/uiproof/`   | **Yes** — `test-results` + JSON report under artefacts        |
| `@playwright/test` root devDependency | **Yes** `^1.62.0`                                             |
| Prefer local browsers path            | **Yes** — `PLAYWRIGHT_BROWSERS_PATH` → `.tools/ms-playwright` |
| Not in `pnpm verify`                  | **Yes**                                                       |

**Pass 2.5 design bar:** `docs/STREAM-A-DESIGN-BAR.md` is present and matches Grok-audit intent (one kit iView, #86 brand, tokens later, pattern checklists). Shipping it **with** PR-2 is slightly early vs “after PR-2” ordering, but content is sound and does not break the GO packet.

---

## 3 · Gaps vs plan

### Critical / gate-breaking for “B green”

1. **No live PROOF.md in this worktree; mainline status is honest unverified**  
   Sandbox Chromium SEGV is documented. Code cannot substitute for a desktop Terminal run of `pnpm ui:proof`.

2. **B4 canary never proven**  
   Mechanism exists; acceptance requires a red run then revert. Without that artefact, the gate is unfalsified.

3. **`report.mjs` false-PASS / stale-PROOF paths** (`report.mjs` L77–86, L140)
   - If Playwright JSON is missing but PNG shots exist → cell **PASS** with detail `shot present (no playwright json)`.
   - `ui:proof` uses `&&`, so a failed Playwright run never refreshes PROOF.md — **stale green PROOF.md can remain on disk** from a prior success while the latest command exited non-zero.
   - Agents (or Nitro) reading an old PROOF.md can believe a lie. Plan §1.2 / §4.2 demand a falsifiable current artefact.

### Important (fix before trusting the gate in CI or polish claims)

4. **B3 softened** — redirect-or-login-form instead of redirect-only. Document as intentional backends-down fixture **or** restore strict URL assert and accept red until auth fixture / guard behaviour is stable.

5. **B2 path mismatch** — plan’s `ls .artifacts/uiproof/*.png` vs implementation `shots/`. Update the operating plan acceptance line **or** also emit root-level copies. Prefer keeping `shots/` and fixing the plan/docs.

6. **Network allowlist is message-text-only** — does not use `consoleMessage.location().url` or Playwright `requestfailed`. High flake risk for B5; also over-broad `includes('fetch')` once a prefix substring appears.

7. **Reuse path skips `/app.js`** — letter-compliant with §2.4#1; can mark boot green against a server that serves HTML but not the bundle. Prefer: on reuse, require both statuses (small, high-value tighten).

8. **Pidfile/log not port-scoped** — A5 multi-port metadata collision.

9. **Runtime brand list ≠ full brand-scan** — missing rail-partner short codes, contact emails; weaker word boundaries on some assembled regexes (`engine-vendor` without `\b`). Split `ban()` correctly avoids **static self-hit** on `matrix.mjs` (good). brand-scan still scans `docs/*.md` and `tooling/uiproof/*.mjs` for literal names — split construction is the right pattern; keep it.

10. **Hardcoded sibling Node 18 path** in `boot.mjs` L85 — convenience only; fails closed via `resolveShellNode` error message if missing.

### Explicitly out of scope (correctly not done)

- Auth fixture / real `/uc/*` empty-vs-error (Pass 3)
- Pixel baselines (Pass 4)
- Prices / #109 (Pass 5)
- axe-core (Phase 1.5)
- Shell UI polish against design bar (Phase 2)
- Wiring `ui:proof` into `pnpm verify` (deliberately forbidden by plan)

### Brand-scan self-hit risk (summary)

| Risk                                      | Assessment                                                                                                           |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `matrix.mjs` listing forbidden literals   | **Mitigated** — `ban('gmas','ter')` etc.                                                                             |
| `proof.spec.mjs` / config                 | **Clean** — no banned literals                                                                                       |
| Design bar / RUNNING / plan docs          | May name process tools; brand-scan allowlist is path-specific — not re-run this audit; flag only if CI brand job red |
| Runtime DOM check weaker than static scan | **Accepted residual** until lists are shared from one source                                                         |

### Sandbox Chromium

`RUNNING-STREAM-A.md` correctly states: if Chromium SEGV/EPERM in a sandboxed agent, re-run in a normal Terminal — harness is fine; sandbox is not. That is the right diagnosis for current PROOF unverified status.

---

## 4 · Must-fix before claiming B green

Ordered. Do not skip.

| Step  | Action                                                                                                                                                                                                                                                                                  | Owner              |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| **1** | On a **non-sandboxed** machine Terminal: ensure Node 18 + shell `npm ci` + `playwright install chromium` per `RUNNING-STREAM-A.md`, then `pnpm ui:proof`                                                                                                                                | Executor           |
| **2** | Attach **current** `.artifacts/uiproof/PROOF.md` (all rows PASS) + confirm **10** PNGs under `shots/`                                                                                                                                                                                   | Executor           |
| **3** | **B4 canary:** inject `throw new Error('uiproof canary')` in a mounted component → `pnpm ui:proof` **non-zero** and that route FAIL in report → revert → green again. Paste red output                                                                                                  | Executor           |
| **4** | **Fix `report.mjs`:** never PASS a cell without a matching Playwright result; if `playwright-report.json` missing → overall FAIL; optionally write PROOF with FAIL when Playwright exits non-zero (e.g. always run report via `playwright …; status=$?; node report; exit $status` or ` |                    | true` then report-driven exit) so PROOF cannot stay stale-green | Executor (small follow-up PR) |
| **5** | **Resolve B3:** either strict `url` matches `/login`, or amend operating plan §2.6 / B3 to “login surface under backends-down” with the stronger of (URL **or** dedicated login route markers), and drop MemberCenter-on-`/uc/account` as a pass                                        | Planner + executor |
| **6** | **Tighten allowlist:** include `msg.location()?.url` and/or `page.on('requestfailed')` classification by URL prefix; narrow bare `fetch` matching                                                                                                                                       | Executor           |
| **7** | (Recommended) Reuse path also requires `/app.js` 200; port-key pidfile/log                                                                                                                                                                                                              | Executor           |
| **8** | Re-run A1–A6 on a fresh worktree once and paste outputs into PR or this audit’s follow-up                                                                                                                                                                                               | Executor           |

Only after **1–3** (and ideally **4**) may anyone say “PR-2 acceptance is green.” Design-bar polish remains a separate track and must not be certified by this harness alone.

---

## 5 · Code-proven vs still needs live PROOF.md

| Claim                                                               | Status                                                        |
| ------------------------------------------------------------------- | ------------------------------------------------------------- |
| Boot implements §2.4 seven behaviours                               | **Code-proven**                                               |
| `ui:boot` / missing-deps / detached / two-stage ready               | **Code-proven**                                               |
| Node 18 requirement documented + resolved in boot                   | **Code-proven**                                               |
| Harness files, matrix routes/viewports, chromium-only, no webServer | **Code-proven**                                               |
| Five assertion _classes_ present in `proof.spec.mjs`                | **Code-proven**                                               |
| Design bar doc exists and matches Pass 2.5 intent                   | **Code-proven**                                               |
| `ui:proof` not polluting `pnpm verify`                              | **Code-proven**                                               |
| A1–A6 **live** exit codes / timings                                 | **Needs live run** (UNKNOWN this audit)                       |
| B1 full matrix green + PROOF.md                                     | **Needs live PROOF.md** — currently **unverified**            |
| B2 ten screenshots on disk                                          | **Needs live run** (path is `shots/`)                         |
| B3 true login redirect                                              | **Not code-proven as specified** (soft assert) + needs live   |
| B4 gate can go red                                                  | **Not proven** (mechanism only)                               |
| B5 backends-down green                                              | **Needs live run**; allowlist risk may require code fix first |
| B6 verify paste                                                     | **Needs live paste**                                          |
| “Stream A visual done” / §4.2 all six exit criteria                 | **Not claimable**                                             |

---

## 6 · Severity rollup (confidence ≥ 80 only)

### Critical

| Issue                                                                   | Conf. | Why                                                                |
| ----------------------------------------------------------------------- | ----- | ------------------------------------------------------------------ |
| Claiming B green without live PROOF + B4 red proof                      | 100   | Plan §4.2, B4 text, Grok §6.4, scoreboard already “unverified”     |
| `report.mjs` can PASS without Playwright JSON / leave stale green PROOF | 90    | `report.mjs` L83–86; `&&` chain skips report on Playwright failure |

### Important

| Issue                                         | Conf. | Why                                             |
| --------------------------------------------- | ----- | ----------------------------------------------- |
| B3 not redirect-strict                        | 90    | Plan §2.6 / B3 vs `proof.spec.mjs` L95–107      |
| Network allowlist text-only → B5 flake / leak | 85    | `proof.spec.mjs` L28–40; Chromium message shape |
| B2 acceptance path vs `shots/`                | 85    | Plan B2 literal vs `SHOTS` dir                  |
| Reuse without `/app.js`                       | 80    | Half-ready server can fake A3                   |

### Not raised as defects (correct choices)

- Playwright outside vendor package
- Backends-down as Phase 1 fixture
- No pixel baselines yet
- Auth empty-vs-error marked unproven
- Design bar forbidding second design system
- `.artifacts/` gitignored

---

## 7 · Auditor sign-off

| Question                                      | Answer                                                                                               |
| --------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| Is #169 code quality acceptable vs GO packet? | **Yes — ship**                                                                                       |
| Is #172 code quality acceptable vs GO packet? | **Ship-with-fix** — merge-as-tooling OK; **B-green claim not OK**                                    |
| Did this audit run the browser gate?          | **No** (read-only; no long servers)                                                                  |
| Is PROOF.md green?                            | **Unverified** — no artefact here; prior status: sandbox SEGV                                        |
| Next human-visible action                     | Executor: desktop `pnpm ui:proof` + B4 canary + report.mjs harden; planner re-audit with PROOF paste |

---

_End of audit. Writer ≠ certifier: this document certifies code-vs-law only; live green remains the executor’s artefact to produce and a later audit’s to confirm._
