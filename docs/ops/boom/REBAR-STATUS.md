# FINISHED

Mill complete. Nitro has no step.

**Merged (this mill):**

- #4260 docs mill contract
- #4261 svc-ledger refuse non-recipe S2S posts (`fcd271333`)
- #4262 svc-trade planMirror loads leader fill (`cb6aebb44`)
- #4263 svc-bank require caller-stable ids on earn/schedule/repay/propose (`9ed41bc54`)

**Leftover software:** none.

**needs_validation (named, no writer, not your work):**

- HMAC fleet stays `accept-both` until an operator sets `INTERNAL_SERVICE_BODY_BIND=require` (compose pin SEALED; mill must not 401 the fleet)
- Trade combo ledger-holds vs matching combo (H4 matching LIVE; trade half not a new door this mill)

**SOCKET / FRONTEND / OWNER / KEEP / SKIP:** INTACHAIN P1, Vue/Codex, owner §8 lists, money-skip anvil, Q-tax map, Q-edge WS proxy, R-E8, Q-support refund, Q-blueprint, Q-rust.

**Watchdog:** deleted on this stamp.
