# LANE STOP — L15 NOTIFY · wave 7 topup · 2026-08-09

```
LANE: L15 wave 7 topup
shipped:
  · A0 open notify PR merge — none open on wall (claim-check clear)
  · W6 #1409 class seals re-verified RAN-IT on tip worktree
       multi-replica rates · reaper/stuck · fan-out kill · claim lease under
       ack_wait · credentials refuse · refusal matrix · event-wiring Class B
  · Engine B README chapter pass — all HOLD
in flight: none
parked:
  · live mark feed for alerts — prod boots dark on purpose until trade public
    mark (or owner-configured source) is injected into AlertService
  · 429 delivery not-before / bus thrash backoff — retryable honesty holds;
    pick-up if ops measures thrash (no invent without thrash)
  · gateway credentials Class X — docs/OWNER-ACTIONS-NOTIFY-GATEWAYS.md
  · non-EN translations of notify.alert.price.crossed.* — EN base shipped;
    only EN catalog exists OS-wide (packages/i18n)
Nitro must decide: gateway credentials Class X if out-of-app must go live
SAFE TO CLOSE: yes
tip: d40dff94
```

## Engine A — units this wave

| Unit                          | Done bar                                       | Status           | Proof                                                                                                                    |
| ----------------------------- | ---------------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------ |
| A0 open notify PR merge       | green or none                                  | **none open**    | `gh pr list` + claim-check clear on `services/svc-notify/**`                                                             |
| A1 multi-replica SMS          | no N× free verification codes under 2 pods     | **SEALED**       | `PostgresTargetRateLimiter` + `target-rate-limit.pg.test` concurrent last-slot; rate defaults pin #1381 class            |
| A1 reaper / stuck pending     | margin alerts not fake-delivered forever       | **SEALED**       | `reapExhausted` arm1 attempts / arm2 `delivery_stuck`; `channel-store.reap.test` + stuck-grace pin + reaper interval pin |
| A1 fan-out kill               | kill writes nothing and pretends not delivered | **SEALED**       | `fanout-off-pin` + `notify-service` early return; kill-switch critical still records `channel.disabled`                  |
| A2 claim lease under ack_wait | redelivery cannot double-send free SMS         | **SEALED**       | `claimLeaseMsFromGatewayTimeout` clamp ≤ ack_wait−5s; claim-lease pin + in-flight tests                                  |
| A2 429 thrash                 | retryable honesty; implement or explicit park  | **PARK**         | 429 → retryable (`gateway.ts`); bounded by max attempts pin; no not-before without thrash evidence                       |
| A2 channel credentials refuse | missing gateway → typed refuse, zero send      | **SEALED**       | `UnconfiguredChannel` + gateway-wire + boot-refusal tests                                                                |
| A2 refusal-code matrix        | every written refusal string on matrix         | **SEALED**       | README matrix pin + refusal-write-catalog pin                                                                            |
| A3 non-EN alert i18n          | or explicit park                               | **PARK**         | EN keys present; OS has single EN catalog                                                                                |
| A3 event-wiring Class B       | list cannot grow orphans                       | **SEALED**       | matrix = 10 durables; boot + pin test; README Events table                                                               |
| A3 Engine B chapter pass      | stop note                                      | **this file**    | below                                                                                                                    |
| A3 gateway credentials live   | park Class X                                   | **PARK Class X** | owner action doc only                                                                                                    |

**Anti-pad:** no new craft shipped to hit a row quota. W6 already closed the wall SAFE; this wave re-verified seals and confirmed residual-empty honesty.

## Engine B — chapter pass (README → code on tip)

| Chapter                            | Verdict                                                                                        |
| ---------------------------------- | ---------------------------------------------------------------------------------------------- |
| The one thing (attempt ≠ accepted) | **hold** — CHECK + settle discipline; no `delivered` overclaim                                 |
| Channels                           | **hold** — four adapters; unconfigured refuse; required fatal in staging/prod                  |
| Addresses                          | **hold** — own targets; verify-through-channel; shared rate windows                            |
| API                                | **hold** — full tRPC surface; self-only; deliveries user-facing                                |
| Events                             | **hold** — 10 consumers match matrix + README; nak only retryable; critical records no-target  |
| Kill-switches                      | **hold** — fanout no-write; OOA off writes `channel.disabled` (incl. critical with no address) |
| Environment                        | **hold** — schema + blank-as-absent + required channels                                        |
| §13 sockets                        | **hold** — adapters real; waiting on owner credentials only (Class X)                          |

No new §8 invented. Digest stays unwired.

## Engine C — attack surface

| Surface          | State                                                              |
| ---------------- | ------------------------------------------------------------------ |
| Replica free SMS | **mitigated** — FOR UPDATE rate windows + concurrent race test     |
| Fake delivered   | **mitigated** — accepted only on transport receipt + DB CHECK      |
| Kill lie         | **mitigated** — fanout off writes nothing; OOA off refuses by name |
| Orphan events    | **mitigated** — Class B growth pin + pending consumer report       |
| Dark gateway     | **mitigated** — typed refuse; zero send                            |
| 429 thrash       | **parked** — retryable + attempt budget; no not-before             |

## RAN-IT re-verify (this wave)

On worktree `feat/w7-l15-notify-residual` at tip:

```
pnpm exec turbo run build --filter=@intafaced/svc-notify...
cd services/svc-notify && pnpm exec vitest run
→ Test Files  35 passed | 4 skipped
→ Tests       269 passed | 46 skipped  (PG suites skip without TEST DB)
```

Denon open PRs path-intersect: **none** on `services/svc-notify/**` (docs/venue/vendor only).

## Parked pick-up (unchanged from W6)

1. Inject real `MarkSource` (trade public mark) — product pick of source.
2. Owner gateway URL/token (Class X) — [`docs/OWNER-ACTIONS-NOTIFY-GATEWAYS.md`](./OWNER-ACTIONS-NOTIFY-GATEWAYS.md).
3. Optional 429 not-before if gateway thrash appears in ops.
4. Non-EN alert key translations when non-EN catalogs land in `packages/i18n`.

## Wall

`services/svc-notify/**` only. No dual-write. Class X credentials never invented.
No packages/** craft this wave.
