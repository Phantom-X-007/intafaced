# Claim D26-P1-M2 — market.vendors lifecycle under stake/commission law

**Lane:** `denon-hard-parallel` / this session  
**Tracker:** `market.vendors` (already `done` Stages 1–3)  
**Branch:** `feat/market-vendors-lifecycle`  
**Scope:** `services/svc-market/**` only

## Done bar

Lifecycle under stake/commission law — refuse blank commission; no invent rates.

## What this cook seals

Stake half already on tip (apply → vet → claimSlot under live `vendorSlots` →
computed `listingEligibility`). Commission half had a hole: **purchase** refused
blank `MARKET_HOUSE_COMMISSION_BPS`, but **createListing** could still burn a
stake slot and the **public catalogue** could still advertise inventory that
could never settle.

Now:

1. `CommerceService.createListing` refuses `market.commission_not_configured`
   before insert or slot claim.
2. `publicListings` returns `[]` when commission is blank.
3. `purchase` still refuses blank (shared `requireCommissionConfigured`).
4. Explicit `0` remains an owner free-commission decision — never invented from silence.

## Non-goals

- No invent commission bps / ranking / C3 subscriptions.
- No second money book; no balances in market.
- Tracker mountain stays `done` (Stages 1–3); this is the Denon board seal, not a re-open.

## Leverage

Phase A **IN**: extend existing `services/svc-market` commerce + vendor seam.
No greenfield SPA / second book / invented rates.
