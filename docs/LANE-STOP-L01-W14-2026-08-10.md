# LANE STOP — L01 PAY public-api residual · wave 14 · 2026-08-10

```
LANE: L01 wave 14 product-velocity substance
shipped: none — no collision-free product Done bar remained beyond the seals already on tip
in flight: Denon #1625 public REST refund replay · Denon #1627 subscription charge cycle
parked: pay:* grants / fee tables / acquirer Class X · KYB money-gate law · chargeback/dispute wire · Denon-owned public-rest and quickstart files
Nitro must decide: pay:* production grant path · KYB gate who/when · card/PSP socket — or none
SAFE TO CLOSE: yes
tip: 3641d8b1
```

## Unit card

1. **Promise:** `docs/adr/2026-08-07-pay-public-api-law.md` §4 requires REST translation, idempotent writes, signed replayable webhooks, sandbox-key routing, and public docs.
2. **Reachable break on tip:** none found outside Denon's open file set; the remaining production gaps require Nitro law or Class X acquisition.
3. **Done bar:** a merchant-facing surface must preserve money replay, merchant authority, webhook secrecy/delivery truth, and sandbox/live separation without inventing a second book or live rail.
4. **Class:** M.
5. **Paths:** read-only falsification under `services/svc-pay/**`; this stop note only. No `public-rest*`, `payment-service.ts`, subscription, tracker, or quickstart edit.
6. **RED test first:** no new implementation unit was justified. Existing adversarial tests were rerun against the current tip.
7. **Collision list:** #1625 owns `public-rest*`, `payment-service.ts`, tracker, and `docs/pay/MERCHANT-PUBLIC-API-QUICKSTART.md`; #1627 owns subscription/payment-service paths; #1626, #1177, #1142, and #1655 do not intersect this wall.

## Tip and collision audit

- `HEAD` and `origin/main` are both `3641d8b1`; the sandbox refused updating the shared worktree `FETCH_HEAD`, but the local remote ref matches the dispatched paste lead.
- Open PR inspection confirms zero dual-write: Denon #1625 and #1627 remain fenced.
- Claim-check is clear on the unfenced public-api implementation paths and this stop note. It separately reports the repository-wide known ownership-map warning for `connect.venue-vault`, unrelated to PAY.
- Internet leverage used: existing `svc-pay` REST translation, ledger-backed balances and refund recipes, merchant webhook service, sandbox rail posture, and identity-issued principal/scopes. No replacement surface or money book was proposed.

## Engine A — product Done bars

| Unit                                 | Verdict                                                                                                       |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------- |
| Public REST + OpenAPI + auth         | Sealed on tip; Denon #1625 currently owns the only open `public-rest*` delta.                                 |
| Required idempotency                 | Sealed; stable replay, changed-body conflict, stale-owner takeover, and late-owner rejection are implemented. |
| Webhooks                             | Sealed; raw-body HMAC, endpoint/event dedupe, retry/disable, and failure dashboard exist.                     |
| Sandbox parity                       | Sealed through the existing rail posture; live principals cannot name a sandbox rail.                         |
| Merchant authority                   | Sealed; actor comes from the principal and stranger/sibling/up-tree forgeries refuse.                         |
| Public quickstart                    | Sealed on tip and currently touched by Denon #1625; no dual edit.                                             |
| Refund honesty                       | Tip has payment-namespaced refund ledger keys; #1625 is babysit-only.                                         |
| Production grants / rates / acquirer | Parked: Nitro §8 / Class X.                                                                                   |

## Engine B — promise falsification

Focused test run after building the PAY dependency graph:

- `rest-idempotency.test.ts`: replay, conflict, abandonment, stale-owner takeover, and hostile late settle/abandon.
- `merchant-webhooks.test.ts`: byte-signature verification, stable event dedupe, decimal strings, HTTPS refusal, retry/disable, and failed-delivery visibility.
- `sandbox-key-routing.test.ts`: sandbox confinement, live-key sandbox refusal, and no parallel stack.
- `merchant-ownership.test.ts`: owner, stranger, anonymous, sibling, ancestor-area, and no-merchant-node cases.
- `public-rest.money.test.ts`: discovered but skipped because PostgreSQL is unavailable in this worktree; this run does not claim a live database money proof.

Result: **34 passed, 1 skipped** across five files. `rest-idempotency` was the only suite runnable before workspace dependencies were built; after `pnpm turbo run build --filter=@intafaced/svc-pay...`, all non-Postgres suites passed.

Full `pnpm verify` after formatting is **not green**: all 36 doctrine gates and repository formatting passed, then the fleet run failed on unrelated resource pressure (`svc-p2p` property-test timeouts and a `svc-notify` Vitest worker RPC timeout). The infrastructure journal also marked the run **INCOMPLETE** because local Postgres, NATS, and EVM probes were unavailable; no green claim is made.

## Engine C — attack surface

| Attack                                | Verdict                                                                                                       |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| Denon dual-write                      | Held: no edit to #1625/#1627 files.                                                                           |
| Double refund / replay                | Existing idempotency and payment-namespaced ledger key seals remain; DB money suite was not runnable locally. |
| Secret leak / dark webhook dependency | HTTPS-only endpoints, signed raw bytes, retry/disable, and dashboard tests pass.                              |
| Scope / merchant bypass               | Stranger, anonymous, sibling, missing-node, and missing-area tests pass.                                      |
| Sandbox value confusion               | Live principal → sandbox rail refuses; missing `key_env` never silently upgrades to sandbox.                  |

## Parked and pickup

- Nitro: publish the production `pay:*` grant path and KYB money-gate timing if these should advance.
- Class X: choose and contract the live card/PSP acquirer; no agent can close it.
- Denon: resolve #1625 and #1627; only then re-open their path intersections.
- A future cook with PostgreSQL may rerun `public-rest.money.test.ts`; this is verification, not a new product unit.

No product or tracker PR is justified from this lane state. Opening one would be residual-empty padding or a dual edit, so this stop note is the only delta.
