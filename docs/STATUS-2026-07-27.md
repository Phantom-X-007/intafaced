# Status for Nitro — 27 Jul 2026 (after all service PRs)

**All of Denon’s service PRs are on `main`. Zero open service PRs at the time of this write. Nothing is a live product yet — routers are not mounted.**

Re-check live: `gh pr list` and `git log origin/main -1`. Numbers go stale; this file is a snapshot.

**Main tip (when this was written):** `7f8beec` — includes #31–#39 and handover #40.

---

## Where we are

- **On main:** ~12 packages, 11 services (identity, ledger, token, matching, trade, pay, p2p, bank, blueprint, agents, protocol) + apps web/admin + shared packages (contracts, events, ledger-client, i18n, …).
- **Not live:** service routers are not mounted into a running API. Logic + tests exist; there is no public product surface.
- The earlier handover that said “9 open PRs” was true when written; it is **false** after the merge wave (#31–#39).

---

## Audit items — status table

| #         | Finding                                               | Status                                                                                                                                                                        |
| --------- | ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **P0-4**  | Multi-stake yield underpay                            | ✅ **Shipped** (#32)                                                                                                                                                          |
| **P0-5**  | `rank.awardXp` was public                             | ✅ **Shipped** (#32) — now scoped write                                                                                                                                       |
| **P1-6**  | DoD money-test false green                            | ✅ **Shipped** (#32) — gate tightened; found real debt                                                                                                                        |
| **P1-7**  | `multiplier_bps` stored, ignored                      | ✅ **Shipped** (#32) — yield weights from stored value                                                                                                                        |
| **P1-12** | `hashTx` separator differs                            | ❌ **Disproved** — both ledgers use `.update(' ')`; no fix needed                                                                                                             |
| **P0-2**  | `order.filled` → `tradeFill` shape gap                | ✅ **Practice shipped in #31**; **ADR** — [`docs/adr/2026-07-27-trade-order-store-source-of-truth.md`](adr/2026-07-27-trade-order-store-source-of-truth.md)                   |
| **P0-1**  | Routers never mounted                                 | ⏸ **Open** — nothing is a live API                                                                                                                                            |
| **P0-3**  | Commingled hold (`userHold` mixes order + withdrawal) | ✅ **Decision A** — Denon’s proposal approved; build after P0-1 design, before deploy — [`docs/decisions/P0-3-purpose-keyed-holds.md`](decisions/P0-3-purpose-keyed-holds.md) |

### Short notes (only what still matters)

**P0-1** — Gate to anything running. Everything else is logic and tests with no live API plane.

**P0-3** — Real bug, not live yet (because of P0-1). Nitro **approved Denon’s proposal (A)** on 2026-07-27. Implementation not started. Build **after** P0-1 mount design is known, **before** any deploy.

**P0-2** — svc-trade already treats its order store as source of truth and derives `fillId` from `(marketId, engineSequence)`. The ADR makes that contract explicit so money posts do not treat the fill event as self-describing.

**P1-12** — Not a bug. Leave it.

---

## Product sequence Denon recommended

1. **P0-1 — mount the routers** (design + implement). First thing that makes a product exist.
2. **P0-3 — purpose-keyed holds** (decision A locked; implement after P0-1 design, before any deployment).
3. **Phase 2 product slices** when claimed: e.g. `trade.convert`, `web.terminal`, `venue.aggregation`; also low-coordination items like `identity.webauthn`, `token.governance` (`pnpm tracker ready`).

Matching, trade, pay, p2p, bank, blueprint, agents, and protocol are **already on main** — do not rebuild them from scratch.

---

## Operational hazards

| Hazard                                | What it means                                                                                                                                  | What to do                                                                                                                                                              |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Shared Postgres `ledger.*` schema** | svc-ledger tests used to `TRUNCATE ledger.*` on a shared schema. Two worktrees could fail each other with “insufficient funds” that look real. | Isolation fix is **#42** (unique schemas via `createTestDb`). **Fixed in #42** (on main). Still rule out a stale local checkout before treating flakes as product bugs. |
| **Vercel MCP**                        | If used, needs human auth in the browser.                                                                                                      | Agents cannot complete Vercel login for you.                                                                                                                            |

---

## Links this status owns

| Doc                                                                                                               | Role                |
| ----------------------------------------------------------------------------------------------------------------- | ------------------- |
| [`docs/adr/2026-07-27-trade-order-store-source-of-truth.md`](adr/2026-07-27-trade-order-store-source-of-truth.md) | P0-2 ADR            |
| [`docs/decisions/P0-3-purpose-keyed-holds.md`](decisions/P0-3-purpose-keyed-holds.md)                             | Nitro decision card |
| [`docs/START-HERE.md`](START-HERE.md)                                                                             | Plain product map   |

Law remains: `INTAFACED_DEFINITIVE_BUILD.md` · agent rules: `AGENTS.md` / `tooling/agent-protocol/AGENT_PROTOCOL.md`.
