# Claim D26-P1-T8 — venue.aggregation payout-grade refuse

**id:** D26-P1-T8  
**tracker:** `venue.aggregation`  
**branch:** `feat/venue-agg-payout-grade-refuse`  
**owner:** Phantom-X-007 (Denon)

## Done bar

Adapters refuse when book is not payout-grade.

## What this claim ships

- `packages/venue-adapter/src/fabric/payout-grade.ts` — absolute floor
  (`DEFAULT_MIN_BEST_LEVEL_NOTIONAL = '100'`, same string as
  `mark-from-depth.ts`) and `assertPayoutGradeBook`.
- Binance Spot + Bybit Spot `snapshotBook` call the assert before returning.
- Two-sided dust → `VenueUnavailableError` reason `no_depth`.
- Empty / one-sided still returned as facts (caller refuses the mid).

## Out of scope (honest)

- Position-relative BPS floor stays in svc-trade (adapters have no position).
- Trading half / Venue Vault / live-network CI unchanged residuals on the row.
- Row stays `ready`, not `done`.
