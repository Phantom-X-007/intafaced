# Claim D26-P1-M2 — market.vendors lifecycle under stake/commission law

**Lane:** `denon-d26-p1-m2-ledger` / this session  
**Tracker:** `market.vendors` (already `done` Stages 1–3; #1714 sealed commerce blank-commission doors)  
**Branch:** `feat/d26-p1-m2-market-vendors-lifecycle`  
**Scope:** `packages/ledger-client/src/recipes/market*` + registry / RECIPES.md — **not** `services/svc-market/src/commerce/**` (D26-P1-M1)

## Done bar

Lifecycle under stake/commission law — ledger recipes for every §8.7 vendor money leg; no invent rates.

## What this cook seals (ledger deepen)

#1714 sealed service doors (createListing / catalogue / purchase refuse blank commission).
Stake capacity stays in svc-token; VendorService still moves no value.

This cook adds the missing **ledger** legs named by doctrine §8.7:

1. `marketPurchase` — live commission split (unchanged conservation law; docs clarify stake gates stay upstream).
2. `marketListingFee` — vendor → `houseFees(market)` (§13 unwired; amount required input).
3. `marketPremiumPlacement` — vendor → `houseFees(market)` keyed by `placementId` (§13 unwired).

No magnitudes invented. No wire into commerce (M1) or VendorService (no-value law).

## Non-goals

- No invent listing/premium fee amounts or commission bps.
- No dual-edit of `commerce-service.ts` / M1 residual.
- No ranking / C3 subscriptions.
- Tracker mountain stays `done`.

## Leverage

Phase A **IN**: extend `packages/ledger-client` market recipes + existing `houseFees('market')`.
No second book, no second SPA.
