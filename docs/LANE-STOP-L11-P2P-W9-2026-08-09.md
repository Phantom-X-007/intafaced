# LANE STOP — L11 P2P · wave 9 topup · 2026-08-09

```
LANE: L11 P2P wave 9 topup
shipped: #1509 W8 residual-empty honesty bank (re-verified; not re-shipped) · this Engine B re-falsify + stop
in flight: none on wall (open p2p PRs = 0; only #1177 Shehzad protocol + 2 dependabot)
parked: p2p:moderate who (Class X) · method registry seed (product) · KMS instrument encrypt (Class X) · offer ceiling env magnitudes (product) · auto-ruling (law forbids) · rank fee discount / p2pLimitMultiplier apply (identity + product) · chat product · Stage-3 erase pseudonymise · floating mark wire (cross-service) · apps/admin dispute console (outside wall) · outbox (events plane) · Denon D26-P1-I2 merchants product-complete (invent-risk; babysit only)
Nitro must decide: none for code; optional later — moderate allowlist/scope mint, registry seed content, ceiling env numbers, KMS
SAFE TO CLOSE: yes
tip: e0126fbb (re-derive at merge)
```

## Tip ritual (start)

| Item                                             | Value                                                                                       |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------- |
| Paste-time tip lead                              | `e0126fbb`                                                                                  |
| Live tip at start                                | `e0126fbb` — tip wins                                                                       |
| Open PRs                                         | 3 total: #1177 Shehzad protocol (babysit only); #1146/#1142 dependabot maven (outside wall) |
| claim-check `services/svc-p2p`                   | clear of open PRs                                                                           |
| Code delta on wall since W8 `#1509` / `61a77559` | **none** (`git log 61a77559..origin/main -- services/svc-p2p/` empty)                       |

## Unit cards (W9 deep re-verify — no pad, no re-ship)

| Prio | Unit                                 | Verdict             | Proof                                                                                                                              |
| ---- | ------------------------------------ | ------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| A0   | Open p2p PR merge                    | **none open**       | claim-check clear; 0 p2p path-intersecting open PRs                                                                                |
| A1   | created timeout re-drive             | **HOLDS**           | `timeoutActionFor('created')→settle_or_void`; state suite + README matrix                                                          |
| A1   | sweepSettlements                     | **HOLDS**           | decide-then-post; recipe keys; second sweep moves nothing (sealed #1283 money suite)                                               |
| A1   | escrowIntegrity                      | **HOLDS**           | per-trade pots; holding statuses + late resolved-unsettled; soft lag on `created`+lock-posted is monitoring only (funds re-driven) |
| A2   | fee service-only                     | **HOLDS**           | no per-take `feeBps` on wire; constructor/`P2P_FEE_BPS` only; fractional refuse                                                    |
| A2   | disputes allowlist                   | **HOLDS**           | empty → `p2p.moderation_unreachable`; natural-person rulings only; escalate-and-hold                                               |
| A2   | instruments disclose                 | **HOLDS** mechanism | held-escrow only + access log; free peek closed; KMS park Class X                                                                  |
| A2   | merchants badges→limits              | **PARTIAL** honest  | Stage 1–2 mechanism; env unset = unlimited (no invent magnitudes); D26-P1-I2 product-complete is Denon hard                        |
| A3   | state machine matrix                 | **HOLDS**           | `state.test.ts` 36 tests: graph, illegal transitions, value-moving timeout set                                                     |
| A3   | kill-switch                          | **HOLDS**           | blocks create/take/**resume**; settlement continues; pause allowed under kill (hide liquidity)                                     |
| A3   | Engine B chapter                     | **this doc**        | below                                                                                                                              |
| A3   | moderate who / KMS / chat / ceilings | **parked**          | Class X / product / outside wall                                                                                                   |

## Sealed (do not re-open)

Decide-then-post · re-drive created (`settle_or_void`) · announce-before-stamp · escalate-and-hold disputes · instruments mechanism · XP business keys · fee not per-take · #1298 naming · #1283 integrity/fee/dust/pause/last_settle_error/IDOR · #1509 W8 honesty bank (boot + API surface).

## Engine B (W9 falsification on tip `e0126fbb`)

| Promise                                                                | Verdict                                                                   |
| ---------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| One thing this service is for (no strand; late OK)                     | **HOLDS**                                                                 |
| Trade state machine                                                    | **HOLDS**                                                                 |
| API (scopes · IDOR NOT_FOUND · fee wire closed · merchants/ops listed) | **HOLDS**                                                                 |
| Events (business keys; dispute.resolved automatic=false)               | **HOLDS** (sealed prior; no code drift)                                   |
| Ledger recipes only; fee from escrow; no fee on refund                 | **HOLDS**                                                                 |
| Can funds ever be stranded?                                            | **HOLDS** (late + durable `last_settle_error`; disputed holds on purpose) |
| DB constraints                                                         | **HOLDS**                                                                 |
| Fiat = config                                                          | **HOLDS**                                                                 |
| Kill-switch does not freeze settlement                                 | **HOLDS**                                                                 |
| Merchants badges → limits                                              | **PARTIAL** (mechanism; policy open)                                      |
| Instruments under held escrow + log                                    | **HOLDS** mechanism; KMS residual named                                   |

## Engine C (attack)

| Surface                   | Note                                                                               |
| ------------------------- | ---------------------------------------------------------------------------------- |
| Stranded funds            | Closed — clocks + decide-then-post + sweeps; disputed = hold-until-human by design |
| Double settle             | Closed — recipe keys + terminal guards                                             |
| Fee invent                | Closed — service env only                                                          |
| Illegal state jump        | Closed — `assertTransition` + DB                                                   |
| Instrument free peek      | Closed mechanism; harvest residual named in README                                 |
| Empty moderator allowlist | Honest refuse — no soft open                                                       |
| Machine ruling            | Blocked — escalate + natural-person + DB trigger                                   |

## Soft (not craft residual — unchanged from W8)

- `escrowIntegrity` SELECT omits `created`+lock-posted window until status advances or ~2 min timeout — monitoring lag only; funds still re-driven via `settle_or_void`.
- Kill-switch also blocks `resumeOffer` (stricter than short “offers+takes” wording; correct under kill — cannot re-list liquidity while trading is off).

## Local re-verify (this stop)

- Pure suite (tip code, packages built in main checkout): **221 passed** — state 36 · pricing 31 · instruments 30 · reputation 27 · linear-pattern 20 · merchant-limits 19 · merchant-programme 15 · router.mount 23 · moderation-auth 9 · release-postable 5 · env-retention 3 · ledger-client 3
- Worktree re-run without workspace package builds: **131 pure** (subset that does not import `@intafaced/contracts`); same tip SHA for `services/svc-p2p/**`
- dispute-ruling-invariant + money suite (`p2p-service.test.ts` etc.): need Postgres/Docker — **unavailable here**; sealed on CI / #1283 path
- Money suite: not re-run (no Docker); sealed prior
- Denon open PRs path-intersect `services/svc-p2p/**`: **none**
- Shehzad #1177: babysit only; not on wall
- No code thrash; no Engine A pad rows; residual-empty stop is the win

## Tracker honesty (mountain events only — no craft PR)

| Mountain                  | Tracker posture                                                                      | Agent craft residual?                       |
| ------------------------- | ------------------------------------------------------------------------------------ | ------------------------------------------- |
| `p2p.disputes`            | STAGE SHIPPED mechanism; owner product residual (`p2p:moderate` mint, admin console) | **No**                                      |
| `p2p.payment-instruments` | MECHANISM DONE; empty registry + KMS Class X                                         | **No**                                      |
| `p2p.merchants`           | Stage 1–2 mechanism; ceilings env / Stage-3 cut                                      | **No** (D26-P1-I2 invent-risk = Denon hard) |

## Next agent

1. Do not invent moderate who / registry seed / KMS / ceilings / auto-ruling / rank multiplier apply / merchants product-complete.
2. Cross-service floating mark only via contracts/events PR first.
3. Babysit only if a new p2p PR appears on the wall.
4. Do not pad Engine A — residual-empty stop is the win.
5. Money suite re-run only when Docker/Postgres available; do not claim full green from pure suite alone.
