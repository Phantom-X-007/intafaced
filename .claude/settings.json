{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Grep",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"${GROK_AGENT}\" ]; then exit 0; fi; graphify hook-guard search"
          }
        ]
      },
      {
        "matcher": "Read|Glob",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"${GROK_AGENT}\" ]; then exit 0; fi; graphify hook-guard read"
          }
        ]
      }
    ]
  }
}
