# Frontend autonomous continue brief — Stream A

**Operator:** Nitro (non-technical). **Do not ask him to run git/CI/Orca.**  
**Lane:** Stream A shell only · `feat/app-*` · Class **N**  
**Product surface:** `vendor/*/05_Web_Front` on `:8090` — not `apps/web`

## Standing order (self-prompt)

1. Orient: `gh pr list`, tip `origin/main`, LIVE-LANES, open heads that touch shell or order-route.
2. **Never collide** with: `feat/order-route-*` (#289), residual futures/pay/ws/ledger money PRs, Denon spine.
3. Claim only Stream A file territory; if LIVE-LANES is hot, skip editing it and note in PR body.
4. One concern per PR. Worktree from tip. `pnpm`/Node 18 for shell. Brand-scan clean.
5. Ship: implement → brand-scan → push (code touch if docs-only would skip CI) → green → self-audit Class N → squash-merge.
6. After merge, re-derive tip and continue next Wave B/C item without waiting for Nitro.
7. Escalate to Nitro only: go-live money, Class X, true product forks he alone can taste (new palette id if he re-picks).

## Done already (tip main)

| Item | PR |
| --- | --- |
| Wave A honesty + locks + grouping | #261 #262 #267 |
| P21 modular teal | #267 |
| B5 prefs · B7 keyboard · B8 impact | #267 |
| B9 partial fill · copy id · CSV | #295 |

## Next craft order (this program)

| ID | Work | Notes |
| --- | --- | --- |
| B2 | Density hierarchy | CSS + structure on desk only |
| B6 | Watchlist rail | Promote favourites without new backend |
| B10 | a11y floor | Live region + focus validation error |
| B3 | uc craft = desk | Shared honesty + token use on money screens |
| B9′ | Fee schedule surface | Page-data fee honesty on ticket (residual of B9) |
| B14 | content-visibility blotter | Measure-first spike if cheap |
| B12 | CMS/OTC last | Only after desk+money |
| Wave C | LWC panes etc. | After B DoD / written plan A6 |

## Hard bans

Fake money/candles · fixture-seeded balances as proof · partner names in UI · pure `#000` brand tattoo forever · re-opening style marathon · order-route / futures invent
