# LANE STOP — L08 NOTIFY+ALERTS · wave 4 · 2026-08-09

```
LANE: L08 wave 4
shipped:
  #1294 mute prefs SQL is executed in CI; /ready shows reaper last tick
  #1292 two replicas share one register/verify rate budget (no N× free SMS)
in flight:
  #1293 stuck-pending arm names delivery_stuck (not attempts_exhausted) — CI blocked by unrelated svc-pay flake
  #1315 permanent gateway reject names transport_rejected + digest stays unwired — same pay flake
parked:
  v22.alerts MVP — product law (core types, dark-source definition, plane F vs B) — Nitro decides before greenfield
  gateway credentials Class X — Nitro obtains URL+token (OWNER-ACTIONS-NOTIFY-GATEWAYS)
  digest cadence product — pure helpers exist; resist wiring without owner cadence law
  operator delivery board — user has notify.deliveries; admin list residual
Nitro must decide:
  v22.alerts MVP scope (price-only core vs more)
  email/push/SMS gateway credentials
  digests: ship cadence law or leave pure helpers forever
SAFE TO CLOSE: yes for agent wall craft this session — in-flight PRs babysit until pay flake clears; no Class X
tip: re-derive origin/main (session tip after #1292)
```

## Engine A scoreboard (tip + this wave)

| Unit                                        | Status                                      |
| ------------------------------------------- | ------------------------------------------- |
| A0 open PR intersect                        | CLEAR at start; then own PRs                |
| A1 delivery state machine (reaped pending)  | SEALED #1187; honesty residual → #1293      |
| A1 v22.alerts MVP                           | PARKED — no product code; law incomplete    |
| A2 channel adapters no credentials → refuse | SEALED                                      |
| A2 rate limits residual (multi-replica)     | SHIPPED #1292                               |
| A2 consent/footer                           | SEALED #1187                                |
| A3 event-wiring Class B                     | SEALED (notify none; only crew B elsewhere) |
| A3 multi-replica no double send             | SEALED (claim lease)                        |
| A3 boot refusal                             | SEALED                                      |
| A3 README matrix                            | SEALED                                      |

## Engine B chapter (falsification pass)

Load-bearing README promises re-checked against tip code+tests: **PROVED** for accepted≠delivered, unconfigured refuse, required-channel boot gate, claim lease, stuck-pending reap, rate limits (now shared), footer, mute critical immunity. **Honest residual:** Class X out-of-app send; operator admin board; #1293/#1315 pick-up. **Not a lie:** #1187 delivered-pending fix holds.

## Engine C

Bus subjects money-critical (margin + liquidation) mounted. Idempotent claim+lease. PII: addresses in targets; gateway detail redacts address echo.

## Pick-up for next L08 chat

1. Merge #1293 + #1315 when Tests green (failure is `svc-pay` destination ref `0xg4finish`, not notify).
2. Rebase any further notify work after those land.
3. Do not invent v22.alerts product law.
