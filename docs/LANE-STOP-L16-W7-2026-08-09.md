# LANE STOP — L16 EDGE · wave 7 topup · 2026-08-09

```
LANE: L16 EDGE wave 7 topup
shipped: #1480 outside-the-door + kill durability · #1511 flagEdgeGateway honesty + /ready rateLimit posture · #1510 hop-by-hop full strip + /ready not kill oracle + EDGE_BODY_LIMIT_BYTES + env blast-radius pin
in flight: none
parked: multi-replica shared kill store (§13 invent fence) · refresh-token httpOnly cookie CORS/WS re-take (§13) · geo-IP region · edge.gateway flag still NOT_ENFORCED (status now says so — do not pretend it gates) · WS route-behind-edge product change (L05 / routing law) · streaming/WS proxy buffering
Nitro must decide: none (Class X host/secrets prod only if/when deploy)
SAFE TO CLOSE: yes
tip: 70388df9
```

## Re-verify (sealed properties · tip)

| Property                                                  | Result   | Proof                                                            |
| --------------------------------------------------------- | -------- | ---------------------------------------------------------------- |
| Kill-switch fail-closed + release asymmetry               | **hold** | `kill-switch.test.ts` + `control-plane.e2e.test.ts` green on tip |
| CORS no `*`, no credentials, preflight not a route oracle | **hold** | `cors.test.ts` green; browser suite skips without Chromium       |
| Console cannot green-halt `ws`                            | **hold** | arm refuse 400 + status `outsideTheDoor.ws` (#1480)              |
| Multi-replica kill not invented                           | **hold** | `killState.multiReplicaShared: false` always on status           |
| `edge.gateway` not a fake kill                            | **hold** | status `flagEdgeGateway.enforced: false` (#1511)                 |
| Hop-by-hop class complete                                 | **hold** | full RFC set stripped (#1510)                                    |
| `/ready` not a halt oracle                                | **hold** | no `disabledModules` on unauthenticated `/ready` (#1510)         |

## Engine A residual disposition

| Unit                            | Disposition                                                                               |
| ------------------------------- | ----------------------------------------------------------------------------------------- |
| A0 open edge PR merge           | none at stop; #1510 + #1511 merged                                                        |
| A1 kill-switch residual         | **empty** for edge craft — operator halt real; unarmable named; gateway flag honesty      |
| A1 CORS residual                | **empty** — sealed re-verify green                                                        |
| A1 WS-behind-edge honesty       | **honesty done** — product socket close stays L05/routing; named residual not fake closed |
| A2 admin-api residual           | **shipped** status honesty (#1480 + #1511)                                                |
| A2 multi-replica kill residual  | **named park** — file/process honesty; shared store §13                                   |
| A2 control-plane e2e residual   | **empty** — suite green + status fields tested                                            |
| A2 route surface residual       | **empty** — table complete + README lock test                                             |
| A2 body-limit residual          | **shipped** `EDGE_BODY_LIMIT_BYTES` (#1510)                                               |
| A3 hop-by-hop residual          | **shipped** (#1510) — audit 2026-08-08 #7                                                 |
| A3 `/ready` oracle residual     | **shipped** (#1510) — audit 2026-08-08 #5                                                 |
| A3 refresh-token socket honesty | **named residual** in README Not-built                                                    |
| A3 Engine B chapter pass        | **done** — README security properties match code on tip                                   |
| A3 path-intersect L05/L11       | **clear** — no dual-write this lane                                                       |
| A3 host/secrets prod            | **park Class X**                                                                          |

## Engine B (promise falsify)

| Chapter              | Verdict                                                                                        |
| -------------------- | ---------------------------------------------------------------------------------------------- |
| Why it exists        | holds                                                                                          |
| API contract         | holds (incl `/metrics`, `/admin/*`, full route table, body budget on `/ready`)                 |
| Security properties  | holds (hop-by-hop complete; `/ready` not halt oracle)                                          |
| Browser origins CORS | holds                                                                                          |
| Kill-switch          | holds (operator surface; flag honesty; outside-the-door)                                       |
| Configuration        | holds (rate limit + body limit + kill path env documented)                                     |
| Not built yet        | holds — only true residuals named (geo-IP, multi-replica kill, cookie, WS proxy, flag enforce) |

## Engine C (attack surface)

| Vector                 | State                                                    |
| ---------------------- | -------------------------------------------------------- |
| kill fail-open         | refused (undecidable → 503)                              |
| CORS free origin       | refused (no `*`; enforced envs closed door)              |
| fake WS close          | refused to arm; status names gap                         |
| admin green while dead | ledgerConfigured boolean; freeze pass-through status     |
| hop-by-hop smuggle     | full class stripped (#1510)                              |
| `/ready` halt oracle   | closed (#1510)                                           |
| secret leak            | edge still holds no `INTERNAL_SERVICE_SECRET` / DB / bus |

## Anti-pad

No Engine A row invented to hit a count. SAFE TO CLOSE = residual-empty honesty for **edge craft L1–L4**, not “zero §13 forever.”
