# LANE STOP — L04 IDENTITY affiliates · wave 11 · 2026-08-09

## Operator block

```
LANE: L04 wave 11 product-velocity
shipped: #1589 affiliate commission rows name the fee pool for payout (trade/pay/… houseFees, not identity default)
in flight: none
parked: DIRECTION §8 affiliate commission magnitudes (Nitro) · fee-event producers in trade/pay (outside wall) · MAX_PAYOUT_TIER_DEPTH owner ruling · pay:* grants never invent · KYC vendor Class X
Nitro must decide: §8 fee-share / IB tier rates if product wants live payout; nothing for merge
SAFE TO CLOSE: yes
tip: ad9cd25a
```

## Unit card this wave

| PR    | Promise (quoted)                                                                                          | Done bar                                                                                           | Class   |
| ----- | --------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ------- |
| #1589 | tracker / open PR bank — accruals must name which module fee pool held the fee so payout sweeps that pool | trade-funded + `sourceModule: trade` pays from `houseFees(trade)`; wrong pool fails without invent | M micro |

A0 was the open-PR bank. CI sealed on Doctrine · Typecheck · Tests · DoD · Gitleaks · CX-8 · Docs · audit; squash-merged as `9497f602` on main (tip later moved past docs/bank).

## Engine A disposition (L04 W11)

| Prio | Unit                        | Disposition                                                                        |
| ---- | --------------------------- | ---------------------------------------------------------------------------------- |
| A0   | Merge #1589 fee pool naming | **SHIPPED** — green CI · on tip                                                    |
| A1   | accrue residual             | **SEALED** — rates refuse-closed without law (`commission-rate-law`)               |
| A1   | payout residual             | **SEALED** — refuse `rates_unset` / ledger unwired; pool provenance fixed by #1589 |
| A1   | multi-tier residual         | **SEALED** — depth / cycle / self refuse (tree + payout)                           |
| A2   | freeze residual             | **SEALED** — no pay invent; frozen beneficiary refuses payout                      |
| A2   | PII isolation residual      | **SEALED mechanism** (#1348 store + gate); vendor Class X park                     |
| A2   | step-up / TOTP multi-pod    | **SEALED** — re-verified pending-totp + webauthn multi-pod suites                  |
| A3   | §8 magnitudes               | **PARK Nitro**                                                                     |
| A3   | mountain-event              | `ops.affiliates` stays **wip** (rates + producer remain) — not agent-`done`        |
| A3   | Engine B pass               | **PASS** this session (independent re-run)                                         |

## Engine B — promise falsification (independent re-run on tip)

| Promise   | Reality on tip                                                                    | Result   |
| --------- | --------------------------------------------------------------------------------- | -------- |
| attribute | cycle / self / depth refuse; advisory lock path                                   | **HOLD** |
| accrue    | unpublished law → AccrualRateRefuseError; `sourceModule` stamped / invalid refuse | **HOLD** |
| payout    | rates_unset refuses with zero balance move; trade pool pays; wrong pool fails     | **HOLD** |
| freeze    | freeze/unfreeze honesty; frozen hop refuses pay                                   | **HOLD** |
| fee pool  | #1589 durable `source_module` + payout sweep uses row module                      | **HOLD** |

**Proof (RAN-IT this session):**

- `vitest` affiliates + pending-totp + webauthn multi-pod — **106 pass**
- `vitest` payout-engine + commission-rate-law — **40 pass** (includes trade-pool pay + wrong-pool residual)
- Local gates on #1589 branch before merge race: **36/36** (PR already green on GitHub when merged)

## Engine C — attack surface

| Attack               | Defence                                                       |
| -------------------- | ------------------------------------------------------------- |
| rate invent          | refuse-closed accrual + payout; no DEFAULT_ACCRUAL_TIERS      |
| payout invent money  | no rates → no post; unfunded / wrong pool → InsufficientFunds |
| hostile sourceModule | invalid slug refused at accrue / plan                         |

## Why stop

Agent-craft product residual on exclusive wall `services/svc-identity/**` is empty for wave-11 velocity after open-PR bank. Sibling L05 W10 stop (#1603) already named residual-empty post-#1589; this lane independently re-verified Engine B and confirms the same stop — no pad craft, no sealed re-cook.

## Next pickup (not this lane)

1. Nitro publishes `IDENTITY_AFFILIATE_ACCRUAL_TIERS_JSON` (§8)
2. Trade/pay wire fee events → `affiliates.accrue` with real `sourceModule` (other walls)
3. Optional owner ruling on `MAX_PAYOUT_TIER_DEPTH`
4. KYC vendor Class X if documents enter the vault
