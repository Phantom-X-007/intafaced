# L3 pack — certs xp emit shape + notify required channels

Does NOT invent L1/L2.

## academy.certs

Pure xpEarned publish shape from Stage-2 intent.

## ops.notifications

Required channel declaration + missing credential check.
