# Wave D audit — 2026-07-30 (post product batch #145–#150)

**Tip at write:** re-check `git rev-parse origin/main` (expected high water includes #150 `p2pEscrowRefunded` fan).  
**Scope:** light wave after balance REST · OHLCV empty · positions empty · convert tracker · notify fans.  
**CI GitHub:** still **billing-blocked** (jobs never start) — human Billing & plans; not a code green claim.

## Ships covered

| PR | Risk | Notes |
| --- | --- | --- |
| #145 | Money-read | `GET /account/balance` — ledger projection, self-only; no second balance store |
| #146 | Public | OHLCV route always honest empty until candle aggregation exists |
| #147 | Public/private | Positions `[]` until `trade.futures`; no leverage/margin routes |
| #148 | Notify | Fans for rankUpdated, stakeCreated, p2pEscrowReleased — userIds present |
| #149 | Tracker | trade.convert → done; money-path tests exist; do not claim Actions green |
| #150 | Notify | p2pEscrowRefunded fan — buyer/seller ids on payload |

## Gates — how to run (from monorepo worktree)

```bash
pnpm scan:brand
pnpm scan:custody
pnpm scan:vendor-shell
pnpm scan:workspace
# tracker: node tooling/scripts/tracker.mjs  (or project-documented tracker check)
```

Cadence reminder (`docs/GRIND-PLAN-2026-07-30.md`): light gates every PR; full wave every ~3–4 product ships.

## Results (this audit PR — run locally on tip worktree)

| Gate | Result | Notes |
| --- | --- | --- |
| brand | **green** | 568 files, 0 forbidden names |
| custody | **green** | 67 files across 3 Protocol Plane service(s) |
| vendor-shell | **green** | 1107 vendor files, 7 hazard patterns checked |
| workspace-sync | **green** | 16 services reach image + fleet |
| tracker | notes may lag fans | Queue item: refresh `ops.notifications` note; convert already #149 |

If `pnpm` install / monorepo deps are unavailable in the agent environment, **do not invent green**. Operator or next agent: run the five gates above on tip and amend this table.

## Explicit skips (safety)

- **p2pDisputeResolved** notify fan — payload has no buyer/seller userIds  
- **Private balance WS** — no safe ledger projection event path for self-only stream yet  
- **OHLCV real candles** — no aggregation job; empty is correct  
- **Futures positions** — product not built; empty array is correct  

## Verdict

**Keep grinding NEXT QUEUE** on `docs/GRIND-LOOP-ACTIVE.md`. Doctrine posture for Wave D product ships is honest-empty / self-only projection / event fans with principals. Status remains **RUNNING** until agent-cookable queue is empty or only human-blocked.

## Next free mountains

1. `account/trades?symbol=` filter if still missing  
2. Tracker note hygiene  
3. Safe notify fans only  
4. Thin pay public REST only if cheap  
5. Human: GitHub Actions billing  
