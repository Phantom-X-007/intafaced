# LANE STOP — L08 NOTIFY + ALERTS · wave 14 · product-velocity substance · 2026-08-10

**Lane wall:** `services/svc-notify/**`  
**Tip:** `3641d8b1` (includes #1638 and the wave-13 notify seal).  
**SAFE TO CLOSE:** **yes** — the product wall is residual-empty; remaining work is Class X or unsettled cross-product law.

```
LANE: L08 wave 14 product-velocity substance
shipped: none — #1638 holds; no new product Done bar remained on the exclusive wall
in flight: none
parked:
  · outbound email/push/SMS credentials — Nitro Class X
  · operator delivery-outcomes view — admin-scope product law is not decided
  · digest cadence — deliberately unwired until product law exists
  · funding/liquidation-proximity/whale alerts — Phase 5 and dependent product law
  · mobile watchlist sync — HUMAN shell/mobile product
Nitro must decide: gateway credentials only if out-of-app delivery must go live; otherwise none
SAFE TO CLOSE: yes
tip: 3641d8b1
```

## Unit card

1. **Promise:** `tooling/tracker/features.mjs:1391` (`ops.notifications`) promises event-driven fan-out; `tooling/tracker/features.mjs:1414` (`v22.alerts`) promises sourced-price conditions and watchlists while refusing dark marks.
2. **Reachable break sought on tip:** a late gateway response stealing a reclaimed attempt; a watch marked active while no live price source exists; compose leaving the ticker dark; duplicate sends after a worker crash; forged/unbounded gateway spam.
3. **Done bar:** in-app fan-out really delivers; out-of-app channels deliver only through configured gateways and otherwise refuse by name; watches evaluate against the public trade ticker when configured and disclose `canFire: false` when dark; reclaim and retries cannot double-notify.
4. **Class:** N (ops truth and notification delivery; no value movement).
5. **Paths:** `services/svc-notify/**` read-only falsification plus this stop note.
6. **RED first:** no new reachable RED was found after #1638. Existing RED-to-green proofs were rerun rather than inventing a duplicate unit.
7. **Collision:** open PRs #1625–#1627, #1177, #1142, and #1655 do not intersect `services/svc-notify/**` or this note; `claim-check` reported these paths clear.

## Engine A — disposition

| Priority | Product unit           | Done bar                                | Result                                                                   |
| -------- | ---------------------- | --------------------------------------- | ------------------------------------------------------------------------ |
| A0       | #1638 holds            | late settle cannot steal reclaim        | **HOLD** — attempt ownership and lease proofs pass                       |
| A1       | notifications residual | honest fan-out                          | **SEALED** — in-app delivers; external gateways refuse when unconfigured |
| A1       | `v22.alerts` core      | sourced mark + honest dark state        | **SEALED** — sweep, HTTP mark, mount, and compose pin pass               |
| A2       | no double notify       | crash/reclaim/retry remains single-send | **SEALED** — lease and bounded-idempotent retry proofs pass              |
| A3       | content/templates      | Class X                                 | **PARK Nitro**                                                           |
| A3       | stop                   | residual-empty honesty                  | **this artifact**                                                        |

No tracker mountain event was created: neither mountain changed owner, status, or product truth in this wave.

## Engine B — promise falsification

| Chapter                  | Proof rerun                                                                            | Verdict  |
| ------------------------ | -------------------------------------------------------------------------------------- | -------- |
| Notify README / channels | channel catalog, refusal matrix, real HTTP gateway wire, required-channel boot refusal | **HOLD** |
| Alerts mountain          | evaluation, mounted sweep, reachable tRPC socket, public trade HTTP mark               | **HOLD** |
| Compose                  | `trade-url-compose-pin.test.ts` requires `TRADE_URL` on the service                    | **HOLD** |
| Reclaim                  | claim-lease, in-flight crash reclaim, channel-store reclaim, settle-attempt ownership  | **HOLD** |
| Kill/reaper              | kill writes no fake send; stuck delivery reaper remains named and bounded              | **HOLD** |

## Engine C — attack surface

| Attack                            | Proof rerun                                                                | State         |
| --------------------------------- | -------------------------------------------------------------------------- | ------------- |
| Double notify after crash         | live lease keeps work alive; expired lease reclaims once                   | **mitigated** |
| Late settle steals reclaim        | delivery settlement is owned by attempt                                    | **mitigated** |
| Spam/forged gateway retries       | gateway retries bounded and idempotency-keyed; per-target rate limits hold | **mitigated** |
| Invented/stale price fires alert  | `MarkSource.kind` and `canFire` expose dark source; no fabricated mark     | **mitigated** |
| Missing gateway pretends delivery | named `channel.not_configured`; required deployment refuses boot           | **mitigated** |

## Validation

- `pnpm exec turbo run build --filter=@intafaced/svc-notify...`: **9/9 packages built**.
- `pnpm --filter @intafaced/svc-notify test`: **40 files passed, 4 skipped; 307 tests passed, 46 skipped**. Skips are the PostgreSQL-backed variants because no local test database was available; memory/store conformance and all product-law proofs passed.
- `pnpm --filter @intafaced/svc-notify typecheck`: **clean**.
- `pnpm verify`: doctrine gates and formatting passed, but the full parallel Turbo run was **not green**: notify's 307 assertions passed and then Vitest reported `Timeout calling "onTaskUpdate"` under fleet load; local Postgres/NATS were also unreachable, leaving 12 infrastructure suites unexecuted. The isolated notify run above is the product-wall signal; this incomplete full run is reported honestly.
- Initial direct service run before dependency builds failed on missing workspace `dist` entries; after building the dependency graph, the identical suite passed. This was worktree setup, not a product defect.

## Stop reason

Wave 13 already closed the last reachable product defects in #1638. The remaining title residuals require owner credentials, HUMAN shell/mobile work, or product law for alert categories and operator views. Editing `svc-notify` again would be residual-empty re-falsification or would invent law, both explicitly banned for this lane.
