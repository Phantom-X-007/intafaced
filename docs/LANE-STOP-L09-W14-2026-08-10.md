# LANE-STOP L09 AGENTS portfolio + risk-compliance · wave 14 · 2026-08-10

```text
LANE: L09 wave 14 product-velocity substance
shipped: none — both assigned product mountains remain truthfully upstream-blocked; no residual-empty craft was re-cooked
in flight: none
parked: agents.portfolio on ops.portfolio/indexer.readmodels plus cross-plane movement law; agents.risk-compliance on ops.compliance plus counsel-owned sanctions/jurisdiction content; live allowlists and agent pricing §8 remain Nitro-only
Nitro must decide: none for craft; counsel/Nitro supplies sanctions and jurisdiction content, and product direction must settle cross-plane rebalance before either parked engine moves
SAFE TO CLOSE: yes
tip: f7a915a6
```

## Tip ritual and wall

- Branch: `feat/w14-l09`, based on `3641d8b1`; live `origin/main` was `f7a915a6` by `git ls-remote`. The only intervening commit was L02's pay stop note, with no `services/svc-agents/**` intersection.
- Open PRs re-derived: #1625–#1627, #1177 and #1142. None touches `services/svc-agents/**` or this stop path.
- `claim-check` reported both paths clear. Its separate incomplete ownership-axis warning concerns `connect.venue-vault`, outside this wall.
- Leverage used: the existing `svc-agents` gateway/runtime, `agent_actions` audit spine, fleet guardrails, readiness truth and ledger-backed metering. No second runtime, balance book, pricing table or allowlist was invented.

## Unit cards

| Unit                          | Promise quoted                                                                               | Reachable break on tip                                                                                                                    | Done bar                                                                                                                                       | Class                       | RED evidence / result                                                                                             |
| ----------------------------- | -------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| Portfolio product stage       | Tracker `agents.portfolio`: “Portfolio Agent — auto-rebalance inside user guardrails (§8.2)” | `ops.portfolio` is blocked on `indexer.readmodels`; the agent has no truthful held-position input, and cross-plane rebalance is undecided | Read both planes without turning absent holdings into zero; every movement remains inside user guardrails, killable, audited and ledger-backed | M when it moves             | No lawful RED implementation test can be written until the upstream view and movement boundary exist — **parked** |
| Risk/compliance product stage | Tracker `agents.risk-compliance`: “screening support and report drafts (§8.2)”               | `ops.compliance` is not done; sanctions blocklist is empty and no jurisdiction is blocked                                                 | Grounded screening support and human-reviewable drafts only; never approve KYC, file, clear or decide                                          | P / X for sanctions content | A fixture-backed “success” would launder missing counsel content into assurance — **parked**                      |
| Stage-1 honesty               | README: every run reports metering on every outcome; runtime law says every action is logged | A forged tool, dark dependency, provider outage or thrown tool could otherwise appear successful                                          | Refuse before execution, log refused/failed, bill zero on dark paths, never expose fake production readiness                                   | N                           | Existing RED pins cover hostile tools, dark-plane zero billing, failures and readiness — **holds; sealed**        |
| Kill persistence              | README/runtime: operator `enabled=false` must stop new sessions                              | Boot re-register could reset the operator kill                                                                                            | Conflict upsert preserves `enabled`; disabled agent remains disabled after redeploy                                                            | N                           | `kill-enable-preserve.test.ts` and runtime integration pin — **holds after #1615; sealed**                        |

Paths remained single-writer: inspection under `services/svc-agents/**`; this stop note is the only repository delta. Portfolio would become Class M because it places orders with user money; no money code was changed in this wave.

## Engine A — ranked residual

| Priority              | Result                                                                                       |
| --------------------- | -------------------------------------------------------------------------------------------- |
| A0 kill-dead holds    | Sealed by #1615; static pins still present, not re-shipped                                   |
| A1 portfolio          | Blocked by `ops.portfolio` / `indexer.readmodels` and undecided cross-plane movement         |
| A1 risk-compliance    | Blocked by incomplete `ops.compliance` and counsel-owned empty screening content             |
| A2 stage-1 honesty    | Existing failure/refusal/dark/readiness pins cover the reachable surface; no new break found |
| A3 allowlists/pricing | Nitro-only / §8; parked                                                                      |
| A3 Engine B/C + stop  | Completed below; residual-empty, no product padding                                          |

## Engine B — promise falsification

At least four independent promises were checked against the implementation and tracker:

1. **README / accountable runtime:** `runtime.act` guardrails before execution, appends failures on thrown tools, and appends executed actions only after the tool returns. The user log remains scoped to the requesting user.
2. **Portfolio mountain:** falsified as currently buildable. Its mandatory held-position substrate does not exist because `ops.portfolio` is blocked on `indexer.readmodels`; treating absent sovereign holdings as zero would be a false balance.
3. **Risk mountain:** falsified as currently buildable. `ops.compliance` is not done and counsel-owned screening content is empty; the tracker explicitly forbids turning a draft agent into an approval/filing/clearance decision maker.
4. **Kill path:** `registerAgent` conflict handling and the boot-register pin preserve operator `enabled=false`; #1615 remains sealed.
5. **Readiness honesty:** readiness tests distinguish mock availability from production inference, refuse orphan/down routes and scan out vendor names.
6. **Billing honesty:** dark fleet runs report billed amount zero; metering-off settlement never calls `feeCharge`; replay and seal/post recovery remain pinned in runtime tests.

## Engine C — attack surface

| Attack                          | Result                                                                                                                                                   |
| ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tool forge                      | Fleet-wide hard denylist covers ledger/pay/bank/trade/P2P write verbs; injected `bank.withdraw` and `trade.order` refuse for every Stage-1 product agent |
| Allowlist invent                | No live tool grant, sanctions entry, jurisdiction block or tier magnitude was added; missing law stays a refusal/park, not permissive configuration      |
| Zombie agent                    | Boot registration does not overwrite the operator kill; disabled agents cannot be resurrected by redeploy                                                |
| Dark dependency                 | Provider/route outage and dark product-plane runs refuse, remain auditable and bill zero rather than claim empty success                                 |
| Compliance authority escalation | Risk/compliance remains drafts-only; no path may write KYC approval, file a report, or clear a flagged account                                           |
| Cross-plane value confusion     | Portfolio stays parked rather than treating a bridge movement as an ordinary rebalance trade                                                             |

## Validation honesty

- `pnpm --filter @intafaced/svc-agents test`: **64 files passed, 389 tests passed; 1 database-backed file / 1 test skipped** because Postgres was unavailable. The fleet pins exercised in this stop are green.
- `pnpm verify`: all 36 doctrine gates and formatting passed, then the repository-wide run failed on an unrelated `services/svc-ws/src/ws/gateway.test.ts` trade-stream timeout (5 seconds). The run was also explicitly **INCOMPLETE** because local Postgres and NATS were unreachable; it is not called full green.
- Source and executable test-pin inspection completed for the six Engine B/C surfaces above. No `svc-agents` failure was observed before Turbo stopped downstream work on the WS failure.

## Pick-up

1. Portfolio resumes only after `ops.portfolio` has truthful ledger + indexer views and direction settles whether a rebalance may cross planes.
2. Risk/compliance resumes only after `ops.compliance` is done and counsel supplies sanctions/jurisdiction content; implementation remains screening support + report drafts, never a compliance decision.
3. Live allowlists, credentials and §8 pricing magnitudes stay Nitro/Class X.

No CLEAR product Done bar remains on this wall at this tip. Re-cooking the sealed Stage-1 fleet or fabricating fixtures for either blocked mountain would be padding, so the lane is safe to close.
