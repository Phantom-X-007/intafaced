# Claim D26-P1-X2

**owner:** Phantom-X-007  
**branch:** `feat/d26-p1-connect-residual`  
**scope:** `packages/venue-adapter/src/fabric/capture-routing.ts` Â· `packages/venue-adapter/src/cost-model.ts` (+ tests) Â· package `index.ts` export only  
**status:** active  
**updated:** 2026-08-12  
**coord:** #1739 capture-lake honesty (path-disjoint â€” do not dual-edit `capture-lake.ts` / `fabric/index.ts` / tracker mountain)

## Done bar

**Public door:** package index exports `routingWeightFromCapture`; `scoreSorCost` + `planRoute` refuse hole venues with `zero_weight` (see `capture-routing-public-doors.test.ts`).

Deepen Â§27 adapter score feed into SOR: a capture **hole** (absent / failed / not connected) contributes **zero** routing weight under the complete cost model. Structural input matches #1739 `CaptureRecord` (`kind: 'book' | 'hole'`) so lake records plug in without inventing empty books or mids.

## Non-goals

- Third venue / CCXT / TSDB invent
- Dual-edit #1739 files
- Letterâ†’bps invent / mark `connect.data-lake` or `execution.sor` done
