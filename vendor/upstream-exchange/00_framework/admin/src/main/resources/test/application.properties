

# Shared secret sent on every wallet RPC call (header X-Rpc-Auth-Token).
# No default: the wallet RPC services reject unauthenticated calls, so a
# missing token must stop this service at startup, not at withdrawal time.
rpc.auth-token=${WALLET_RPC_AUTH_TOKEN}
