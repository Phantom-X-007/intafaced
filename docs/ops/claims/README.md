# Atomic claim files

One file per claim: `pnpm swarm:claim <id>`.

**Do not hand-edit `docs/LIVE-LANES.md` mid-wave** — it sits inside open Denon PRs.

First writer creates the file; that is the lock. Update heartbeat / done bar in the same file.
