# D26-P1-T9 — Multi-asset instruments resume

**Owner:** Phantom-X-007 (Denon)  
**Branch:** `feat/trade-multi-asset-t9`  
**Done bar:** Additive model; closed-venue refuse; spot suite unchanged

## What this closes

DIRECTION §2 + ADR `2026-08-04-instrument-enum-authority` (D-S-05). The catalogue,
migration, and `assertMarketOpen` already landed via #102 / #167; #734 correctly
closed as superseded. This PR finishes the **enum-authority** half of the resume:

1. **`TRADING_SCHEDULES` is the sole schedule authority** — `scheduleKeySchema` /
   wire `marketScheduleKeySchema` derive from its keys (no handwritten mirror).
2. **Unknown schedule → `trade.unknown_schedule`** (names permitted keys) — not a
   500, not a soft open, distinct from session-closed `trade.market_closed`.
3. **Unknown `asset_class` → `trade.unknown_asset_class`** (names `ASSET_CLASSES`)
   on `assertTradable` before any hold.
4. **Additive spot bar** — crypto `crypto-24x7` stays open on the FX weekend;
   existing spot suite unchanged.
5. **Public-door proofs** — `instrument-enums-public-doors.test.ts` mounts
   Fastify public + private REST: GET `/api/v1/markets` sessionOpen (additive +
   closed venue) and POST `/api/v1/orders` through real
   `assertTradable` / `assertSettlementRails` / `assertMarketOpen` (not
   helper-only stubs).

## Explicitly NOT this mountain

- Settlement asset law / listing forex as fundable — **P0-05 / D26-P1-T7**.
- Options / CCXT capability matrix / MM honesty — T5 / T6 / T10.

## Sibling overlap (same Denon author)

T6 (#1716) / T7 (#1717) also touch `types.ts` / `ccxt-errors.ts` / `risk.ts` for
their refuse codes. T9 keeps new proofs in `instrument-enums.ts` (+ tests) so
merge conflict surface is additive error-code wiring only.

## Leverage

Phase A: extend `packages/contracts` instruments + `svc-trade` spot instrument
enums / risk call sites — no greenfield SPA, no second book, no invented settlement.
