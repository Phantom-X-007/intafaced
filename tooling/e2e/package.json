{
  "name": "@intafaced/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "End-to-end tests against a running fleet, through svc-edge. §14.3.",
  "scripts": {
    "e2e": "vitest run",
    "e2e:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf .turbo"
  },
  "dependencies": {
    "@intafaced/auth": "workspace:*",
    "@intafaced/config": "workspace:*",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2",
    "vitest": "^3.0.5"
  }
}
