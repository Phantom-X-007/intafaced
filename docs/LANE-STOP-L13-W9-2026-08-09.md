# LANE STOP — L13 EDGE · wave 9 topup · 2026-08-09

```
LANE: L13 EDGE wave 9 topup
shipped: none this wave (prior seals still hold: #1480 outside-the-door + kill durability · #1511 flagEdgeGateway honesty + /ready rateLimit · #1510 hop-by-hop full strip + /ready not kill oracle + EDGE_BODY_LIMIT_BYTES + env blast-radius pin)
in flight: none
parked: multi-replica shared kill store (§13 invent fence) · refresh-token httpOnly cookie CORS/WS re-take (§13) · geo-IP region · edge.gateway flag still NOT_ENFORCED (status says so — do not pretend it gates) · streaming/WS proxy buffering · WS route-behind-edge product change (sibling WS lane / routing law, not edge micro) · EDGE_TRUST_PROXY topology on compose (operator call, not invent shared store)
Nitro must decide: none (Class X host/secrets prod only if/when deploy)
SAFE TO CLOSE: yes
tip: e0126fbb
```

## Tip ritual (this wave)

| Check                           | Result                                                                                                                   |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `origin/main`                   | `e0126fbb` — docs(ops): L12 WS wave 8 stop                                                                               |
| Open PRs                        | 3 — #1177 Shehzad protocol (babysit only); #1146/#1142 dependabot maven — **zero** `services/svc-edge/**` path intersect |
| claim-check `services/svc-edge` | **clear** of open PRs                                                                                                    |
| Sealed #1510 / #1511            | both ancestors of tip                                                                                                    |

## Re-verify (sealed properties · tip `e0126fbb`)

Ran in detached tip worktree after package builds:

```
cd services/svc-edge && pnpm test
→ Test Files  13 passed | 1 skipped (14)
→ Tests  254 passed | 5 skipped (259)
  (5 browser e2e skipped — no Chromium binary; suite skips loudly)
```

| Property                                                | Result   | Proof                                                                       |
| ------------------------------------------------------- | -------- | --------------------------------------------------------------------------- |
| Kill-switch fail-closed + release asymmetry             | **hold** | `kill-switch.test.ts` 42 green                                              |
| Console cannot green-halt `ws` / ledger / matching      | **hold** | arm → 400; status `outsideTheDoor.*` (#1480)                                |
| Multi-replica kill not invented                         | **hold** | `killState.multiReplicaShared: false` always                                |
| CORS no `*`, no credentials, preflight not route oracle | **hold** | `cors.test.ts` 58; browser suite skip-loud without Chromium                 |
| `edge.gateway` not a fake kill                          | **hold** | status `flagEdgeGateway.enforced: false` + pin tests (#1511)                |
| Rate-limit posture honest                               | **hold** | `/ready.rateLimit` + `multiReplicaShared: false`                            |
| Hop-by-hop class complete                               | **hold** | full RFC set stripped (#1510)                                               |
| `/ready` not a halt oracle                              | **hold** | no `disabledModules` on unauthenticated `/ready` (#1510)                    |
| Body budget operator-visible                            | **hold** | `EDGE_BODY_LIMIT_BYTES` + 413 tests (#1510) — **not** still "Not built"     |
| Env blast radius                                        | **hold** | schema pin excludes `DATABASE_URL` / `NATS_URL` / `INTERNAL_SERVICE_SECRET` |
| Boot proxy honesty                                      | **hold** | `edge.no_route` + `edge.upstream_unavailable` via real `index.ts` boot e2e  |

## Engine A residual disposition (wave 9)

| Unit                                      | Disposition                                                        |
| ----------------------------------------- | ------------------------------------------------------------------ |
| A0 open edge PR merge                     | **none** — no open edge PR                                         |
| A1 kill-switch residual                   | **empty** — operator halt real; unarmable named; fail-closed holds |
| A1 hop-by-hop / ready not kill oracle     | **holds** #1510                                                    |
| A1 flag-only gateway kill                 | **holds** #1511                                                    |
| A2 CORS residual                          | **empty** for craft — sealed suites green; DELETE honesty holds    |
| A2 outside-the-door residual              | **holds** — 400 not green lie; status names gap                    |
| A2 edge.gateway NOT_ENFORCED honesty      | **holds** — status + pin; enforcement still product PR (park)      |
| A2 control-plane residual                 | **empty** — e2e + admin-api green                                  |
| A3 rate-limit multiReplicaShared honesty  | **holds** — always false on `/ready` + status                      |
| A3 Engine B full pass                     | **done** this note                                                 |
| A3 path-intersect WS                      | **clear** — no dual-write; L12 owns socket product residual        |
| A3 L6 multi-replica kill / refresh cookie | **named park** §13 — invent fence                                  |

## Engine B (promise falsify — unbounded)

| Chapter              | Verdict                                                                                                                                                |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Why it exists        | **holds** — sole producer of principal signature for `scopedProcedure`                                                                                 |
| API contract         | **holds** — route table ↔ UPSTREAMS pin; `/metrics` + `/admin/*`; body budget on `/ready`                                                              |
| Security properties  | **holds** — reserved strip first; region server-side; bearer never forwarded; bad token → anonymous; hop-by-hop complete; no DB/bus/service secret     |
| Browser origins CORS | **holds** — no `*`; no credentials; OPTIONS not proxied; `/admin` not CORS surface; preflight not kill/route oracle                                    |
| Kill-switch          | **holds** — operator surface live; outside-the-door; flag honesty; release asymmetry (tRPC cancel + CCXT DELETE + futures close); path-resolve + batch |
| Configuration        | **holds** — rate limit + body limit + kill path + trust proxy documented and tested                                                                    |
| Not built yet        | **holds** — only true residuals named (geo-IP, multi-replica kill, cookie re-take, WS proxy buffer, flag enforce)                                      |

## Engine C (attack surface)

| Vector                    | State                                                                            |
| ------------------------- | -------------------------------------------------------------------------------- |
| kill fail-open            | refused (undecidable → 503)                                                      |
| CORS free origin          | refused (no `*`; enforced envs closed door)                                      |
| fake WS close             | refuse to arm; status names gap                                                  |
| admin green while dead    | ledgerConfigured boolean; freeze pass-through; `flagEdgeGateway.enforced: false` |
| hop-by-hop smuggle        | full class stripped                                                              |
| `/ready` halt oracle      | closed                                                                           |
| secret leak               | edge still holds no `INTERNAL_SERVICE_SECRET` / DB / bus                         |
| invent multi-replica kill | refused — `multiReplicaShared` always false                                      |

## Anti-pad

No Engine A craft row invented to look busy. Prior waves (L15 W8 / L16 W7) already emptied L1–L4 edge craft; this wave re-derived tip, re-ran the suite, and found **no new reachable break** inside `services/svc-edge/**`.

Widening `ALWAYS_ALLOWED_PROCEDURES` past leaf `cancel` (e.g. pay refund as release) remains a deliberate product PR with an argument — code comment forbids silent expansion; not residual pad.

## Path-intersect this wave

- Open: #1177 protocol, #1146/#1142 dependabot — **no** edge files
- claim-check: clear for `services/svc-edge`
- Wall exclusive: no code PR this wave
