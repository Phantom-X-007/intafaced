# Grok frontend GO — operator brief

**Status:** Grok-owned execution contract. Product map stays [`FRONTEND-REMAINING-SOT-2026-08-25.md`](FRONTEND-REMAINING-SOT-2026-08-25.md) on **origin/main** (door copy may be stale).  
**Nitro says:** `go`  
**Grok is the boss from that word.** Do not re-interview him for this split, this order, or these unspoken needs.

This file is not a second remaining-SOT and not a 14-mountain DAG. It tells Grok how to finish the existing frontend without painting, and when Codex must be called.

---

## 0 · What “go” means

1. Fetch `origin/main`. Work only in `pnpm wt` off that tip. Never the Grok door.
2. Run the sequence in §6. Classify every slice LOOK / TRUTH / BOTH (§3). Dispatch Codex only for LOOK (and the look half of BOTH).
3. Stay the orchestrator. Use **supervised** Orca dispatch (`orca orchestration …`). Do **not** full-handoff and stop.
4. If Codex is required and unavailable: **stop that look slice**. Continue only TRUTH work that does **not** touch Vue chrome/CSS/N4/ticket-strip/blotter layout. **Never substitute Grok paint. Never mount new ticket/admin controls as “truth-only.”**
5. Stop asking Nitro. Unblock with refuse-closed code. Taste pass = delivered 1440+390 crops at (a) skill-v1 methodology checkpoint, (b) Money / Bank / Pay Layer A, (c) end-of-round. Not a chat interview. SOCKET items (Advanced Charts access, Trading Platform, hotkey blast-radius policy, layout-share, mobile control plane) stay SOCKET — `go` does not close them.
6. Merge TRUTH PRs under AGENTS.md. LOOK/BOTH PRs are **illegal to merge** until an **independent certifier** has crop-true on that SHA (1440+390 hashed files). Certifier ≠ painter (Codex, or a fresh Grok with no paint memory). CI is informational. The orchestrator chat must not author and certify the same look change.

His later message may be only `go`. **Chat is not memory.** This session will compact. After every compact or cold start:

1. `git fetch origin main`
2. Read **this file from origin/main** (`docs/PROMPT-GROK-FRONTEND-GO.md` in the worktree, or `git show origin/main:docs/PROMPT-GROK-FRONTEND-GO.md`). Do not reconstruct the plan from a chat summary.
3. Read remaining-SOT the same way (`git show origin/main:docs/FRONTEND-REMAINING-SOT-2026-08-25.md`). The Grok-door copy may be stale. Never inventory `/Users/Nitro/projects/Sovereign` as tip.
4. Progress = merged PRs on `origin/main` plus open PRs. Not a LIVE tracker. Not this thread.
5. Resume the first incomplete §6 step. Do not restart planning.

Do not swallow this file into a feature PR. It is the operating contract; remaining-SOT stays the product map.

---

## 1 · Locked operator needs (do not ask again)

He directs. He cannot read code. The crop is the result.

- Deliver 1440 and 390 crops as files. A localhost URL is convenience, not proof.
- Do not ask him to run git, install, or a terminal command.
- Do not wait for CI green or `pnpm verify`.
- Do not invent balances, fills, prices, limits, sanctions, or production magnitudes.
- Do not build a second SPA, design system, or money book. Shell is vendored Bazaar `vendor/upstream-exchange/05_Web_Front`. Value stays in `packages/ledger-client`. Decimal strings on the wire; scaled bigint in memory.
- N4 is closed: near-black, square, no glass, no orange identity, green/red = market only. Remaining visible orange is a janitor defect with before/after crops — not a new palette. **N4 closed does not mean Money/Bank/Pay look is done.** Those rooms still need Codex crops; his eye on them is still open.
- Session → ticket → blotter. Do not polish LWC. Do not implement Advanced Charts until access. **Chart freshness is not skipped:** latest-request-wins, snapshot vs live vs stale vs reconnecting vs as-of is Grok TRUTH on the existing host. Close (`DELETE /api/v1/positions/:id`) is not flatten. Destructive flatten/reverse/join stay off until a blast-radius payload.
- Money, Bank, Pay are separate Layer A surfaces. Bank does not cover Pay. `/bank` is not `/bank/business`. `/pay` is not `/pay/checkout`. Venue wallet vs platform ledger vs bank space stay **labeled**. Never two unlabeled “totals.”
- No persistent browser bearer. Protected UI does not paint before the authority check. No optimistic success; timeout is unknown until the service reconciles.
- Admin is `apps/admin`, not undeployed `04_Web_Admin`. One service/surface per PR.
- He is not the pipeline’s critical path except: new factual claims, irreversible acts, spend beyond the stated map, and the visual taste pass on delivered crops.

---

## 2 · Stronger frontend (why Codex exists)

Grok 4.6 is worse at look than Codex. That is accepted. The fix is **not** “Grok tries harder at CSS in the same chat.” **Operator override 2026-09-05:** leftover LOOK (desk touch, admin queue density, Money/Bank/Pay Layer A) may be Grok-painted **iff** a second Grok (fresh worktree, no paint memory) crop-trues 1440+390 and Impeccable critique/audit (detector on). Same agent may not author and certify. Codex remains the uncorrelated painter when available. Grok still certifies **truth** (fixture, session, money honesty, provenance). Taste stays the owner. SOCKET stays SOCKET.

Ambition survives. If Vue 2 cannot deliver a named M07 NOW job, **refuse-closed control** — not a prettier fake, not a silent cut to “chart + ticket.”

---

## 3 · LOOK / TRUTH classifier (automatic)

Before every slice, write one line in the PR/body: `CLASS: LOOK | TRUTH | BOTH`.

**LOOK — painter ≠ certifier.** Codex preferred. Grok may paint leftover slices under the 2026-09-05 override; a **fresh** Grok certifies. The orchestrator does not sign its own paint.  
A stranger judges it in ~2 seconds: spacing, type, density, N4, 390 composition, ticket/blotter/admin-queue chrome, identity janitor (visible orange), crop appearance.

**TRUTH — Grok only. Codex must not.**  
Launcher, Node 24, lockfile, unique-port boot, fixtures, session fail-closed, recovery/idempotency, capability **tests**, `closePosition` wire, blotter empty-reason **copy only if IxState already exists**, admin BFF/source-backed tables, matrix/goldens as executable truth, decimal/fixed-point, refuse-closed holes, chart **freshness** (no LWC restyle), Graphify after `services/`/`packages/`, evidence vocabulary, adversarial money review.

TRUTH may not add or restyle Vue chrome, CSS, N4 tokens, ticket strip, blotter layout, or admin density. New visible controls = BOTH.

**BOTH — two PRs, same family, Grok first.**

1. Grok ships behavior + honest states using existing IxState (no new paint, no new buttons).
2. Codex look PR: density, 390, N4, **mount refuse-or-real ticket controls**, crops.  
   Never one PR where Grok invents look. Iceberg/peg/collar/close chrome is Codex-mounted; Grok writes the tests and the door.

**Codex dispatch (supervised, Grok waits):** bounded brief only — route, fixture class, falsifier, N4, “do not invent money,” “do not second SPA,” remaining-SOT cite. Codex marks look crop-true. Grok marks truth (provenance, session, fixture honesty). Same agent may not author and certify the same material change. Money-facing: extra adversarial pass by someone other than the **fixture author** and other than the look author. 503 + memory session is never a money proof.

---

## 4 · Implicit requirement loop (mandatory, every lane)

Copy remaining-SOT §11. Do it before code. Do not skip because the mountain “looks specified.”

1. Name the user decision (believe / choose / authorize / recover).
2. Name the authority (`svc-*`, ledger-client, identity session, market feed). None → refuse closed.
3. Enumerate states, at minimum: loading, reachable-empty, **reachable-zero**, live, stale, partial, refused, unauthorized, unconfigured, malformed, timeout, unknown outcome, recovered.  
   Failed ≠ `$0` ≠ empty. Unauthorized ≠ anonymous. Memory+HTTP 503 on `/uc/money` is authenticated degraded — not a ledger, not a balance.
4. Follow transitions: refresh, back/forward, reload, duplicate tab, slow/reordered response, disconnect/reconnect, expiry, resize, zoom, route change.
5. Input modes: keyboard, pointer, touch, AT, zoom/reflow, reduced motion, storage-disabled.
6. Research only the unstable normative question. Primary sources. No second stack.
7. Write the falsifier first.
8. Close with durable evidence. Scorecard indexes; it does not replace crops.

WCAG 2.2 AA is the a11y target. Axe does not certify it. Named AT claims need route, fixture, browser, AT+version, viewport, named task, expected vs observed.

**In force every lane — not only the final DoD sentence.** remaining-SOT §4, §12, and §15.2–15.3 apply as you code, not as a mop-up.

- §4: quiet honesty; dual-book labels; no fake live-ness; `/exchange` stays desk chrome; Money/OS get a thin OS header (not the ticker row); **iView 3 one kit** (no Tailwind/shadcn/new component kit); 390 first-class; Graphify does not map vendor Vue.
- §12.2: authority is not a truthy “session”; no persistent bearer; titles, stable H1, skip path, branded 404.
- §12.3: 320 CSS px + 400% reflow; 768/1024 as well as 1440/390; 24×24 targets (44 px on submit/cancel/high-risk); drag has pointer + keyboard alternatives; color is never the only signal.
- §12.4: visibility/reconnect revalidates; error boundaries isolate a failing module; retry never turns unknown into success.
- §12.5: admin **denies when auth config is blank**; consequential confirmations bind exact facts (what you see is what you sign); CSP as specified; checkout/widget scoped separately.
- §12.6 / §19.7.8: browser-support policy + perf/RUM are residuals, not “done at T2.”
- §15.2: Tier A = every route 1440+390; Tier B family adds 320, 768, 1024, 200% zoom, reduced motion, high contrast, keyboard, focus, axe — not screenshot goldens unless screenshots were compared. Tier C workflows and Tier D human/AT are DoD, not the five-path calibration.
- §15.3: freeze locale/timezone/clock; mask never covers balance/status/authority/receipt; scorecard append-only.

**NOW work is still written when it cannot yet be BROWSER-PROVED.** Recovery-lock, unknown writes, reachable-zero live: implement refuse-closed behavior; keep the claim SOURCE-READ until a named real fixture exists. Do not skip R11 because the four calibration classes cannot prove it. Do not call it BROWSER-PROVED on memory+503.

**Named residuals (do not treat as closed):** FE-P0 evidence still belongs in the §18.2 return gate; `ix-money.ratio()` IEEE leftover is Grok TRUTH; Greeks on screen are decimal strings from our adapter, never QuantLib-in-the-browser; Advanced Charts ≠ Trading Platform; Alerts and Bar Replay stay refused even after AC access.

---

## 5 · Evidence law (folded B1–B4)

**DoD is remaining-SOT §18.2 copied literally.** This file’s sequence is a wrapper. Checking the wrapper is not frontend-done.

Claims are exactly one of: BROWSER-PROVED · SOURCE-READ · UNVERIFIED · REFUSED.

Four live fixture classes only, until a named real fixture exists:

1. Anonymous + dependencies down
2. Memory-authenticated + dependencies down
3. Reachable but genuinely empty
4. Explicitly refused / not built

The wider inventory (kill switches, Class X, subaccounts, paper/live, fills, reconnect-lock, unknown write, reachable-zero live, admin consequential) stays SOURCE-READ / UNVERIFIED / REFUSED until a real fixture exists. **Do not seed balances.** Fixture expansion is a **claim gate**, not a new mountain.

Methodology calibration (after F0 + unique-port boot) — five paths, none a stand-in for another:

- `/`
- `/login`
- `/exchange/btc_usdt`
- `/uc/money` (include signed-out as its own combo; this route is not `requiresAuth`)
- `/bank` (OS glance, remaining-SOT mountain 2)

`/bank/business` is a different page (tables/payroll). Matrix family `bank` currently points at `/bank/business` — do not let that cover `/bank`, and do not let `/bank` cover business. Pay is **out** of this calibration. Later Layer A: `/pay` and `/pay/checkout` separately (glance ≠ public checkout; checkout is embeddable / CSP-scoped). SHOW-PAY does not exist; absence is not permission to invent Pay style.

v1 is the **methodology** governor (provenance, fixtures, N4 law, evidence vocabulary). It is **not** a visual freeze of ticket/blotter/admin/Pay. Those still get their own Codex look PRs.

Every BROWSER-PROVED crop records: route, fixture, browser, viewport, SHA, worktree, evidence file, user task, API/session behavior. Unique port. Bind server to this worktree + SHA. Never reuse a foreign process because `:8090` returned 200. Ready for a desk claim is that **desk route**, not marketing `GET /`. Tracked PNGs are not a current pass. “211 green” / Axe / 10 calibration PNGs are not design, a11y, or §18.2. Axe here is WCAG 2.0/2.1 tags; target remains WCAG **2.2** AA.

`intafaced-ui` v0: Grok authors truth/provenance/fixtures/money/a11y-evidence. Codex authors look routing / Impeccable allow-deny / N4. Neither merges v0/v1 alone. v1 does not govern remaining work until look+truth+a11y transcripts exist **and** 1440+390 calibration crops are delivered files.

Impeccable: pin `c0f495212236129c2e92aaf7714a3a9914569d13`. Allow document/layout/quieter/distill/harden/clarify/critique/audit/adapt/polish/shape. Forbid init/extract. Forbid Impeccable `live` CLI (including inside Orca). Critique without detector = degraded, never “full Impeccable.” Vercel pin `e3d624baaf29dc1fc645aff3e38f03e564d2d6b1`. No Taste-Skill. No Anthropic frontend-design. No `npx` installer.

---

## 6 · Sequence (wrapper — §20 is the product order)

Fresh worktrees from then-current origin/main. Do not reuse dirty `91c9f2eef` trees (including `chore-lockfile-frozen-t2`) as evidence.

1. **T1 TRUTH** — launcher uses absolute repo-pinned pnpm (`…/.tools/bin/pnpm`). Known bug: `tooling/scripts/worktree.mjs` `spawnSync('pnpm', …, { shell: true })` on PATH. Provision Node 24 from a pinned checksummed source. Falsifiers: PATH `pnpm`; missing checksum; silent Node 26; only works in one interactive shell. **T1 must not require frozen-install success.**
2. **T2 TRUTH** — repair lockfile with that pinned pnpm. Falsifier: `pnpm install --frozen-lockfile` exits 0 **and** `pnpm-lock.yaml` bytes unchanged. Diff = manifest-required resolution only (expected: `@asteasolutions/zod-to-openapi@7.3.4` + `openapi3-ts`). No graphify-out. A second non-frozen `--lockfile-only` against a live registry is not the test. **T2 is not Wave 3.** Leftover Vue 2.7 / less-loader / Node LTS member-build remains a later toolchain PR.
3. **F0** — T1 and T2 both pass. No visual implementation before F0.
4. **Proof provenance TRUTH** — unique port; SHA/worktree bind; kill only the process you spawned.
5. **Skill v0** — split authorship (§5). States “no UI is certified yet.”
6. **Calibrate** the five paths in §5 × applicable fixture classes. Name inapplicable combos. Codex LOOK transcripts + crops (Codex crop-true). Grok TRUTH transcript. AT only if actually performed.
7. **v1 freeze** — after independent reviews + delivered crops (taste pass).
8. **Product order = remaining-SOT §20** (one family per PR). Order is not a drop-list. Named set in §7 stays in play even if not “next.”  
   session truth (R09+R11+R10) → ticket honesty (R04) → blotter+risk (R05+R06) → chart **freshness** (Grok, no LWC polish) → admin queue **look** (Codex) + admin browser harness (Grok) → hashed uiproof crops → Money / Bank / Pay Layer A as **separate** surfaces with Codex crops and a taste pass.  
   Grok: `closePosition` wire, capability tests, empty-reason if IxState exists, refuse rows, EMS parent+child **read-only if the wire returns it**. Codex: chrome judged in 2s, including refuse-or-real ticket mounts.
9. Graphify after `services/` or `packages/` edits. Never commit graphify-out on a product PR.
10. **Return-to-DoD gate.** Before any “frontend done” sentence, re-read remaining-SOT §18.2 and mark every bullet BROWSER-PROVED / SOURCE-READ / REFUSED / SOCKET with a named evidence object. v1 freeze, 211 cells, Axe, or hashed crops are not that gate. Tier C/D and M07 NOW rows cannot close on the four calibration fixtures.

---

## 7 · Named set (drop none; stop at refuse)

**M07 NOW (code or refuse-closed control):** R01 local Reset/⌘K residual (⌘K must reach `/quant` `/execution` `/ops` `/market` `/support` `/portfolio` `/predict` `/mining` — not dropped because Reset already landed), R03 densify existing book/tape/watchlist (no heatmap), R04 capability matrix + ticket refuse-or-real, R05 blotter unify existing APIs, R06 risk strip if wire returns it, R08 a11y/degraded, R09 per-channel status, R10 lock+banner, R11 recovery-lock, R13 EMS tree read-only if wire returns it, R14 instrument-borne funding/expiry only, R15 local version+Reset, R17 cancel/cancel-all/staged reprice/**close** (`DELETE /api/v1/positions/:id`, ACCEPTED/REJECTED/UNKNOWN — not a fill).

**REFUSE / SOCKET (do not “finish” by building):** R02 Advanced Charts until access; R03 heatmap/footprint until L3; R07 mobile control plane; R12 budgets later; R16 journal/replay; join/cross/flatten/reverse until blast-radius payload; M25 **care** chrome; TCA conclusions; custody `CustodyNotBuilt`; fake IV; pirate TV; second SPA; Hyperswitch; seeded money.

**Layer A residuals still open:** durable Tier-A crops; admin source-backed queues (withdrawal `NOT MOUNTED`); chart live STOMP/as-of is **not** a look polish (leave AC; Grok may truth-fix freshness without LWC restyle); browser/perf/RUM policy. §19.7 “chart drag-reprice” is superseded by §20: amend from the ticket.

**Other mountains after §19.4 NOW:** M08/M10/M11/M12/M14/M15/M16/M26/M27 and `/predict` `/mining` — screen or refuse; do not invent new routes.

---

## 8 · Codex brief (paste into the dispatch — nothing else)

```
LOOK only. Do not change session/money/authority/fixtures/lockfile/boot.

Route: <matrix path>
Fixture: <1–4 or inapplicable>
Viewports: 1440×900 and 390×844
N4: near-black, square, no glass, no orange identity, green/red = market only
Do not invent balances, fills, or live-ness
Do not second SPA / new kit / packages/ui as the member shell
Falsifier: <one sentence that would prove the crop lies>
Ship: hashed crops + worktree SHA. Codex marks look crop-true. Grok marks truth only.
```

---

## 9 · Horror list (named so they cannot recur)

- `boot.mjs` reuses any GET `/` 200 on `:8090` without cwd/SHA.
- `worktree.mjs` PATH `pnpm` (`shell: true`).
- System Node 26 vs shell `engines.node ^24`.
- Lockfile missing `@asteasolutions/zod-to-openapi@7.3.4`.
- Tracked visual PNG treated as a current pass.
- Axe or 211 matrix cells treated as design/a11y/browser certification.
- `auth.spec` 3 tests, not 5. Visual stubs 503+memory; accessibility does **not** stub 503.
- Memory+503 declared as proven `/uc/money`.
- `$0` painted for a down wallet.
- Graphify-out committed on a product PR. Official graphify hook skips worktrees.
- Dirty worktree used as evidence.
- Grok restyle because Codex was slow.

---

## 10 · Frontend done

Only remaining-SOT **§18.2**. Until then the status is: baseline shipped; closure in progress. Layer A chrome PRs do not imply Layer B desk done.
