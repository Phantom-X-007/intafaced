# LANE STOP — L02 PAY gateway + plugins · wave 14 · 2026-08-10

## Verdict

**SAFE TO CLOSE.** Wave 14 found no clear product-code unit beyond the wave-13 bank. Hosted checkout, payment links, the TypeScript plugin reference path, API-key authentication shape, idempotency pins, decimal-string money, and frozen webhook verification are on tip. The remaining gaps are owner law, Class X supply, or active Denon file walls.

## Unit card

1. **Promise quoted:** `INTAFACED_DEFINITIVE_BUILD.md` §6.1 — “Branded gateway: hosted checkout, payment links”; tracker `pay.plugins` — “Woo / Magento / OpenCart plugins”. The accepted wave-13 decision reclassifies the latter to the in-repo TypeScript reference path plus frozen contract vectors (`docs/pay/PLUGINS-REFERENCE-PATH-2026-08-10.md`).
2. **Reachable break on tip:** none outside fenced files. Checkout/link routes and tests are present; plugin request/auth/idempotency/webhook tests are present.
3. **Done bar:** a payer can open an honestly configured hosted checkout; a downstream store integration has a tested request/auth contract and can reject forged or stale webhook delivery.
4. **Class:** M for gateway money paths; N for the reference client. No money code changed this wave.
5. **Paths:** read-only audit of `services/svc-pay/**`; this stop and its claim are docs-only.
6. **RED first:** no honest RED exists without inventing a rate, grant, rail, or product rule. Existing RED coverage already pins missing idempotency, numeric money, tampered webhook bodies, stale timestamps, and non-live checkout rails.
7. **Collisions:** Denon #1625 owns `payment-service.ts`, `public-rest*`, quickstart and `features.mjs`; #1627 owns `payment-service.ts`, `index.ts`, and `subscriptions/**`; #1626 owns `features.mjs`/LIVE-LANES. None were edited.

## Engine A — ranked residual

| Unit                   | Finding                                                                                                                                         |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| Denon path-intersect   | **FENCED** — #1625 refund and #1627 subscription file sets remain single-writer.                                                                |
| Gateway product stage  | **BANKED** — payment links, checkout sessions, public HTML routes, live-rail posture gates, and tests exist.                                    |
| Plugins install/auth   | **BANKED #1633** — TypeScript reference client builds Bearer-authenticated public API requests and freezes webhook vectors.                     |
| Missing configuration  | **BANKED** — public checkout refuses `pay.checkout_rail_not_live`; staging/prod sandbox posture refuses at boot/runtime.                        |
| Ledger money paths     | **BANKED** — service uses ledger-client; no balance or second money book was introduced.                                                        |
| Acquirer/rates         | **PARKED** — live acquiring supply and fee/approval-rate content are Nitro/Class X or §8 decisions.                                             |
| Tracker mountain event | **DEFERRED** — `features.mjs` is inside open #1625/#1626 and the gateway cannot honestly read `done` while acquiring and KYB gates remain open. |

## Engine B — promise falsification

| Promise                                      | Evidence / verdict                                                                             |
| -------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| Gateway has hosted checkout                  | `checkout-page.ts`, checkout tRPC procedures, migrations 0002/0003, and route tests: **PASS**. |
| Payment links are bounded                    | expiry/max-use/session state is persisted and tested: **PASS**.                                |
| Plugin path has real authentication shape    | reference requests require Bearer API key and money POST idempotency: **PASS**.                |
| Plugin money stays decimal-safe              | amount must serialize as a decimal string: **PASS**.                                           |
| Webhooks resist forgery/replay               | HMAC vectors match core signer; tampered bodies and stale timestamps refuse: **PASS**.         |
| Production cannot masquerade sandbox as live | boot/runtime posture gates and checkout-specific refusal: **PASS**.                            |

## Engine C — attack surface

| Attack                  | Disposition                                                                                         |
| ----------------------- | --------------------------------------------------------------------------------------------------- |
| Configuration invention | Missing live rail refuses; no approval rate or cost is synthesized.                                 |
| Double charge by retry  | Money POST helpers require `Idempotency-Key`; Denon #1625 owns the active refund correction.        |
| Plugin auth forgery     | API key is carried only in `Authorization: Bearer`; no merchant identity is invented by the client. |
| Webhook forgery/replay  | Raw-body HMAC, constant-length comparison, and timestamp tolerance reject tamper/stale delivery.    |

## Parked / pickup

1. Live card acquirer and fee schedule — Nitro/Class X commercial supply.
2. `pay:*` grant path and KYB money-gate product law — owner decision; do not infer from existing flags.
3. PSP/PayFac product-complete engines — Denon invent-risk fence.
4. Any work in #1625/#1627 paths — wait for those PRs to land, then re-derive residual from tip.

```text
LANE: L02 wave 14 product-velocity substance
shipped: no new product PR — wave 13 #1633 plugin install/auth path and existing hosted checkout remain the complete clear bank
in flight: Denon #1625 refund · Denon #1627 subscriptions (babysit only)
parked: live acquirer/fees Class X · pay:* grants and KYB money-gate law · PSP/PayFac invent-risk · tracker event behind open-file fence
Nitro must decide: live acquirer/fee supply or pay:* / KYB gate law; otherwise none
SAFE TO CLOSE: yes
tip: 3641d8b1
```
